---
phase: 08-xrandr-vnc-compatibility-focus-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - src/Events.cpp
  - tests/support/WmFixture.h
  - tests/support/XTestDriver.h
  - tests/test_wm_process.cpp
  - tests/lsan.supp
  - scripts/preflight.sh
autonomous: true
requirements: [TEST-05, TEST-07]

user_setup:
  - service: "local apt archive (Ubuntu 22.04)"
    why: "libxft-dev + libfontconfig1-dev are absent; the project cannot be configured or built until they are installed. libxtst-dev is required for the D-08 XTEST input harness."
    dashboard_config:
      - task: "Grant passwordless sudo, or run `sudo apt-get install -y libxft-dev libfontconfig1-dev libxtst-dev xterm` manually before executing this plan"
        location: "developer workstation shell"

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`cmake -S . -B build/debug -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTS=ON` succeeds from a clean tree with no pre-existing build/ directory (D-01)."
    - "A Catch2 test can fork/exec the real `wm2-born-again` binary onto a display it allocated itself, observe the WM reach readiness, and shut it down with SIGTERM (D-07)."
    - "Destroying a dock window under the running WM produces no AddressSanitizer report and leaves `_NET_WORKAREA` recomputed (checklist scan finding 2, coverage item 3)."
    - "`scripts/preflight.sh` exits non-zero when any of x11/xext/xft/fontconfig/xrandr pkg-config modules, the X11 tool set, a simple X client, or the fc-match fallback chains are unavailable (D-01)."
    - "The preflight runs as a ctest FIXTURES_SETUP so it fails before any behavioral test executes, and is not a CMake configure-time check (D-02)."
    - "Every `catch_discover_tests()` call registers Catch2 tags as ctest labels via `ADD_TAGS_AS_LABELS`, so `ctest -L '^<tag>$'` selects a non-empty set for every tag the phase gates on (D-33)."
    - "A ctest gate whose filter matches nothing exits non-zero rather than reporting success, because every invocation in the phase carries `--no-tests=error` (D-33)."
    - statement: "Process-level WM tests never share display `:99` with `test_client` / `test_ewmh` / `test_smoke` / `test_xft_poc`, and `ctest -j` cannot interleave the two groups."
      verification: backstop
  artifacts:
    - scripts/preflight.sh
    - tests/support/WmFixture.h
    - tests/support/XTestDriver.h
    - tests/test_wm_process.cpp
    - tests/lsan.supp
  key_links:
    - "WmFixture readiness poll -> `_NET_SUPPORTING_WM_CHECK` published by `WindowManager::setupEwmhProperties()` (src/Manager.cpp:452-460)"
    - "Child ASAN_OPTIONS log_path/exitcode -> waitpid status -> Catch2 failure (child sanitizer reports must not be silently swallowed)"
    - "ctest fixture `preflight` -> FIXTURES_REQUIRED on every display-backed target"
    - "Catch2 `TEST_CASE` tag -> `ADD_TAGS_AS_LABELS` -> ctest LABELS property -> the `-L '^tag$'` filter every gate in plans 08-01..08-14 depends on"
  prohibitions:
    - "Do not add any .github/workflows/ file or other CI pipeline; gates run locally only."
    - "Do not reuse or repair the existing `build/` directory; it records a stale XFT_FOUND and must be deleted."
    - "Do not use sleep()-based synchronisation anywhere in the harness; poll a property with a deadline."
    - "Do not run the process-level WM tests on display :99."
    - "Do not link libXtst into the shipped `wm2-born-again` target; it is test-only."
    - "Do not select tests by Catch2 tag with `ctest -R`; tags are labels and `-R` matches names, so such a filter silently matches nothing. Use `-L '^tag$'`."
    - "Do not write a ctest invocation without `--no-tests=error`; a zero-match run would otherwise report success."
    - "Do not add `TEST_PREFIX`/`TEST_SUFFIX` to any `catch_discover_tests()` call; it would rename tests out from under the label convention and cannot express multi-tag targets."
    - "Do not introduce C++20/C++23 language features."
---

<objective>
Unblock the build on this host, then land the thinnest possible end-to-end slice that proves the whole Phase 8 machine works: a Catch2 test that launches the real compiled `wm2-born-again` binary on a display it allocated itself, destroys a dock window through it, and proves under AddressSanitizer that the known `eventDestroy` use-after-free is gone.

Purpose: every other Phase 8 plan depends on (a) a configurable tree and (b) a process-level harness. Proving both on one real path first means an architectural dead-end in the harness surfaces after one commit, not after ten.
Output: a buildable tree, `scripts/preflight.sh` + its ctest fixture, `tests/support/WmFixture.h`, `tests/support/XTestDriver.h`, `tests/test_wm_process.cpp`, `tests/lsan.supp`, and the `eventDestroy` fix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-xrandr-vnc-compatibility-focus-rules/08-CONTEXT.md
@.planning/phases/08-xrandr-vnc-compatibility-focus-rules/08-RESEARCH.md
@.planning/phases/08-xrandr-vnc-compatibility-focus-rules/08-PATTERNS.md
@COMPILED_CODE_BEHAVIOR_CHECKLIST.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "destroy a dock window under the real WM, ASan-clean" — one path only</name>
  <precondition>`sudo apt-get` can install packages on this host (passwordless sudo or an operator-run install). Without it the tree cannot be configured and this task halts.</precondition>
  <files>CMakeLists.txt, src/Events.cpp, tests/support/WmFixture.h, tests/test_wm_process.cpp, tests/lsan.supp</files>
  <read_first>
    - src/Events.cpp (lines 230-290 — the `eventDestroy` STEP 1..STEP 4 sequence and the post-erase `c->isDock()` dereference)
    - include/Manager.h (lines 20-33 `FdGuard` — the in-repo move-only handle wrapper to copy; lines 118-121 the `unique_ptr` client vectors that make the erase destructive)
    - src/Manager.cpp (lines 452-470 `setupEwmhProperties()` — publishes `_NET_SUPPORTING_WM_CHECK`, the readiness signal; lines 170-186 — ordering of `initialiseScreen()` vs `scanInitialWindows()`/`loop()`)
    - tests/test_ewmh.cpp (the `x11::DisplayPtr` + six-out-param `XGetWindowProperty` + `REQUIRE(status == Success)` assertion shape to copy)
    - CMakeLists.txt (lines 9-14 pkg-config block; lines 85-102 the Xvfb fixture idiom; lines 166-180 a display-backed target)
    - .planning/phases/08-xrandr-vnc-compatibility-focus-rules/08-RESEARCH.md (Pattern 1 WmFixture sketch; Pitfall 4 ASan child capture; Pitfall 9 the exact use-after-free)
  </read_first>
  <behavior>
    - Test 1 (the tracer): `WmFixture` allocates a free display, starts Xvfb on it, execs `wm2-born-again`, and `_NET_SUPPORTING_WM_CHECK` becomes readable on that display's root within the deadline.
    - Test 2: a dock window (`_NET_WM_WINDOW_TYPE_DOCK` + `_NET_WM_STRUT_PARTIAL`) mapped on the fixture display is picked up by the WM and `_NET_WORKAREA` shrinks accordingly.
    - Test 3: destroying that dock window leaves `_NET_WORKAREA` restored to full screen dimensions, the WM still alive and responsive, and zero AddressSanitizer report files produced by the child.
    - Test 4: `SIGTERM` to the WM child produces exit status 0 within the deadline (not the ASan `exitcode` sentinel, not a signal death).
  </behavior>
  <action>
Install the two build-blocking dev packages and stand the tree back up, then wire one complete vertical path from the build system through production source, the new harness, the real binary, and an X-protocol assertion.

Step A — environment. Run `sudo apt-get install -y libxft-dev libfontconfig1-dev libxtst-dev xterm`. Then delete the entire existing `build/` directory (it records a stale `XFT_FOUND:INTERNAL=1` and an empty `CMAKE_BUILD_TYPE`, so reusing it can make a broken build look green) and configure three fresh trees: `build/debug` with `-DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTS=ON`, `build/release` with `-DCMAKE_BUILD_TYPE=Release -DBUILD_TESTS=ON`, and `build/asan` with `-DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTS=ON -DCMAKE_CXX_FLAGS="-fsanitize=address,undefined -fno-omit-frame-pointer" -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=address,undefined"`. Confirm `.gitignore` still excludes `build`.

Step B — production fix (scan finding 2, per the checklist "Current Scan Findings" entry for `src/Events.cpp:237-283`). In `WindowManager::eventDestroy`, capture the dock-ness of the client into a local `const bool wasDock` immediately after the null check and before STEP 3, and branch the post-erase workarea recomputation on that local instead of dereferencing the pointer whose owning `unique_ptr` was just erased. Cite the decision in a comment in the codebase's established `// D-xx:` form, referencing the checklist scan finding. Change nothing else in the function.

Step C — the harness, one path only. Create `tests/support/WmFixture.h` as a header-only RAII class following RESEARCH Pattern 1 and the `FdGuard` move-only shape from `include/Manager.h:20-33`. It must provide, at minimum: a free-display allocator that scans upward from a base offset and takes the first `n` where neither `/tmp/.X<n>-lock` nor `/tmp/.X11-unix/X<n>` exists (never probe with `XOpenDisplay` — a successful open proves the display is taken, not free); `spawnXvfb()` accepting extra server arguments so later plans can pass `-extension RANDR` / `-extension RENDER`; `waitForXServer()`; `spawnWm()` which `fork()`s then `execv()`s the `wm2-born-again` binary located from a CMake-injected absolute path, with `--new-window-command=xclock` (never `xterm` — it may not be installed) and with `ASAN_OPTIONS`, `LSAN_OPTIONS`, `UBSAN_OPTIONS` explicitly `setenv`'d in the child before exec, using an ASan `log_path` under the CMake binary directory (never a bare fixed `/tmp` name) plus a distinctive `exitcode`; `waitForWmReady()` which polls root for `_NET_SUPPORTING_WM_CHECK` with a wall-clock deadline; a `display()` accessor returning the fixture's own display string; and a destructor that SIGTERMs the WM, `waitpid`s it, globs and reports any ASan log files before killing Xvfb, then SIGTERMs Xvfb. Tests must read the fixture's display string, not the process `DISPLAY` env var.

Write `tests/lsan.supp` with leak suppressions for the X/font stack that allocates never-freed caches by design: `libX11.so`, `libXft.so`, `libfontconfig`, `FcFontSet`, `FcPatternObject`. The `LSAN_OPTIONS` string must set `fast_unwind_on_malloc=0` or the stripped system frames never appear and the suppressions never match.

Step D — the tracer test. Create `tests/test_wm_process.cpp` with the four behaviors above and no others. Tag them `[wm_process]`. Use `x11::DisplayPtr` for every connection (never a raw `XOpenDisplay` plus manual close) and the six-out-param `XGetWindowProperty` assertion triple from `tests/test_ewmh.cpp`.

Step E — build wiring. In `CMakeLists.txt`, inside `if(BUILD_TESTS)` and guarded by `XVFB_EXECUTABLE`, add a `test_wm_process` target linking `Catch2::Catch2WithMain` and `PkgConfig::X11`, including `${CMAKE_SOURCE_DIR}/include` and `${CMAKE_SOURCE_DIR}/tests`, with `target_compile_definitions` carrying two string macros: `WM2_BINARY_PATH`, set from a CMake `TARGET_FILE` generator expression for the `wm2-born-again` target, and `WM2_LSAN_SUPPRESSIONS`, set to `${CMAKE_SOURCE_DIR}/tests/lsan.supp`. Add `add_dependencies(test_wm_process wm2-born-again)`. Register with `catch_discover_tests(test_wm_process ADD_TAGS_AS_LABELS)` and deliberately do NOT attach `ENVIRONMENT "DISPLAY=:99"` or `FIXTURES_REQUIRED xvfb_display` — this target owns its own server.

Step F — the tag-selection convention, established here once for the whole phase (D-33). `catch_discover_tests` registers each ctest test under its `TEST_CASE` *name*, so a Catch2 tag is invisible to `ctest -R`: a tag-shaped `-R` expression matches zero tests, and ctest exits 0 on zero matches, making every such gate green-on-nothing. Fix it at the registration site rather than at the call site, and retrofit the whole file in the same commit so the tree never holds two conventions:

1. Append the `ADD_TAGS_AS_LABELS` option (Catch2 v3.14 `extras/Catch.cmake`, documented at its line 152) to **every** `catch_discover_tests()` call in `CMakeLists.txt` — the ten pre-existing ones for `test_raii`, `test_smoke`, `test_eventloop`, `test_autoraise`, `test_client`, `test_xft_poc`, `test_ewmh`, `test_config`, `test_desktopentry`, `test_binaryscanner`, `test_appcache`, plus the new `test_wm_process`. The option is orthogonal to the existing `PROPERTIES ENVIRONMENT ... FIXTURES_REQUIRED ...` arguments: Catch2 emits the labels through a separate `set_tests_properties` call, so nothing already attached is clobbered.
2. Every ctest gate in this phase selects by label, anchored, never by name: `ctest -L '^wm_process$'`, `ctest -L '^(wm_lifecycle|wm_sizehints|wm_gravity)$'`. Anchoring is load-bearing — `-L` is an unanchored regex, so a bare `-L config` would also drag in `wm_config_runtime`.
3. Every ctest invocation in the phase carries `--no-tests=error` (ctest >= 3.20; this host runs 3.22.1). Without it a zero-match run still exits 0; with it, exit code 8. This is the backstop that makes a mistyped or renamed tag fail loudly instead of passing silently.

Labels are per-*test*, not per-target, which is why this mechanism and not `TEST_PREFIX` — several later targets carry three tag groups in one file (`test_wm_fallbacks`, `test_wm_lifecycle`, `test_wm_state`, `test_wm_runtime`) and a per-target prefix could not address them separately. Record the convention in a `# D-33:` comment above the first `catch_discover_tests` call.
  </action>
  <verify>
    <automated>cmake --build build/debug --parallel &amp;&amp; ctest --test-dir build/debug -L '^wm_process$' --output-on-failure --no-tests=error</automated>
    <automated>cmake --build build/asan --parallel &amp;&amp; ctest --test-dir build/asan -L '^wm_process$' --output-on-failure --no-tests=error</automated>
  </verify>
  <acceptance_criteria>
    - `pkg-config --exists x11 xext xft fontconfig xrandr` exits 0
    - `test -d build/debug -a -d build/release -a -d build/asan` exits 0 and `test -f build/CMakeCache.txt` exits non-zero
    - `src/Events.cpp` contains the token `wasDock`
    - `grep -c 'c->isDock()' src/Events.cpp` equals 0
    - `test -f tests/support/WmFixture.h -a -f tests/lsan.supp -a -f tests/test_wm_process.cpp` exits 0
    - `grep -v '^\s*//' tests/support/WmFixture.h | grep -c 'sleep('` equals 0
    - `grep -c 'DISPLAY=:99' CMakeLists.txt` equals 4 — the four legacy display-backed targets and no more. `test_wm_process` owns its own display and must not add a fifth. (The `Xvfb :99` server-launch line is not a `DISPLAY=` assignment and is not counted.)
    - `ctest --test-dir build/debug -L '^wm_process$' --output-on-failure --no-tests=error` exits 0 and reports 4 tests passed
    - `ctest --test-dir build/asan -L '^wm_process$' --output-on-failure --no-tests=error` exits 0 and no file matching the ASan `log_path` glob remains after the run
    - Every registration carries the option: `grep -c 'ADD_TAGS_AS_LABELS' CMakeLists.txt` equals `grep -c 'catch_discover_tests(' CMakeLists.txt`, and both are at least 12
    - The labels are actually registered, not merely requested: `ctest --test-dir build/debug --print-labels` lists `wm_process`, and also lists the pre-existing `config`, `ewmh`, `client`, `smoke`, `autoraise`, `eventloop`, `raii`, `xft` (proof the ten retrofitted calls took effect)
    - The label selects a non-empty set: `ctest --test-dir build/debug -L '^wm_process$' -N` reports `Total Tests: 4`
    - The backstop is live — a bogus label now fails instead of passing: `ctest --test-dir build/debug -L '^zzz_nomatch$' --no-tests=error` exits non-zero, while the same command without `--no-tests=error` exits 0
    - No `catch_discover_tests` call reintroduces name-based selection: `grep -c 'TEST_PREFIX\|TEST_SUFFIX' CMakeLists.txt` equals 0
  </acceptance_criteria>
  <done>A single ctest invocation launches the real binary, drives a dock window through create/map/destroy, and passes clean under both Debug and ASan/UBSan. The `eventDestroy` scan finding is fixed and proven, not merely patched.</done>
  <reversibility rating="costly">D-07 fixes the harness shape (Catch2 child-process fork/exec) that all 13 coverage items are written against; changing it later means rewriting every process-level test.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Environment preflight script and blocking ctest fixture (TEST-07, D-01, D-02)</name>
  <files>scripts/preflight.sh, CMakeLists.txt</files>
  <read_first>
    - COMPILED_CODE_BEHAVIOR_CHECKLIST.md (the "Environment Preflight" section — the exact command set the script must implement)
    - CMakeLists.txt (lines 85-102 — the `add_test` + `FIXTURES_SETUP` idiom to copy for the setup-only fixture)
    - .planning/phases/08-xrandr-vnc-compatibility-focus-rules/08-RESEARCH.md (Environment Availability table — the authoritative list of what must be checked and what is currently missing)
  </read_first>
  <action>
Create `scripts/preflight.sh`, the project's first shell script. There is no in-repo shell convention to inherit, so establish one: `#!/usr/bin/env bash`, `set -euo pipefail`, and every failure line prefixed `wm2: ` on stderr to match the binary's own convention.

The script checks, accumulating failures rather than aborting on the first so the operator sees the full picture in one run, then exits non-zero if any failed:

1. pkg-config modules present: `x11`, `xext`, `xft`, `fontconfig`, `xrandr`. Print each resolved version via `pkg-config --modversion`.
2. Toolchain versions recorded for signoff evidence: `cmake --version`, `c++ --version`, `pkg-config --version`, and the distro string from `/etc/os-release`.
3. X11 tooling on PATH: `Xvfb`, `Xephyr`, `xprop`, `xwininfo`, `xdotool`, `xdpyinfo`, `xrandr`.
4. At least one simple X client available from the set `xclock`, `xmessage` — the checklist explicitly forbids relying on `xterm` unless it is a declared dependency, so `xterm` may be reported as informational but must never satisfy this check on its own.
5. fontconfig fallback chains resolve, using the exact two patterns the WM uses: `fc-match "Noto Sans,DejaVu Sans,Sans:size=12"` and `fc-match "Noto Sans,DejaVu Sans,Sans:bold:size=12"`. A resolution to a real font file is required; an empty or error result is a failure.

Accept an optional `--quiet` flag that suppresses the informational version lines but still prints failures, so the ctest fixture output stays readable.

Then wire it into `CMakeLists.txt` as a ctest fixture per D-02 — a setup-only `add_test(NAME preflight COMMAND ${CMAKE_SOURCE_DIR}/scripts/preflight.sh --quiet)` with `set_tests_properties(preflight PROPERTIES FIXTURES_SETUP preflight_ok LABELS "preflight")`, and add `preflight_ok` to the `FIXTURES_REQUIRED` list of every behavioral target: `test_smoke`, `test_client`, `test_xft_poc`, `test_ewmh`, and `test_wm_process`. Deliberately do NOT add this as a CMake configure-time `check` — that would block plain binary builds on machines that never run tests.

The explicit `LABELS "preflight"` is what keeps the phase on a single selection mechanism. This is a hand-written `add_test`, not a Catch2 case, so Task 1's `ADD_TAGS_AS_LABELS` does not reach it; without the manual label the only way to select it would be `ctest -R '^preflight$'`, reintroducing the name-vs-label split that D-33 exists to remove. A label on a fixture test does not change fixture semantics — ctest still pulls the setup test in automatically for any `-L`-selected test that declares `FIXTURES_REQUIRED preflight_ok`, whether or not the setup test itself matched the filter.

Mark the script executable in git.
  </action>
  <verify>
    <automated>bash scripts/preflight.sh &amp;&amp; ctest --test-dir build/debug -L '^preflight$' --output-on-failure --no-tests=error</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/preflight.sh` exits 0
    - `bash scripts/preflight.sh` exits 0 on this host after Task 1's installs
    - `PATH=/nonexistent bash scripts/preflight.sh` exits non-zero (the script detects a missing tool rather than silently passing)
    - `head -2 scripts/preflight.sh | grep -c 'set -euo pipefail'` equals 1
    - `grep -c 'fc-match' scripts/preflight.sh` is at least 2
    - `grep -c 'preflight_ok' CMakeLists.txt` is at least 6
    - `ctest --test-dir build/debug -L '^preflight$' --output-on-failure --no-tests=error` exits 0 and runs exactly 1 test
    - `ctest --test-dir build/debug --print-labels` lists `preflight`
    - `ctest --test-dir build/debug -N | grep -c 'preflight'` is at least 1
    - Fixture semantics survive the label: `ctest --test-dir build/debug -L '^wm_process$' --output-on-failure --no-tests=error` runs `preflight` first even though `preflight` does not carry the `wm_process` label
  </acceptance_criteria>
  <done>Running the full suite on a host missing any declared dependency fails at the `preflight` fixture with a named cause, before a single behavioral assertion executes.</done>
</task>

<task type="auto">
  <name>Task 3: XTEST input driver and display-exclusivity guard (D-08, TEST-05)</name>
  <files>tests/support/XTestDriver.h, tests/test_wm_process.cpp, CMakeLists.txt</files>
  <read_first>
    - tests/support/WmFixture.h (created in Task 1 — the driver must attach to the fixture's own display string)
    - .planning/phases/08-xrandr-vnc-compatibility-focus-rules/08-RESEARCH.md (Code Example 2 XTestDriver sketch; Pitfall 3 display exclusivity; Pitfall 10 grab imperviousness)
    - src/Client.cpp (lines 1356-1380 `eventButton` — note the trailing `e->send_event` early return that makes XSendEvent-based synthesis useless)
    - include/x11wrap.h (the `x11::DisplayPtr` ownership pattern the driver's connection must follow)
  </read_first>
  <action>
Create `tests/support/XTestDriver.h`, header-only, following RESEARCH Code Example 2 and the `x11::DisplayPtr` ownership convention.

The driver opens its own dedicated `Display` connection to the fixture's display — separate from every assertion connection — because XTEST grab-imperviousness is a property of the calling connection and the fake-event `delay` argument blocks the calling connection for its whole duration. On construction it must call `XTestQueryExtension` and then `XTestGrabControl(dpy, True)` on that same connection, or the WM's own pointer grabs during `menu()`, move and resize will swallow every synthesised event.

Expose `moveTo(x, y)`, `press(button)`, `release(button)`, and `click(button)`, each passing `CurrentTime` (0) as the delay and following with an explicit `XFlush`. Long-press interactions such as the `destroy-window-delay` tab button are expressed by callers as `press()` → wall-clock wait → `release()`, never via the delay parameter.

Add one expansion case to `tests/test_wm_process.cpp` that uses the driver against the real binary end-to-end: warp the pointer onto a mapped client's frame, synthesise a Button1 press-and-release on the tab, and assert `_NET_ACTIVE_WINDOW` on root becomes that client's window id. This proves the driver actually drives the WM rather than merely emitting events into the void.

In `CMakeLists.txt`, add `pkg_check_modules(XTST REQUIRED IMPORTED_TARGET xtst)` inside the `if(BUILD_TESTS)` block only, and link `PkgConfig::XTST` into `test_wm_process` only. The shipped `wm2-born-again` target must not gain this dependency (the 512MB VPS constraint argues against dragging test-only libraries into the binary).

Finally, close the display-exclusivity hole per RESEARCH Pitfall 3: a running WM owns `SubstructureRedirectMask` exclusively and reparents any window created on the same display, which would corrupt `test_client` and `test_ewmh`. Since `test_wm_process` already allocates its own display, add `RESOURCE_LOCK "x_display_99"` to the test properties of the four legacy `:99` targets (`test_smoke`, `test_client`, `test_xft_poc`, `test_ewmh`) so `ctest -j` can never interleave them with each other or with a future WM-bearing target that mistakenly lands on `:99`.
  </action>
  <verify>
    <automated>cmake --build build/debug --parallel &amp;&amp; ctest --test-dir build/debug -L '^(wm_process|ewmh|client)$' -j4 --output-on-failure --no-tests=error</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/support/XTestDriver.h` exits 0
    - `grep -c 'XTestGrabControl' tests/support/XTestDriver.h` equals 1
    - `grep -c 'XSendEvent' tests/support/XTestDriver.h` equals 0
    - `grep -c 'PkgConfig::XTST' CMakeLists.txt` equals 1
    - `ldd build/release/wm2-born-again | grep -c libXtst` equals 0
    - `grep -c 'RESOURCE_LOCK' CMakeLists.txt` equals 4
    - `ctest --test-dir build/debug -L '^(wm_process|ewmh|client)$' -j4 --output-on-failure --no-tests=error` exits 0 and is stable across 3 consecutive runs
  </acceptance_criteria>
  <done>A synthesised click on a real window frame changes `_NET_ACTIVE_WINDOW` in the real WM, and a parallel `ctest -j4` run of the WM-bearing and `:99`-bearing groups is green and repeatable.</done>
</task>

</tasks>

<planner_assumptions>
Probe-surfaced edge rows carried by this plan (all `unresolved` — no SPEC.md exists for this phase, so these are explicit flagged assumptions, not silently dropped items):

- **TEST-05 / idempotency** — "What happens if this runs twice on the same input?" Assumption: `WmFixture` is safe to construct twice within one ctest run because each instance allocates a distinct free display and tears down its own Xvfb and WM child. Not proven by a dedicated test in this plan; the `-j4` acceptance criterion is the closest evidence. Flagged for `/gsd-verify-work`.
- **TEST-05 / concurrency** — "If interrupted or run in parallel, what is guaranteed?" Assumption: a SIGKILLed or crashed test leaks an Xvfb process and its `/tmp/.X<n>-lock`, which the next run's allocator will skip rather than collide with. Cleanup of orphaned servers is NOT guaranteed. Flagged.
- **TEST-07 / unclassified** — no category assigned by the probe. Assumption: "preflight passes" means every declared dependency resolves on the *current* host at the moment of the run; it makes no claim about other hosts or about versions drifting later. Flagged for manual review.
</planner_assumptions>

<assumption_delta_decision>
The assumption-delta detector fired `pluralization` on the token "fallback" in this phase's scope. Before Phase 8 the codebase had exactly one optional X extension (SHAPE), guarded ad hoc at a single helper. Phase 8 introduces a second and third (RANDR, RENDER), so "the optional extension" loses its monopoly.

- **Primary noun:** *optional X server capability* — not *Shape*.
- **Decision:** `promote`. Every optional capability gets the identical triple (an `int` member sentinel initialised to `-1` when absent, a `has*Extension()` predicate over it, and exactly one call funnel that no-ops when the predicate is false). SHAPE is demoted from "the special case with a bespoke guard" to "one instance of the capability pattern". Rationale: adding RANDR alongside a still-bespoke SHAPE guard would leave two shapes of the same idea in one file, and the phase's own governing note asks for the funnels to be deliberately symmetric.
- **Not promoted to:** a generic `Capability` class or registry. Three instances in a 512MB-constrained C++ binary do not justify indirection; the pattern is enforced by convention plus greppable invariants, not by a type.
- **Companion invariant test (accepted):** the greppable single-funnel assertions in plans 08-03 and 08-04 (`grep -c` on the raw X API name equals 1) go red the instant a future phase reintroduces an unguarded call site.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| X client → WM | Untrusted, client-controlled event ordering and property content crosses into `WindowManager` event handlers |
| Test harness → filesystem (`/tmp`, build dir) | Predictably-named lock and log paths in a potentially shared temp namespace |
| apt archive → build host | Third-party package contents installed with root privilege |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8-UAF | Elevation of Privilege | `WindowManager::eventDestroy` (src/Events.cpp:237-283) | high | mitigate | Capture `wasDock` before the owning `unique_ptr` erase; prove absence with the ASan dock-destroy case in Task 1. Client-triggerable by destroying a dock window. |
| T-8-TMP | Tampering | ASan `log_path`, Xvfb lock files | medium | mitigate | ASan logs go under the CMake binary directory, never a bare fixed `/tmp/<name>`. Display allocation reads `/tmp/.X<n>-lock` but never writes a predictable path of its own. |
| T-8-AC | Elevation of Privilege | `Xvfb -ac` in test fixtures | low | accept | `-ac` disables X access control and is confined to hermetic test servers. Accepted for tests; plan 08-14 carries the prohibition against it leaking into deployment documentation. |
| T-8-SC | Tampering | apt package installs (libxft-dev, libfontconfig1-dev, libxtst-dev, xterm) | medium | accept | Not an npm/PyPI/crates ecosystem. All four are long-standing Ubuntu 22.04 `main` packages from X.Org / freedesktop upstreams; the RESEARCH Package Legitimacy Audit records verdict OK for each with zero `[ASSUMED]`, `[SUS]`, or `[SLOP]` entries, so no blocking legitimacy checkpoint is required. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Symbols created across all of Phase 8 (listed once here so downstream source-grounding does not flag them as drift). Plans 02-14 create the remainder; this plan creates the entries marked **[08-01]**.

**New files:** `scripts/preflight.sh` **[08-01]**, `scripts/analysis/run-static-analysis.sh`, `scripts/analysis/cppcheck-suppressions.xml`, `scripts/gates/build-all.sh`, `scripts/capture-display-capabilities.sh`, `.clang-tidy`, `tests/lsan.supp` **[08-01]**, `tests/support/WmFixture.h` **[08-01]**, `tests/support/XTestDriver.h` **[08-01]**, `tests/test_wm_process.cpp` **[08-01]**, `tests/test_wm_geometry.cpp`, `tests/test_wm_fallbacks.cpp`, `tests/test_wm_focus.cpp`, `tests/test_wm_rules.cpp`, `tests/test_rules.cpp`, `tests/test_wm_lifecycle.cpp`, `tests/test_wm_state.cpp`, `tests/test_wm_runtime.cpp`, `tests/test_wm_resource.cpp`, `include/Rules.h`, `src/Rules.cpp`, `docs/RELEASE-NOTES.md`, `.planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/`

**New C++ symbols:** `WindowManager::hasRandrExtension()`, `WindowManager::screenWidth()`, `WindowManager::screenHeight()`, `WindowManager::handleScreenGeometryChange()`, `WindowManager::lastUserInteraction()`, `WindowManager::noteUserInteraction()`, `WindowManager::m_randrEventBase`, `WindowManager::m_lastKnownScreenW`, `WindowManager::m_lastKnownScreenH`, `WindowManager::m_lastUserInteraction`, `Border::combineShape()`, `Client::getClassHint()`, `Client::resName()`, `Client::resClass()`, `Client::shouldFocusOnMap()`, `Client::demandAttention()`, `Client::applyWindowRules()`, `Client::m_resName`, `Client::m_resClass`, `Client::m_ruleNoDecorate`, `Client::m_skipTaskbar`, `Client::m_demandsAttention`, `struct WindowRule`, `enum class RuleMatchMode`, `enum class RuleMatchField`, `WindowRule::matches()`, `Config::rules`, `Config::focusStealingPrevention`, `Atoms::net_wmUserTime`, `Atoms::net_wmUserTimeWindow`, `Atoms::net_wmStateDemandsAttention`, `Atoms::net_wmStateSkipTaskbar`, `Atoms::net_wmStateSkipPager`, `WmFixture` **[08-01]**, `XTestDriver` **[08-01]**

**New config keys:** `rule-match-class`, `rule-match-name`, `rule-match-type`, `rule-match-mode`, `rule-no-decorate`, `rule-position`, `rule-size`, `rule-skip-taskbar`, `focus-stealing-prevention`

**New CLI flags:** `--focus-stealing-prevention`, `--no-focus-stealing-prevention`, `--help`

**New env vars (internal test levers, never user-documented):** `WM2_FORCE_NO_SHAPE`, `WM2_FORCE_NO_RANDR`

**New CMake targets/fixtures:** `test_wm_process` **[08-01]**, `test_wm_geometry`, `test_wm_fallbacks`, `test_wm_focus`, `test_wm_rules`, `test_rules`, `test_wm_lifecycle`, `test_wm_state`, `test_wm_runtime`, `test_wm_resource`; fixtures `preflight_ok` **[08-01]**, `xvfb_norandr`, `xvfb_norender`; resource lock `x_display_99` **[08-01]**; `pkg_check_modules` entries `XRANDR`, `XTST` **[08-01: XTST]**
</artifacts_this_phase_produces>

<verification>
- `bash scripts/preflight.sh` exits 0 and names every resolved dependency version.
- `ctest --test-dir build/debug -L '^(wm_process|preflight)$' --output-on-failure --no-tests=error` exits 0.
- `ctest --test-dir build/asan -L '^wm_process$' --output-on-failure --no-tests=error` exits 0 with no ASan log files left behind.
- Three consecutive `ctest --test-dir build/debug -j4 --output-on-failure --no-tests=error` runs are green with no flakes.
- `git status --porcelain build` reports nothing (build trees are ignored, not committed).
</verification>

<success_criteria>
The project builds from a clean checkout on this host; a Catch2 test drives the real compiled binary end-to-end on a display it owns; the `eventDestroy` use-after-free is fixed and proven absent under AddressSanitizer; and no behavioral test can run on a host that fails preflight.
</success_criteria>

<output>
Create `.planning/phases/08-xrandr-vnc-compatibility-focus-rules/08-01-SUMMARY.md` when done
</output>
