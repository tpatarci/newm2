---
phase: 08-xrandr-vnc-compatibility-focus-rules
plan: 11
type: execute
wave: 11
depends_on: ["08-10"]
files_modified:
  - tests/test_wm_lifecycle.cpp
  - CMakeLists.txt
autonomous: true
requirements: [TEST-05]

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Destroying a normal managed client leaves no stale entry in the client list, no dangling active-window value, no crash and no sanitizer report (checklist coverage item 2)."
    - "Hiding and unhiding a client moves it correctly between the visible and hidden client lists and keeps both the hidden state and the client list accurate (coverage item 4)."
    - "Every size-hint constraint the resize path honours — minimum, maximum, base, increments, fixed-size clients, and zero or invalid increments — is covered by an assertion against the real binary (coverage item 8)."
    - "Every window gravity mode the placement path handles is covered by an assertion against the real binary (coverage item 9)."
    - "All four coverage items are exercised in the sanitizer build as well as the debug build."
  artifacts:
    - tests/test_wm_lifecycle.cpp
  key_links:
    - "`WmFixture` -> the real binary -> `_NET_CLIENT_LIST` and `_NET_ACTIVE_WINDOW` after destroy"
    - "`XTestDriver` resize drags -> the size-hint constraint path -> the resulting frame geometry"
    - "client-set size hints including gravity -> the placement path -> the frame's on-screen position"
  prohibitions:
    - "Do not assert on WM internal state; every outcome must be read back from the X server."
    - "Do not use sleep()-based synchronisation; poll with a deadline."
    - "Do not weaken an assertion to make a case pass; a genuine defect found here is a finding, not a test to soften."
    - "Do not add any .github/workflows/ file or other CI pipeline."
    - "Do not introduce C++20/C++23 language features."
---

<objective>
Close four of the checklist's missing-coverage items with process-level tests: normal-client destroy, hidden-list transfers, the full set of size-hint resize constraints, and every window gravity mode.

Purpose: these are the paths where a use-after-free or a stale list entry hides, and where the WM's geometry negotiation either honours a client's stated constraints or quietly ignores them. Both classes of defect are invisible to the existing primitive-level tests, which never run the real binary.
Output: `tests/test_wm_lifecycle.cpp` and its CMake target.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-xrandr-vnc-compatibility-focus-rules/08-CONTEXT.md
@.planning/phases/08-xrandr-vnc-compatibility-focus-rules/08-RESEARCH.md
@.planning/phases/08-xrandr-vnc-compatibility-focus-rules/08-10-SUMMARY.md
@COMPILED_CODE_BEHAVIOR_CHECKLIST.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Destroy and hidden-list coverage (checklist items 2 and 4)</name>
  <files>tests/test_wm_lifecycle.cpp, CMakeLists.txt</files>
  <read_first>
    - src/Events.cpp (lines 237-290 `eventDestroy` — the four-step teardown, including the fix landed in 08-01, and the client-list update that must be observable)
    - src/Client.cpp (lines 775-800 `hide()` and `unhide()` — the vector-to-vector transfers of the owning pointer and the state changes they make)
    - src/Manager.cpp (`addToHiddenList()`, `removeFromHiddenList()`, `updateClientList()`, `updateActiveWindow()` — the four functions whose effects are read back from root properties)
    - tests/test_wm_process.cpp (created in 08-01 — the fixture idiom, the tag convention, and the dock-destroy case whose structure the normal-destroy case mirrors)
    - COMPILED_CODE_BEHAVIOR_CHECKLIST.md (the "Missing Automated Coverage To Add" entries for managed window destroy and for hidden-list transfers — the exact assertions each item calls for)
  </read_first>
  <behavior>
    - Test 1: destroying a mapped, focused, normal client removes it from the client list, clears or reassigns the active-window value so it never names the destroyed window, and leaves the WM alive.
    - Test 2: destroying one of three mapped clients removes exactly that one from the client list and leaves the other two in place and in order.
    - Test 3: destroying a client while it is the focus candidate mid-focus-tracking does not crash and produces no sanitizer report.
    - Test 4: hiding a client keeps it in the client list, adds the hidden state to its state property, and unmaps its frame.
    - Test 5: unhiding a hidden client removes the hidden state, remaps the frame, and keeps the client list entry intact throughout.
    - Test 6: hiding then destroying a client without unhiding it removes it from the client list and produces no sanitizer report — the destroy path must find it in the hidden vector, not the visible one.
  </behavior>
  <action>
Create `tests/test_wm_lifecycle.cpp` tagged `[wm_lifecycle]`, using the fixture and assertion idioms established in `tests/test_wm_process.cpp`.

Implement the six behaviours above. Read every outcome from the X server: the client list and active-window values from root properties, the per-client hidden state from the client's own state property, and map state with a window-attributes query on the frame. Poll with a deadline; never sleep a fixed interval.

Two cases need care. For the focus-candidate destroy, first move the pointer onto the client with the input driver so focus tracking is genuinely in flight, then destroy the window before the auto-raise delay elapses — shorten that delay via a CLI flag so the window is small but real. For the hide-then-destroy case, drive the hide through the client-message path a real client would use rather than by calling into the WM, so the test exercises the same route an application does.

In `CMakeLists.txt`, add a `test_wm_lifecycle` target cloned from `test_wm_rules`: same libraries including the input-driver library, same include directories, the WM binary-path compile definition, `add_dependencies` on the WM target, and `catch_discover_tests` requiring the preflight fixture and no shared-display fixture.
  </action>
  <verify>
    <automated>cmake --build build/debug --parallel &amp;&amp; ctest --test-dir build/debug -R wm_lifecycle --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/test_wm_lifecycle.cpp` exits 0
    - `grep -c 'test_wm_lifecycle' CMakeLists.txt` is at least 3
    - `grep -v '^\s*//' tests/test_wm_lifecycle.cpp | grep -c 'sleep('` equals 0
    - `grep -c 'DISPLAY=:99' CMakeLists.txt` equals 4 (unchanged from 08-01)
    - `ctest --test-dir build/debug -R wm_lifecycle --output-on-failure` exits 0 and reports 6 tests passed
    - `ctest --test-dir build/asan -R wm_lifecycle --output-on-failure` exits 0 with no ASan report files left behind
    - Running `ctest --test-dir build/debug -R wm_lifecycle` three consecutive times is green every time
  </acceptance_criteria>
  <done>Checklist coverage items 2 and 4 are automated and green in both the debug and the sanitizer build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Size-hint resize-constraint coverage (checklist item 8)</name>
  <files>tests/test_wm_lifecycle.cpp</files>
  <read_first>
    - src/Client.cpp (around line 1144 `fixResizeDimensions()` — every branch this task must cover: the minimum and maximum clamps, the base-size subtraction, the increment quantisation, and the guard against a zero or negative increment)
    - src/Client.cpp (lines 139-167 in the map path — where the size hints are read and where the fixed-size determination is made from equal minimum and maximum dimensions)
    - src/Client.cpp (the resize entry point and how it reaches the constraint function from a drag)
    - tests/support/XTestDriver.h (the press, motion and release primitives needed to drive a real resize drag)
    - COMPILED_CODE_BEHAVIOR_CHECKLIST.md (the coverage entry naming the six constraint classes)
  </read_first>
  <behavior>
    - Test 1: a client declaring a minimum size cannot be dragged smaller than it, in either dimension.
    - Test 2: a client declaring a maximum size cannot be dragged larger than it, in either dimension.
    - Test 3: a client declaring a base size and a resize increment lands only on sizes that are the base plus a whole multiple of the increment.
    - Test 4: a client declaring an increment but no base size quantises against its minimum size instead, without producing a negative intermediate.
    - Test 5: a fixed-size client — equal minimum and maximum in both dimensions — is not resized by a drag at all, and its resize handle behaves accordingly.
    - Test 6: a client declaring a zero or negative resize increment is resized freely rather than dividing by that value, and the WM does not crash.
    - Test 7: a client declaring a maximum smaller than its minimum is handled without a crash and without a negative dimension reaching the server.
  </behavior>
  <action>
Extend `tests/test_wm_lifecycle.cpp` with a group tagged `[wm_sizehints]` covering the seven behaviours above.

Each case creates a client that sets normal-hints with exactly the flag combination under test before mapping — the flag bits matter as much as the values, since the constraint function branches on which flags are present, and a test that sets a value without its flag would exercise nothing.

Drive resizing through a real drag on the frame's resize handle using the input driver, not by sending a configure request: the constraint path this item covers is the interactive one, and a configure request takes a different route. Locate the handle from the frame geometry, press, move in a series of motion events, then release, and read the resulting geometry back from the server.

Cases 6 and 7 are the ones most likely to find a genuine defect. Write them to assert the absence of a crash and the absence of a nonsensical dimension rather than a specific size, since the correct behaviour for contradictory hints is "do something sane", not "do one exact thing". If either finds a real defect in `fixResizeDimensions()`, fix it in `src/Client.cpp` and record the fix in the plan summary — do not soften the assertion.
  </action>
  <verify>
    <automated>cmake --build build/debug --parallel &amp;&amp; ctest --test-dir build/debug -R 'wm_lifecycle|wm_sizehints' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'wm_sizehints' tests/test_wm_lifecycle.cpp` is at least 7
    - `grep -c 'PResizeInc\|PBaseSize\|PMinSize\|PMaxSize' tests/test_wm_lifecycle.cpp` is at least 4
    - `ctest --test-dir build/debug -R 'wm_lifecycle|wm_sizehints' --output-on-failure` exits 0 and reports 13 tests passed
    - `ctest --test-dir build/asan -R wm_sizehints --output-on-failure` exits 0 with no ASan report files left behind
    - Running `ctest --test-dir build/debug -R wm_sizehints` three consecutive times is green every time
  </acceptance_criteria>
  <done>Checklist coverage item 8 is automated, including the two degenerate-hint cases that had no coverage at all.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Window gravity coverage (checklist item 9)</name>
  <files>tests/test_wm_lifecycle.cpp</files>
  <read_first>
    - src/Client.cpp (around line 702 `gravitate()` — every gravity mode it switches on and the offset each applies, plus the inverted call used when unmanaging)
    - src/Client.cpp (the map path's call to the placement adjustment before framing, and the unreparent path's inverted call)
    - include/Border.h (the frame width and tab width constants that determine the offsets gravity compensates for)
    - COMPILED_CODE_BEHAVIOR_CHECKLIST.md (the coverage entry calling for all gravity modes used by the placement path)
  </read_first>
  <behavior>
    - Test 1: for each gravity mode the placement path handles, a client requesting a specific position ends up with its client area at the position the mode implies once the frame is accounted for.
    - Test 2: a client declaring no gravity at all is placed using the default mode, matching current behaviour.
    - Test 3: a client declaring a gravity value outside the handled set is placed without a crash and without a negative coordinate reaching the server.
    - Test 4: the placement adjustment is its own inverse — a client mapped, then unmapped and remapped, ends up at the same client-area position rather than drifting by the frame offset each cycle.
  </behavior>
  <action>
Extend `tests/test_wm_lifecycle.cpp` with a group tagged `[wm_gravity]` covering the four behaviours above.

Enumerate the gravity modes from the placement function's own switch rather than from memory, and write one assertion per handled mode inside a single data-driven case so adding a mode later means adding a row, not a case. For each mode, create a client that sets the gravity flag and value in its normal-hints before mapping, request a known position, and read the client window's absolute position back from the server after mapping — translate coordinates to root, since the client is reparented into a frame and its own geometry is frame-relative.

Case 4 is the one that catches the classic bug in this area: an adjustment that is applied on map but not correctly inverted on unmap makes windows creep across the screen with every hide-and-restore cycle. Run at least three cycles and assert the position is identical each time, not merely close.

Case 3 must assert on the absence of a negative coordinate specifically, because an unhandled mode falling through to an uninitialised or wrapped offset is the plausible failure and it would place a window off the top-left of the screen rather than crashing.

If any case finds a genuine defect in the placement path, fix it in `src/Client.cpp` and record the fix in the plan summary rather than adjusting the expectation.
  </action>
  <verify>
    <automated>cmake --build build/debug --parallel &amp;&amp; ctest --test-dir build/debug -R 'wm_lifecycle|wm_sizehints|wm_gravity' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'wm_gravity' tests/test_wm_lifecycle.cpp` is at least 4
    - `grep -c 'PWinGravity' tests/test_wm_lifecycle.cpp` is at least 1
    - `grep -c 'XTranslateCoordinates' tests/test_wm_lifecycle.cpp` is at least 1
    - `ctest --test-dir build/debug -R 'wm_lifecycle|wm_sizehints|wm_gravity' --output-on-failure` exits 0 and reports 17 tests passed
    - `ctest --test-dir build/asan -R wm_gravity --output-on-failure` exits 0 with no ASan report files left behind
    - `ctest --test-dir build/debug --output-on-failure` exits 0
  </acceptance_criteria>
  <done>Checklist coverage item 9 is automated for every handled gravity mode plus the unhandled and round-trip cases.</done>
</task>

</tasks>

<planner_assumptions>
This plan carries no probe-surfaced edge rows of its own; TEST-05's two rows (idempotency and concurrency) are carried by plan 08-01, which builds the harness those properties belong to.

One planner assumption specific to this plan, flagged rather than silently absorbed: the size-hint and gravity cases assert against the behaviour the current implementation produces where the correct behaviour is not specified by ICCCM in a single unambiguous way — in particular for contradictory hints such as a maximum below a minimum. Those assertions pin current behaviour as a regression guard, not as a claim of specification conformance.
</planner_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| X client → WM | Size hints, gravity values and destroy timing are all client-controlled and can be degenerate or hostile |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8-UAF | Elevation of Privilege | Client teardown while focus tracking is in flight | high | mitigate | Test 3 destroys a client that is the live focus candidate and runs under the sanitizer; Test 6 destroys a client from the hidden vector, exercising the branch the visible-vector path does not. |
| T-8-DOS | Denial of Service | Degenerate size hints reaching the resize-constraint arithmetic | medium | mitigate | Cases 6 and 7 cover a zero or negative increment and an inverted minimum and maximum, asserting no crash and no nonsensical dimension reaching the server. A genuine defect found here is fixed rather than tolerated. |
| T-8-GEO | Denial of Service | An unhandled gravity value producing a negative placement | low | mitigate | Case 3 asserts specifically on the absence of a negative coordinate, which is the plausible failure mode rather than a crash. |
| T-8-SC | Tampering | No package installs in this plan | low | accept | This plan adds no dependency of any ecosystem; nothing to audit. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by this plan: `tests/test_wm_lifecycle.cpp` (test groups `[wm_lifecycle]`, `[wm_sizehints]`, `[wm_gravity]`); the CMake target `test_wm_lifecycle`.

Modified by this plan: `CMakeLists.txt`, and `src/Client.cpp` only if a case finds a genuine defect, in which case the fix is recorded in the plan summary.

The complete Phase 8 artifact inventory is listed once in `08-01-PLAN.md` under "Artifacts this phase produces".
</artifacts_this_phase_produces>

<verification>
- `ctest --test-dir build/debug -R 'wm_lifecycle|wm_sizehints|wm_gravity' --output-on-failure` exits 0 with 17 tests passed, stable across three runs.
- `ctest --test-dir build/asan -R 'wm_lifecycle|wm_sizehints|wm_gravity' --output-on-failure` exits 0 with no sanitizer reports.
- `ctest --test-dir build/debug --output-on-failure` (full suite) exits 0.
- `bash scripts/analysis/run-static-analysis.sh` exits 0.
</verification>

<success_criteria>
Four of the thirteen missing-coverage items — normal-client destroy, hidden-list transfers, all size-hint constraints, and all gravity modes — are automated against the real binary and green under the sanitizer.
</success_criteria>

<output>
Create `.planning/phases/08-xrandr-vnc-compatibility-focus-rules/08-11-SUMMARY.md` when done
</output>
