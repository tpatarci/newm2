---
phase: 05-configuration-system
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/main.cpp
  - src/Manager.cpp
  - include/Manager.h
  - src/Border.cpp
  - include/Border.h
  - src/Events.cpp
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-04]

must_haves:
  truths:
    - "WM reads config at startup and uses values for colors, timing, focus, frame thickness, spawn command"
    - "Running without config file works perfectly with built-in defaults"
    - "CLI options override config file values at runtime"
    - "Changing config file and restarting WM picks up new settings"
    - "FRAME_WIDTH is a runtime value, not a compile-time constant"
    - "spawn() uses configurable new-window-command with execUsingShell support"
  artifacts:
    - path: "src/main.cpp"
      provides: "Entry point calling Config::load() before WindowManager construction"
      contains: "Config::load"
    - path: "include/Manager.h"
      provides: "WindowManager accepts Config const ref in constructor"
      contains: "const Config&"
    - path: "include/Border.h"
      provides: "FRAME_WIDTH is a runtime int, no longer constexpr"
      contains: "extern int FRAME_WIDTH"
  key_links:
    - from: "src/main.cpp"
      to: "Config::load()"
      via: "Calls load(argc, argv) before WindowManager construction"
      pattern: "Config::load"
    - from: "src/Manager.cpp"
      to: "Config struct members"
      via: "m_config member, used in initialiseScreen() and spawn()"
      pattern: "m_config\\."
    - from: "src/Border.cpp"
      to: "Config color strings"
      via: "allocateColour(m_config.tabForeground, ...) in Border constructor"
      pattern: "m_config\\."
    - from: "src/Events.cpp"
      to: "Config timing values"
      via: "chrono::milliseconds(m_config.autoRaiseDelay) etc."
      pattern: "m_config\\."
---

<objective>
Wire the Config struct into the WM: main.cpp calls Config::load(), passes Config to WindowManager, and all consumers (Manager, Border, Buttons, Events) read their settings from Config instead of hardcoded values. FRAME_WIDTH becomes a runtime variable.

Purpose: This is the integration plan that makes config actually work at runtime. Plans 01 and 02 built the parsing infrastructure; this plan connects it to every consumer in the codebase.

Output: All hardcoded config values replaced with Config struct reads. WM fully configurable at runtime.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-configuration-system/05-CONTEXT.md
@.planning/phases/05-configuration-system/05-RESEARCH.md
@.planning/phases/05-configuration-system/05-01-SUMMARY.md
@.planning/phases/05-configuration-system/05-02-SUMMARY.md

<interfaces>
<!-- Config struct from Plans 01+02, now consumed by WindowManager -->

From include/Config.h (created by Plans 01+02):
```cpp
struct Config {
    std::string tabForeground = "black";
    std::string tabBackground = "gray80";
    std::string frameBackground = "gray95";
    std::string buttonBackground = "gray95";
    std::string borders = "black";
    std::string menuForeground = "black";
    std::string menuBackground = "gray80";
    std::string menuHighlight = "gray60";
    std::string menuBorders = "black";
    bool clickToFocus = false;
    bool raiseOnFocus = false;
    bool autoRaise = false;
    int autoRaiseDelay = 400;
    int pointerStoppedDelay = 80;
    int destroyWindowDelay = 1500;
    int frameThickness = 7;
    std::string newWindowCommand = "xterm";
    bool execUsingShell = false;

    static Config load(int argc, char** argv);
    void applyFile(const std::string& path);
    void applyKeyValue(const std::string& key, const std::string& value);
    void applyCliArgs(int argc, char** argv);
};
```

From include/Manager.h (current, pre-modification):
```cpp
class WindowManager {
public:
    WindowManager();  // Currently takes no args
    // ...
    unsigned long allocateColour(const char *name, const char *desc);
    // ...
};
```

From include/Border.h (current, pre-modification):
```cpp
constexpr int FRAME_WIDTH = 7;  // Must become runtime
class Border {
    // Static color members allocated from hardcoded strings in constructor
    static unsigned long m_frameBackgroundPixel;
    static unsigned long m_buttonBackgroundPixel;
    static unsigned long m_borderPixel;
};
```

Hardcoded value locations to replace:
- Manager.cpp:284 -> "black" -> config.menuBorders
- Manager.cpp:310 -> "black" -> config.menuForeground
- Manager.cpp:311 -> "gray80" -> config.menuBackground
- Manager.cpp:312 -> "gray60" -> config.menuHighlight
- Manager.cpp:584 -> 400ms -> config.autoRaiseDelay
- Manager.cpp:630 -> 80ms -> config.pointerStoppedDelay
- Manager.cpp:549 -> "xterm" -> config.newWindowCommand
- Border.cpp:63 -> "gray95" -> config.frameBackground
- Border.cpp:64 -> "gray95" -> config.buttonBackground
- Border.cpp:65 -> "black" -> config.borders
- Border.cpp:72 -> "black" -> config.tabForeground
- Border.cpp:73 -> "gray80" -> config.tabBackground
- Border.cpp:134 -> "black" -> config.tabForeground (XftColorAllocName)
- Border.cpp:137 -> "gray80" -> config.tabBackground (XftColorAllocName)
- Border.cpp:924 -> 1500L -> config.destroyWindowDelay
- Border.h:12 -> constexpr FRAME_WIDTH = 7 -> runtime from config
- Events.cpp:101 -> 80ms -> config.pointerStoppedDelay
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Config into main.cpp and WindowManager constructor, make FRAME_WIDTH runtime</name>
  <files>src/main.cpp, include/Manager.h, src/Manager.cpp, include/Border.h</files>
  <read_first>
    src/main.cpp (current entry point -- will be modified)
    include/Manager.h (WindowManager class declaration -- adding Config member)
    src/Manager.cpp (WindowManager constructor, initialiseScreen, spawn, considerFocusChange, checkDelaysForFocus -- all consume config values)
    include/Border.h (FRAME_WIDTH constexpr -- must become runtime)
    include/Config.h (Config struct API from Plans 01+02)
  </read_first>
  <action>
**Step 1: src/main.cpp**
Replace the current entry point. Remove the "usage" error when argc > 1 (CLI args are now valid). Call Config::load() and pass result to WindowManager:

```cpp
#include "Manager.h"
#include "Config.h"
#include <cstdio>

int main(int argc, char** argv) {
    Config config = Config::load(argc, argv);
    WindowManager manager(config);
    return 0;
}
```

**Step 2: include/Manager.h**
Add `#include "Config.h"` to includes.
Change WindowManager constructor to accept Config: `WindowManager(const Config& config);`
Add a private member: `Config m_config;`
Add a public accessor: `const Config& config() const { return m_config; }`

**Step 3: src/Manager.cpp**
Update constructor signature to `WindowManager::WindowManager(const Config& config)` and initialize m_config in initializer list: `, m_config(config)`.
In initialiseScreen(), replace hardcoded color strings with m_config members:
- Line 284: `allocateColour("black", "menu border")` -> `allocateColour(m_config.menuBorders.c_str(), "menu border")`
- Line 310: `XftColorWrap(display, visual, cmap, "black")` -> `XftColorWrap(display, visual, cmap, m_config.menuForeground.c_str())`
- Line 311: `XftColorWrap(display, visual, cmap, "gray80")` -> `XftColorWrap(display, visual, cmap, m_config.menuBackground.c_str())`
- Line 312: `XftColorWrap(display, visual, cmap, "gray60")` -> `XftColorWrap(display, visual, cmap, m_config.menuHighlight.c_str())`

In considerFocusChange() (line 584):
- Replace `std::chrono::milliseconds(400)` with `std::chrono::milliseconds(m_config.autoRaiseDelay)`

In checkDelaysForFocus() (line 630):
- Replace `std::chrono::milliseconds(80)` with `std::chrono::milliseconds(m_config.pointerStoppedDelay)`

In spawn() (line 549):
- Replace `execlp("xterm", "xterm", nullptr)` with conditional logic:
  - If m_config.execUsingShell: `execl("/bin/sh", "sh", "-c", m_config.newWindowCommand.c_str(), nullptr)`
  - Else: `execlp(m_config.newWindowCommand.c_str(), m_config.newWindowCommand.c_str(), nullptr)`
- Update error message to use m_config.newWindowCommand

**Step 4: include/Border.h**
Change `constexpr int FRAME_WIDTH = 7;` to `extern int FRAME_WIDTH;` (declared as extern, defined in Border.cpp). This is the critical change per RESEARCH.md Pitfall 1 -- FRAME_WIDTH is used in 40+ places in Border.cpp for shape math, but all uses are inside function bodies (runtime), not compile-time contexts. The constexpr becomes a runtime int.

Also change: `constexpr int TRANSIENT_FRAME_WIDTH = 4;` to `constexpr int TRANSIENT_FRAME_WIDTH = 4;` (keep constexpr -- this is a fixed ratio, not user-configurable per the setting inventory).
  </action>
  <verify>
    <automated>cmake --build build && cd build && ctest --output-on-failure</automated>
  </verify>
  <done>
    - src/main.cpp calls Config::load(argc, argv) and passes result to WindowManager constructor
    - WindowManager constructor accepts `const Config& config`
    - WindowManager stores m_config and exposes config() accessor
    - initialiseScreen() uses m_config.menuForeground, m_config.menuBackground, m_config.menuHighlight, m_config.menuBorders for color allocation
    - considerFocusChange() uses m_config.autoRaiseDelay for timer
    - checkDelaysForFocus() uses m_config.pointerStoppedDelay for timer
    - spawn() uses m_config.newWindowCommand and m_config.execUsingShell
    - Border.h declares `extern int FRAME_WIDTH;` instead of constexpr
    - Build succeeds with no errors
    - All existing tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Config colors into Border, timing into Events, destroy-window-delay into button hold</name>
  <files>src/Border.cpp, src/Events.cpp</files>
  <read_first>
    src/Border.cpp (Border constructor allocates colors from hardcoded strings; shapeResize uses FRAME_WIDTH; eventButton has destroy-window-delay at line 924)
    src/Events.cpp (MotionNotify handler uses 80ms pointerStoppedDelay at line 101)
    include/Manager.h (config() accessor returns const Config&)
    include/Border.h (FRAME_WIDTH extern declaration)
  </read_first>
  <action>
**Step 1: src/Border.cpp**
Define the FRAME_WIDTH global at top of file (replacing the constexpr):
```cpp
int FRAME_WIDTH = 7;  // Default, overwritten in constructor from config
```

In Border constructor, after the `if (m_tabFont == nullptr)` block enters (first Border instance):
- Initialize FRAME_WIDTH from config: `FRAME_WIDTH = windowManager()->config().frameThickness;`
- Replace hardcoded color strings with config values:
  - Line 63: `allocateColour("gray95", ...)` -> `allocateColour(windowManager()->config().frameBackground.c_str(), ...)`
  - Line 64: `allocateColour("gray95", ...)` -> `allocateColour(windowManager()->config().buttonBackground.c_str(), ...)`
  - Line 65: `allocateColour("black", ...)` -> `allocateColour(windowManager()->config().borders.c_str(), ...)`
  - Line 72: `allocateColour("black", "tab foreground")` -> `allocateColour(windowManager()->config().tabForeground.c_str(), "tab foreground")`
  - Line 73: `allocateColour("gray80", "tab background")` -> `allocateColour(windowManager()->config().tabBackground.c_str(), "tab background")`

In allocateXftColors():
- Line 134: `XftColorAllocName(d, visual, cmap, "black", ...)` -> use `windowManager()->config().tabForeground.c_str()`
- Line 137: `XftColorAllocName(d, visual, cmap, "gray80", ...)` -> use `windowManager()->config().tabBackground.c_str()`

In eventButton() (line 924):
- Replace `tdiff > 1500L` with `tdiff > static_cast<unsigned long>(windowManager()->config().destroyWindowDelay)`

**Step 2: src/Events.cpp**
In the MotionNotify handler (line 101):
- Replace `std::chrono::milliseconds(80)` with `std::chrono::milliseconds(windowManager()->config().pointerStoppedDelay)`

Note: Events.cpp does not include Manager.h's full definition -- check if windowManager() accessor is accessible. Since Events.cpp includes "Manager.h" and "Client.h", and Client has a windowManager() method returning WindowManager*, the config() accessor should be accessible.
  </action>
  <verify>
    <automated>cmake --build build && cd build && ctest --output-on-failure</automated>
  </verify>
  <done>
    - FRAME_WIDTH is initialized from config.frameThickness (runtime, not constexpr)
    - Border constructor uses config.frameBackground, config.buttonBackground, config.borders for pixel allocation
    - Border constructor uses config.tabForeground, config.tabBackground for tab colors (both XftColorAllocName and XAllocNamedColor paths)
    - eventButton() uses config.destroyWindowDelay for the button-hold-to-delete timer
    - Events.cpp MotionNotify handler uses config.pointerStoppedDelay
    - Build succeeds, all tests pass
    - grep -rn '"gray95"' src/Border.cpp returns 0 matches (hardcoded color replaced)
    - grep -rn '"black"' src/Border.cpp returns 0 matches in color allocation lines
    - grep -rn 'constexpr.*FRAME_WIDTH' include/Border.h returns 0 matches (no longer constexpr)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config struct -> WindowManager/Border/Events | Internal trusted boundary. Config validated at load time, consumed as read-only const ref. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | FRAME_WIDTH runtime modification | mitigate | FRAME_WIDTH set once from Config in first Border constructor; Config is const ref in WindowManager. No public setter for FRAME_WIDTH after initialization |
| T-05-10 | Elevation of Privilege | spawn() with execUsingShell | accept | execUsingShell uses /bin/sh -c with user's configured command. User already has shell access via the WM process. No escalation vector |
| T-05-11 | Denial of Service | FRAME_WIDTH = 0 or negative | mitigate | Config clamping ensures frameThickness >= 1 (enforced by applyKeyValue validation). Shape math with FRAME_WIDTH >= 1 is safe |
</threat_model>

<verification>
1. Build succeeds: `cmake --build build`
2. All tests pass: `cd build && ctest --output-on-failure`
3. No hardcoded config values remain in consumers:
   - `grep -rn '"gray95"' src/Border.cpp` returns 0 matches
   - `grep -rn '"black"' src/Border.cpp` returns 0 in color lines
   - `grep -rn '"gray80"' src/Manager.cpp` returns 0 matches
   - `grep -rn 'constexpr.*FRAME_WIDTH' include/Border.h` returns 0
4. Config integration points present:
   - `grep -n 'Config::load' src/main.cpp` finds the call
   - `grep -n 'm_config\.' src/Manager.cpp` finds config usage
   - `grep -n 'config\(\)' include/Manager.h` finds the accessor
</verification>

<success_criteria>
- WM starts with `Config::load()` before WindowManager construction
- WindowManager stores Config as const member, exposes config() accessor
- All 18 config values flow from Config struct to their consumers:
  - 9 color strings -> allocateColour() and XftColorAllocName() calls
  - 3 timing ints -> chrono::milliseconds() calls
  - 1 frame thickness -> FRAME_WIDTH global in Border.cpp
  - 3 bools -> stored for future focus policy consumption (Phase 8 FOCUS-02)
  - 1 command string + 1 bool -> spawn() execlp/execl logic
- FRAME_WIDTH is runtime int, no longer constexpr
- Build succeeds, all tests pass
- No hardcoded config values remain in src/
</success_criteria>

<output>
After completion, create `.planning/phases/05-configuration-system/05-03-SUMMARY.md`
</output>
