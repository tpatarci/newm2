---
phase: 08.5-v1.0-closeout
plan: 8
subsystem: testing
tags: [gates, ctest, asan, cppcheck, clang-tidy, provenance, ewmh, xvfb, release-evidence]

requires:
  - phase: 08.5-12
    provides: Decision A — 08.5-07 superseded, this plan released with its precondition re-planned onto the closeout's own ten-run table (evidence/gates/fix-measurement/)
  - phase: 08.5-11
    provides: the event-loop fix whose self-pipe signal handler this capture's release log flagged, and the modal loops (ledger 8) whose dead stores cppcheck flagged
provides:
  - the fourteen-file criterion-7 gate bundle at the top level of evidence/gates/, bound to commit f54de6e by PROVENANCE.txt with the no-source-drift command
  - DOC-GUARDS.txt — gap-1 guards A and B (0 and 0) and the RULES-01 rule-key parity guard (5 shipped vs 6 described, the one described-only member being the retired spelling), recorded at the capture commit
  - evidence/gates/capture-attempts/ — the three earlier captures: two stopped on their own findings (red log, RED/GREEN pair, the release compiler log that carried the warning) and one all-green bundle superseded when the branch reviews changed the source
  - three source fixes the capture surfaced: dead stores and a shadowed local in the modal loops (cppcheck), the window manager's own _NET_WM_NAME published one byte short (smoke transcript), an ignored write() result in the signal handler (release compiler log); plus the four the two branch reviews surfaced between captures 3 and 4 (review-fixes/README.md)
affects: [08.5-05, COMPILED_CODE_BEHAVIOR_CHECKLIST.md, evidence/README.md, v1.0 release]

actuals:
  tokens: 82000
  tasks: 3
  commits: 6

tech-stack:
  added: []
  patterns:
    - "A capture that goes red is stopped, its red log kept under a distinguishing name, the finding fixed at source, and the whole capture re-run at the fixed commit — never a gate re-run until it passes"
    - "Read every produced log before committing it, not only the transcript T-8-REMOTE names: two of the three findings here were in files that had been produced, green, and were about to be committed unread"
    - "A gate log whose content is byte-identical across captures on an unchanged host (preflight-versions.log) gets a two-line header binding it to the commit, in the form ldd-release.txt already uses; the build-all logs are bound by PROVENANCE.txt alone"

key-files:
  created:
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-debug.log
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-release.log
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-asan.log
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-debug.log
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-release.log
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-asan.log
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/sanitizer-reports/
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/static-analysis.log
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/preflight-versions.log
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/ldd-release.txt
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/DOC-GUARDS.txt
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/capabilities.txt
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/root-properties.txt
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/window-tree.txt
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/capture-attempts/README.md
  modified:
    - src/Border.cpp
    - src/Client.cpp
    - src/Manager.cpp
    - src/Events.cpp
    - tests/test_wm_process.cpp
    - tests/test_wm_rules.cpp
    - tests/test_eventloop.cpp

key-decisions:
  - "Precondition satisfied by the closeout's own ten-run table (fix-measurement/README.md, ten green at 70fbd8f) rather than by an 08.5-07 summary, per 08.5-12 Task 5 Decision A"
  - "Each of the three findings was fixed at source and the entire capture re-run at the fixed commit; no log from a superseded commit is kept except the red or warning-carrying one that stopped it"
  - "The cppcheck baseline was not regenerated; the five new findings were removed from the code"
  - "preflight-versions.log carries a two-line commit header because its content is otherwise byte-identical on an unchanged host"

patterns-established:
  - "capture-attempts/: a record directory for stopped captures, opening by saying what it is not, holding only what each attempt found"

requirements-completed: [TEST-08, XDIS-05, RULES-01]

coverage:
  - id: D1
    description: "Blockers 1 and 2 — build and full ctest in debug, release and asan trees, each log independently closing with its own build-all OK line"
    requirement: TEST-08
    verification:
      - kind: integration
        ref: "bash scripts/gates/build-all.sh {debug,release,asan} → evidence/gates/build-all-*.log"
        status: pass
    human_judgment: false
  - id: D2
    description: "Blocker 3 — static analysis against the untouched baseline"
    requirement: TEST-08
    verification:
      - kind: automated_ui
        ref: "bash scripts/analysis/run-static-analysis.sh → evidence/gates/static-analysis.log, last line static-analysis OK"
        status: pass
    human_judgment: false
  - id: D3
    description: "Blocker 4 — runtime smoke transcript at the capture commit on a display started for the run"
    requirement: TEST-08
    verification:
      - kind: e2e
        ref: "scripts/capture-display-capabilities.sh :135 runtime-smoke under WM2_EVIDENCE_ROOT → evidence/gates/runtime-smoke/"
        status: pass
    human_judgment: false
  - id: D4
    description: "Gap 1 of 08.5-VERIFICATION.md closed by recorded readings at the capture commit; RULES-01 rule-key parity recorded with both set sizes"
    requirement: XDIS-05
    verification:
      - kind: other
        ref: "evidence/gates/DOC-GUARDS.txt — guard A 0, guard B 0, shipped 5 / described 6, described-only = rule-match-name"
        status: pass
    human_judgment: false
  - id: D5
    description: "PROVENANCE.txt binds the bundle to one commit and the no-drift command prints nothing"
    requirement: TEST-08
    verification:
      - kind: other
        ref: "git diff --name-only f54de6efec4057ec9259d0200f70b93fae5cc4f4..HEAD -- src include tests scripts CMakeLists.txt (empty)"
        status: pass
    human_judgment: false
  - id: D6
    description: "rule-match-title byte-semantics limit recorded, not closed (v1.1)"
    requirement: RULES-01
    verification: []
    human_judgment: true
    rationale: "The plan marks this truth verification: backstop — no test pins the behaviour; DOC-GUARDS.txt records both caps and both source lines and names it as a v1.1 line"

duration: 130min
completed: 2026-09-05
status: complete
---

# Phase 8.5 Plan 08: Criterion-7 gate capture — Summary

**The four hard blockers are re-run at the shipping tree and bound to one commit; it took four captures — the first two surfaced defects in their own logs, the third was superseded when the branch reviews changed the source — and every finding was fixed at source rather than recorded around.**

## Performance

- **Duration:** about 130 min wall clock across four full captures (first launch 13:33:44Z; final capture 15:20:14Z–15:43:26Z)
- **Tasks:** 3 of 3
- **Files created:** 15 bundle files plus `capture-attempts/` (5 files)
- **Source files modified by the findings:** 4 source files, 3 test files (capture findings plus the two branch reviews between captures 3 and 4)

## Accomplishments

- Debug, release and ASan trees each **336/336**, exit 0, logs closing with their own `build-all OK: <tree>` line; **0 compiler or linker warning lines in every tree** (Phase 8's baseline was 0; attempt 2 had 1 and was stopped).
- Static analysis `OK`: cppcheck **2.7**, 15 findings, all accounted for in the baseline; clang-tidy **14.0.0**, 0 fatal, 149 reported-only, no enforced check fired. Baseline file unmodified (`git status --porcelain -- scripts/analysis/cppcheck-suppressions.xml` empty).
- Six sanitizer report files copied in full; every one is the LeakSanitizer suppression-accounting stanza `Suppressions used: 2 288 libfontconfig` (all six byte-identical, one md5) — accounting, not findings.
- `DOC-GUARDS.txt` closes gap 1 by reading, names `195ac51`, and records the parity guard with both set sizes.
- Runtime smoke on Xvfb `:135` (`-nolisten tcp`, no access-control flag), release binary plus one `xmessage` titled `wm2-smoke-f54de6e`; window tree read before commit: only the window manager's windows and that client. `_NET_CLIENT_LIST` present (4 lines in root-properties.txt).
- `PROVENANCE.txt` names commit `f54de6efec4057ec9259d0200f70b93fae5cc4f4`, branch, date, one exit line per gate (debug 0, release 0, asan 0, static-analysis 0, preflight 0, runtime-smoke 0), the three test totals quoted from the logs, the sanitizer-report statement, `DOC-GUARDS.txt`, the measurement directories as non-gate records, and the no-drift command.

## Gate results, quoted

| Gate | Exit | Reading from its own log |
|---|---|---|
| debug | 0 | `100% tests passed, 0 tests failed out of 336`; `Total Test time (real) = 239.04 sec`; 0 warning lines |
| release | 0 | `100% tests passed, 0 tests failed out of 336`; `233.44 sec`; `link audit OK (24 entries, all in the intended runtime set)`; 0 warning lines |
| asan | 0 | `100% tests passed, 0 tests failed out of 336`; `383.13 sec`; 6 report files, all suppression accounting; 0 warning lines |
| static-analysis | 0 | `cppcheck OK: 15 finding(s), all accounted for in the baseline`; `clang-tidy OK: no enforced check fired`; `static-analysis OK` |
| preflight | 0 | `pkg-config modules` section present: x11 1.7.5, xext 1.3.4, xft 2.3.4, fontconfig 2.13.1, xrandr 1.5.2 |
| runtime-smoke | 0 | SHAPE, RANDR, RENDER present; header `commit: f54de6ef…` matches PROVENANCE |

`ctest --test-dir build/debug -N --no-tests=error | tail -1` after the gate: `Total Tests: 336`, agreeing with all three logs.

## `cmp` against the Phase 8 bundle

Every file with a Phase 8 counterpart differs: build-all-{debug,release,asan}.log, compiler-{debug,release,asan}.log, static-analysis.log, preflight-versions.log, ldd-release.txt, PROVENANCE.txt. The three runtime-smoke files have no Phase 8 counterpart at that path. In attempt 1 `preflight-versions.log` **was** byte-identical to Phase 8's — the script prints only versions, the host has not changed, and the file carried no commit — so from attempt 2 on it is prefixed by two `#` header lines naming the commit and date, in the form `ldd-release.txt` already uses; the command's own output follows unedited.

## The three documentation guards (DOC-GUARDS.txt)

| Guard | Command | Reading |
|---|---|---|
| Gap-1 A (XDIS-05) | `grep -c 'never installed' docs/RELEASE-NOTES.md` | **0** |
| Gap-1 B (XDIS-05) | `grep -c 'Not tested' docs/RELEASE-NOTES.md` | **0** |
| Rule-key parity (RULES-01) | shipped: `sed -n '/isRuleMatchKey/,/;/p' src/Config.cpp \| grep -oE 'rule-match-[a-z]+' \| sort -u`; described: `grep -oE 'rule-match-[a-z]+' docs/RELEASE-NOTES.md \| sort -u` | shipped **5** (class, instance, mode, title, type); described **6**; shipped-only (none); described-only `rule-match-name` — the expected single asymmetry, the retired spelling surviving only in the migration note (D-8.5-01) |

An exact-membership verdict (shipped set equals the five keys; described-only equals exactly `rule-match-name`) is appended: PASS / PASS. The file states that a zero on either side would be a guard failure, that `08.5-VERIFICATION.md` predates `195ac51` and these readings are what close gap 1, the byte-semantics limit (400-byte title read at `src/Client.cpp:1005` — the plan cites `:875`, which the file also names literally; 256-character config value at `src/Config.cpp:130-132`) as a v1.1 line not fixed here, and that a guard reading is a snapshot nothing re-runs.

## No-source-drift

```
git diff --name-only f54de6efec4057ec9259d0200f70b93fae5cc4f4..HEAD -- src include tests scripts CMakeLists.txt
```
prints nothing at the time of writing. Later commits in this phase are documentation only; the reader re-runs the command at the phase's final commit.

## Task Commits

1. **Findings from attempt 1, fixed** — `2a94cbb` (fix: dead stores in the modal loops, WM name published one byte short) with the RED/GREEN pair under `capture-attempts/`
2. **Finding from attempt 2, fixed** — `d08b6e5` (fix: bind write()'s result at both self-pipe sites)
3. **Attempt 3 bundle** — `fa8c533` (superseded; retired in `f54de6e`)
4. **Branch-review fixes between attempts 3 and 4** — `6412440` (CodeRabbit CLI: tab-button hide/kill on signal, two test-helper hardenings), `f54de6e` (Codex: gesture interruption, frameless maximize)
5. **Task 1-3 final bundle** — the commit that follows this summary (docs(08.5-08): criterion-7 gate bundle captured at f54de6e)

## Deviations from Plan

### Auto-fixed issues (each stopped its capture; fixed at source; whole capture re-run)

**1. [Rule 1 — Bug] Static analysis red at `8e29d6d`: five cppcheck findings outside the baseline**
- **Found during:** Task 2, attempt 1
- **Issue:** four `unreadVariable` (`done = true; break;` — the store is dead) in the ledger-8 modal loops (`src/Border.cpp` tab-button loop; `src/Client.cpp` move, resize, gesture) and one `shadowVariable` (`ModalWait w` shadowing the resize loop's width local). All from this closeout's own code.
- **Fix:** stores deleted, result renamed `wait`. Baseline not regenerated. Red log kept: `capture-attempts/8e29d6d-static-analysis-FAILED.log`.
- **Verification:** `static-analysis OK` on the cleaned tree before the re-capture, and in the final bundle.

**2. [Rule 1 — Bug] The window manager's own `_NET_WM_NAME` published one byte short**
- **Found during:** Task 3, attempt 1 — reading the window tree before committing it showed the check window titled `"wm2-born-agai"`
- **Issue:** `src/Manager.cpp` passed a literal length 13 for the 14-byte name. Present since the EWMH work; visible in every earlier transcript, never read.
- **Fix:** length from `std::strlen`. The `[wm_process]` supporting-window case now reads the name back as `UTF8_STRING`, byte for byte, and requires `"wm2-born-again"`: `capture-attempts/8e29d6d-red-wm-name-truncated.log` (`"wm2-born-agai" == "wm2-born-again"` fails), `8e29d6d-green-wm-name-truncated.log` (17 assertions).

**3. [Rule 1 — Bug] One new release-build warning at `2a94cbb`**
- **Found during:** reading `compiler-release.log` of attempt 2 (every gate green) — `src/Manager.cpp:434: ignoring return value of 'write' … [-Wunused-result]`, where Phase 8's release log had 0 warning lines
- **Issue:** the 08.5-11 self-pipe signal handler used `(void)write(...)`; GCC does not take a `(void)` cast as a use of a `warn_unused_result` value, and the attribute is enforced only under `-O2`, so debug and asan were silent.
- **Fix:** result bound to a named local at both self-pipe sites (`src/Manager.cpp`, `src/Events.cpp`). Release build of the window-manager target: no warning line. Log kept: `capture-attempts/2a94cbb-compiler-release.log`.

### Recorded, not fixed

- **Deferred item 13** — the smoke run's stderr carries one `X_UnmapWindow … BadWindow` at shutdown (the resize handle is a child of the client window; every client destroy logs one). Pre-existing, already recorded; not touched.
- **Credential screen** — `grep -rnE 'API_KEY|ACCESS_KEY|SECRET|TOKEN|PRIVATE KEY|Bearer ' evidence/gates/` has exactly one match: `flake-measurement/README.md:117`, the sentence that names the screen's own pattern. Benign.
- **Precondition wording** — the plan's precondition names an 08.5-07 summary; 08.5-07 was superseded (08.5-12 Task 5 Decision A) and the precondition was re-planned onto `fix-measurement/README.md`, which carried ten consecutive green debug runs at `70fbd8f` when the first capture started. That tree differs from the final capture tree by the fix commits named above, which `fix-measurement/README.md` records. A third ten-run series at the final capture commit `f54de6e` follows and is the README's third section.
- **Smoke header commit in attempt 2** — `capabilities.txt` stamps HEAD at the moment it runs; in attempt 2 a documentation commit had landed during the release build so the header named `af242a2` while PROVENANCE named `2a94cbb`. In captures 3 and 4 no commit landed and both files name the same commit. Recorded so the next capture keeps the tree still.
- **Attempt 3 superseded by review** — the all-green bundle at `d08b6e5` was committed, then the CodeRabbit CLI and Codex passes over the whole branch changed `src/` and `tests/` (one hide-or-kill on a signal during a tab-button press; an uninitialised event read on gesture interruption; frameless maximize one pixel short; two test-helper hardenings), so it no longer described the shipping tree. Retired, recorded as attempt 3 in `capture-attempts/README.md`, re-captured at `f54de6e`.

## What this bundle does not establish

A gate bundle is a snapshot bound to `f54de6e`; nothing in the repository re-runs it when a later commit lands. The measurement directories beside it (`flake-measurement/`, `attribution/`, `wakeup/`, `reproducer/`, `eventloop/`, `timestamp/`, `menupaint/`, `modal/`, `review-fixes/`, `fix-measurement/`, `capture-attempts/`) are records, not gate results, and each opens by saying so.
