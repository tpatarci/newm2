---
phase: 08.5-v1.0-closeout
plan: 4
type: execute
wave: 2
depends_on: ["08.5-03"]
files_modified:
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-debug.log
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-release.log
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-asan.log
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-debug.log
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-release.log
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-asan.log
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/sanitizer-reports/
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/static-analysis.log
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/preflight-versions.log
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/ldd-release.txt
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/capabilities.txt
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/root-properties.txt
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/window-tree.txt
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt
autonomous: true
gap_closure: true
requirements: [TEST-08]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Each of the four hard blockers has a log under .planning/phases/08.5-v1.0-closeout/evidence/gates/ produced by a command run during this phase, not copied from another phase's bundle."
    - "PROVENANCE.txt names the commit the gates ran at, and no tracked file under src/, include/, tests/, scripts/ or CMakeLists.txt differs between that commit and the phase's final commit."
    - "A reviewer can tell, from the bundle alone and without running anything, which tree each figure came from and whether that tree's run was clean."
    - "The runtime smoke transcript (blocker 4) was captured against a binary built during this phase, on a display started for the run."
    - "Any test that failed and was then attributed to the known intermittent was reproduced in isolation and its measured rate is recorded next to the claim."
  artifacts:
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-debug.log
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-release.log
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-asan.log
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/static-analysis.log
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/preflight-versions.log
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/ldd-release.txt
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/capabilities.txt
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt
  key_links:
    - "scripts/gates/build-all.sh -> evidence/gates/build-all-{debug,release,asan}.log (blockers 1 and 2)"
    - "scripts/analysis/run-static-analysis.sh -> evidence/gates/static-analysis.log (blocker 3)"
    - "scripts/capture-display-capabilities.sh (WM2_EVIDENCE_ROOT override) -> evidence/gates/runtime-smoke/ (blocker 4)"
    - "git HEAD at capture time -> evidence/gates/PROVENANCE.txt -> the gate rows plan 08.5-05 repoints"
  prohibitions:
    - "Do not copy, adapt or partially reuse any file from .planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/gates/. Inheriting that snapshot is the exact thing success criterion 7 forbids, and a copied log is worse than a missing one because it looks like evidence."
    - "Do not transcribe a figure from 08.5-VERIFICATION.md or from any summary into a log or into PROVENANCE.txt. Every number in this bundle is the output of a command run in this plan."
    - "Do not create sanitizer-reports/ if the ASan run produced no report files. An empty or fabricated directory misrepresents what the gate found. Record the absence in PROVENANCE.txt instead."
    - "Do not attribute a failing test to the known intermittent without reproducing it in isolation and recording the measured rate. A single failure assumed to be the known flake is how a real regression ships."
    - "Do not run a bare ctest against the asan tree. LSan suppressions are not wired into the Catch2 binaries (deferred item 5), so a bare run is red for reasons that are not findings. The asan tree is gated only through scripts/gates/build-all.sh."
    - "Do not read, copy, quote or commit ~/.xsession-errors or any part of it."
    - "Do not use pkill with a process-name pattern to stop an X server. The pattern matches the killing command's own wrapper and takes the shell down with it; this happened on this host on 2026-08-30. Kill by PID."
    - "Do not start the smoke display with any network-exposing or access-control-disabling flag (threat T-8-AC)."
---

<objective>
Capture the durable gate record this phase never produced. Success criterion 7
asks for the four hard blockers re-run at the final commit rather than inherited
from Phase 8's snapshot, and `.planning/phases/08.5-v1.0-closeout/evidence/gates/`
does not exist.

Purpose: the gates themselves pass today -- the verifier ran
`scripts/analysis/run-static-analysis.sh` to exit 0 and the phase's own session
reported a green regression run at HEAD. This is missing record, not a red gate,
so plan for capture-and-commit rather than for debugging. What is missing is
what a release signoff is checkable against: every gate row in
`COMPILED_CODE_BEHAVIOR_CHECKLIST.md` currently certifies a tree that no longer
exists, five source files having changed since that snapshot.

Output: a `gates/` bundle carrying all four blockers plus the environment and
link-surface evidence the Release Evidence section requires, and a
`PROVENANCE.txt` that binds the whole bundle to one commit and proves nothing in
the source moved after it.

Sequencing: this plan depends on 08.5-03 because a gate bundle captured before
the release-notes correction lands is stale the moment that correction is
committed. The documentation corrections go first; the gates certify the tree
that results.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/README.md
</context>

<!-- planner-discipline-allow: libXtst -->

<tasks>

<task type="auto">
  <name>Task 1: Blockers 1 and 2 -- build and full ctest in all three trees, logs captured</name>
  <files>.planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-debug.log, .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-release.log, .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-asan.log, .planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-debug.log, .planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-release.log, .planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-asan.log, .planning/phases/08.5-v1.0-closeout/evidence/gates/sanitizer-reports/</files>
  <read_first>
    - `scripts/gates/build-all.sh:1-60` -- the header's two design points. `--no-tests=error` is mandatory and there is exactly one test-runner invocation carrying it; sanitizer reports from forked children are sunk under the build tree rather than a fixed `/tmp` name (threat T-8-TMP). Read this before deciding what a green run means.
    - `scripts/gates/build-all.sh:130-155` -- where each tree's build output is teed to `build/<tree>/build.log` and the warning count is printed. Those are the files that become `compiler-*.log` in the bundle.
    - `.planning/phases/08.5-v1.0-closeout/08.5-CONTEXT.md` "Known flake" and "Deferred items still open" -- item 17 `[wm_menureopen]` fails intermittently at a measured 11/1 rate on both the old and the reworked menu code, and item 5 records that a bare ctest against the asan tree is red for reasons that are not findings.
    - `.planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/gates/build-all-debug.log` (tail only, ~12 lines) -- the shape of a completed log and the `build-all OK: <tree>` line that closes it. Read for shape; copy nothing.
  </read_first>
  <precondition>The working tree has no uncommitted modifications to tracked files under `src/`, `include/`, `tests/`, `scripts/` or `CMakeLists.txt` -- `git status --porcelain -- src include tests scripts CMakeLists.txt` prints nothing. A gate run against a dirty source tree certifies a state no commit holds.</precondition>
  <reversibility rating="reversible">New log files under a new directory. Deleting them restores the prior state exactly; nothing consumes them until plan 08.5-05 repoints the checklist at them.</reversibility>
  <action>
Create `.planning/phases/08.5-v1.0-closeout/evidence/gates/` and run the build-and-test gate once per tree, capturing each run's full output.

Run the three trees separately rather than as one invocation, so that a tree that goes red produces a log for itself and does not truncate the other two: `bash scripts/gates/build-all.sh debug`, then `release`, then `asan`, each with `set -o pipefail` and its stdout and stderr teed into `evidence/gates/build-all-<tree>.log`. The gate script configures, builds and runs the unfiltered suite in each tree.

After each tree completes, copy that tree's `build/<tree>/build.log` -- the compiler and linker warning capture the script writes -- to `evidence/gates/compiler-<tree>.log`. The Release Evidence section has a row that points at those files for the "no new warnings" claim, and plan 08.5-05 repoints it at this bundle.

**Sanitizer reports.** After the asan tree finishes, look at `build/asan/sanitizer-reports/`. If it holds files, copy them into `evidence/gates/sanitizer-reports/` in full and read one before committing: Phase 8's six were LeakSanitizer suppression accounting rather than findings, and the whole reason they were committed in full was so that distinction could be checked rather than believed. If the directory is empty or absent, create nothing -- record the absence in the summary and leave it for Task 4 to state in `PROVENANCE.txt`.

**If a tree goes red.** Do not re-run until it happens to pass and commit that. The phase's own record already contains one intermittent, `[wm_menureopen]`, measured at 11 passes to 1 failure over twelve isolated runs on both the reworked and the pre-existing menu code. The rule from `08.5-CONTEXT.md` is that a single failure counts as that known intermittent only after being reproduced in isolation, never by assumption. So: identify the failing case by name, run it alone at least twelve times, record the observed pass/fail split, and put that measurement in the summary next to the claim. If the failing case is anything other than the known intermittent, stop and report -- a real regression at the release gate is a finding, not a retry.

**Keep the red log.** If a tree needed a second run, both logs belong in the bundle under distinguishable names, with the reason in the summary. A bundle that shows only the run that passed is the same class of record as the snapshot this criterion was written to eliminate.
  </action>
  <verify>
    <automated>set -o pipefail && bash scripts/gates/build-all.sh debug 2>&1 | tee .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-debug.log</automated>
    <fails_when>non-zero exit; or the log's last line is not `build-all OK: debug`; or the log contains a line matching `tests failed` or `: FAILED`. A silently-empty run is caught by the gate's own mandatory `--no-tests=error`, which makes a zero-test run exit non-zero rather than green.</fails_when>
    <automated>set -o pipefail && bash scripts/gates/build-all.sh release 2>&1 | tee .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-release.log</automated>
    <fails_when>non-zero exit; or the log's last line is not `build-all OK: release`; or the log's link audit section reports `libXtst`, which is test-only and must not be linked into the shipped binary.</fails_when>
    <automated>set -o pipefail && bash scripts/gates/build-all.sh asan 2>&1 | tee .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-asan.log</automated>
    <fails_when>non-zero exit; or the log's last line is not `build-all OK: asan`; or the log reports a sanitizer finding rather than the `no sanitizer findings` verdict.</fails_when>
    <automated>for t in debug release asan; do test -s ".planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-$t.log" && test -s ".planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-$t.log" || echo "MISSING: $t"; done</automated>
    <fails_when>prints any `MISSING:` line -- a tree ran without leaving both of its logs in the bundle, or a log was created empty.</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-{debug,release,asan}.log` all exist, are non-empty, and each ends with its own `build-all OK: <tree>` line
    - `.planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-{debug,release,asan}.log` all exist and are non-empty
    - The three logs' ctest totals agree with each other and with `ctest --test-dir build/debug -N | tail -1` run after the gate; any disagreement is investigated, not averaged
    - No file in `evidence/gates/` is byte-identical to its counterpart in `.planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/gates/` -- checked with `cmp` and the result recorded
    - `evidence/gates/sanitizer-reports/` exists only if `build/asan/sanitizer-reports/` produced files; if it exists, at least one file was read and its content class (suppression accounting versus finding) is stated in the summary
    - If any tree required a re-run, both logs are in the bundle, the failing case is named, and its isolated pass/fail split over at least twelve runs is recorded
    - `git status --porcelain -- src include tests scripts CMakeLists.txt` printed nothing before the first gate ran
  </acceptance_criteria>
  <done>Blockers 1 and 2 have logs produced by runs in this phase, at this phase's tree, with any intermittent measured rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 2: Blocker 3 and the environment evidence -- static analysis, tool versions, release link surface</name>
  <files>.planning/phases/08.5-v1.0-closeout/evidence/gates/static-analysis.log, .planning/phases/08.5-v1.0-closeout/evidence/gates/preflight-versions.log, .planning/phases/08.5-v1.0-closeout/evidence/gates/ldd-release.txt</files>
  <read_first>
    - `scripts/analysis/run-static-analysis.sh:1-30` -- why the two tools are handled differently: cppcheck gets a content-hash baseline at `scripts/analysis/cppcheck-suppressions.xml`, clang-tidy has no baseline concept and gets an explicit fatal-check allowlist in `.clang-tidy`. The gate exits non-zero the moment a finding appears that is not already in the baseline.
    - `.planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/gates/static-analysis.log` -- the shape: a cppcheck block, a clang-tidy block, and a closing `static-analysis OK`. Read for shape; copy nothing.
    - `.planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/gates/ldd-release.txt:1-4` -- the two header comment lines that state what the file is for and the one library that must not appear in it.
    - `scripts/preflight.sh:60-110` -- the pkg-config module list and the toolchain version lines. This is the output that becomes `preflight-versions.log` and backs the Environment Preflight rows.
  </read_first>
  <precondition>`cppcheck` and `clang-tidy` are both on PATH -- `command -v cppcheck && command -v clang-tidy` exits 0. Plan 08-01 recorded a host where neither was installed, and the gate degrades rather than failing loudly if a tool is missing, which would produce a green log that measured half of what it claims.</precondition>
  <action>
Capture blocker 3 and the two environment artifacts the Release Evidence section points at.

**Static analysis.** Run `bash scripts/analysis/run-static-analysis.sh` with its combined output teed to `evidence/gates/static-analysis.log`. Do not pass `--regenerate-baseline`: regenerating the baseline at signoff time would absorb any new finding into it silently, which is the opposite of what a gate is for. If a new finding appears, stop and report it rather than baselining it.

Record in the summary the two figures the log states -- the cppcheck finding count against the baseline, and whether any enforced clang-tidy check fired -- and note the tool versions the log names, because the baseline is keyed on `(id, fileName)` and a different cppcheck version can change what it reports.

**Tool and dependency versions.** Run `bash scripts/preflight.sh` with its combined output teed to `evidence/gates/preflight-versions.log`. This backs both the Environment Preflight rows and the Release Evidence "dependency/package list and tool versions" row.

**Release link surface.** With the release tree built by Task 1, capture `ldd build/release/wm2-born-again` into `evidence/gates/ldd-release.txt`, preceded by two comment lines in the same form Phase 8 used: the command that produced it, and the statement that test-only libraries must not appear. Then check the file for `libXtst` -- it is linked into the test binaries and must not reach the shipped one. If it appears, that is a real finding and the phase does not close on it.
  </action>
  <verify>
    <automated>set -o pipefail && bash scripts/analysis/run-static-analysis.sh 2>&1 | tee .planning/phases/08.5-v1.0-closeout/evidence/gates/static-analysis.log</automated>
    <fails_when>non-zero exit, or the log does not end with `static-analysis OK`. A new finding outside the baseline exits non-zero and names the file it came from.</fails_when>
    <automated>set -o pipefail && bash scripts/preflight.sh 2>&1 | tee .planning/phases/08.5-v1.0-closeout/evidence/gates/preflight-versions.log</automated>
    <fails_when>non-zero exit, or the resulting log contains no `pkg-config modules` section -- preflight ran in a mode that suppressed the version lines, leaving a file that records nothing.</fails_when>
    <automated>ldd build/release/wm2-born-again | tee -a .planning/phases/08.5-v1.0-closeout/evidence/gates/ldd-release.txt</automated>
    <fails_when>non-zero exit (the release binary is absent, meaning Task 1's release tree did not build), or the output contains `libXtst`, which is test-only and must not be linked into the shipped binary.</fails_when>
    <automated>! grep -v '^#' .planning/phases/08.5-v1.0-closeout/evidence/gates/ldd-release.txt | grep -q 'libXtst'</automated>
    <fails_when>non-zero exit -- the shipped binary links a test-only library. This is a finding, not a bundling problem. The `grep -v '^#'` filter is load-bearing: the file's own two leading comment lines name the excluded library, so an unfiltered gate would fail against a correct file and pass only if the header were deleted.</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/gates/static-analysis.log` exists, is non-empty, and its last line is `static-analysis OK`
    - The log names both tool versions, and the cppcheck finding count and clang-tidy verdict are quoted in the summary from the log rather than from any prior phase's figure
    - `scripts/analysis/cppcheck-suppressions.xml` is unmodified by this task -- `git status --porcelain -- scripts/analysis/cppcheck-suppressions.xml` prints nothing
    - `evidence/gates/preflight-versions.log` exists, is non-empty, and contains a `pkg-config modules` section listing `x11`, `xext`, `xft` and `fontconfig` with versions
    - `evidence/gates/ldd-release.txt` exists, opens with the two comment lines naming the command and the excluded library, and contains no `libXtst` line **outside** those leading comment lines -- the filtered gate in `<verify>` is the check, and an unfiltered one would be self-invalidating against its own header
    - No file written by this task is byte-identical to its counterpart in the Phase 8 bundle
  </acceptance_criteria>
  <done>Blocker 3 and the two environment artifacts exist for this phase's tree, produced by fresh runs, with the baseline untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Blocker 4 -- a runtime smoke transcript at this phase's commit</name>
  <files>.planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/capabilities.txt, .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/root-properties.txt, .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/window-tree.txt</files>
  <read_first>
    - `scripts/capture-display-capabilities.sh:1-60` -- the three design points in the header (a missing extension is a result and not a failure; no server flags are suggested or applied; the transcript contains window titles and is committed), the `<display> <label>` argument shape, and `EVIDENCE_ROOT="${WM2_EVIDENCE_ROOT:-$PHASE_DIR/evidence}"` at line 55. `PHASE_DIR` is hardcoded to the Phase 8 directory, so the override is mandatory or the output lands in the wrong bundle.
    - `.planning/phases/08.5-v1.0-closeout/evidence/x2go-nxagent/SCOPE.md` -- the "What was run" block, for the shape of an Xvfb-plus-client invocation that produced a clean transcript on this host.
    - `.planning/phases/08.5-v1.0-closeout/08.5-CONTEXT.md` "Operational gotchas" -- the host carried 23 sockets in `/tmp/.X11-unix/` on 2026-08-30, so a low display number is not free; and the rule against stopping X servers by process-name pattern.
    - `.planning/phases/08.5-v1.0-closeout/08.5-CONTEXT.md` security section -- T-8-REMOTE, and why a window tree captured on this workstation must be read before it is committed.
  </read_first>
  <precondition>An unused X display number is available -- check `/tmp/.X11-unix/` first and pick a number no socket there claims. The host accumulates orphaned Xvfb servers from test runs, so the low numbers are not free.</precondition>
  <action>
Blocker 4 is the runtime smoke transcript, and it is the one blocker whose Phase 8 artifact cannot simply be re-pointed: the transcripts this phase already holds were captured at `9869aea` and `f5bbea7`, both before the release-notes correction and both stamping their own commit in their provenance header. Capture a fresh one against the binary Task 1 built.

Start an Xvfb on a display number nothing else claims, with no network-exposing and no access-control-disabling flag. Start `build/release/wm2-born-again` on it, then one test client with a title chosen for this run so the window tree carries nothing from the operator's real desktop.

Run the capture script with the evidence root overridden to this phase's gates directory and the label `runtime-smoke`, so the three files land at `evidence/gates/runtime-smoke/`. The script writes its own provenance header naming the commit, the branch and the tree state; that header is what ties this transcript to the same commit as the rest of the bundle.

**Read the window tree before committing it.** This workstation also runs the operator's real desktop and `xwininfo -root -tree` records every window's title. The transcript is taken on a display started for this run and should therefore carry only the window manager's own windows and the one test client -- confirm that by reading it, not by assuming it.

Stop the Xvfb by PID when the capture is done. Do not stop it with a process-name pattern: the pattern matches the killing command's own wrapper and takes the shell down with it, which happened on this host on 2026-08-30.

A missing extension in this capture is a result, not a failure -- the script is built on that principle and this display is a plain Xvfb, so the matrix is expected to show the full set.
  </action>
  <verify>
    <automated>test -s .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/capabilities.txt && test -s .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/root-properties.txt && test -s .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/window-tree.txt</automated>
    <fails_when>non-zero exit -- one of the three transcript files is missing or zero-length, which happens when `WM2_EVIDENCE_ROOT` was not exported and the capture landed in the Phase 8 bundle instead.</fails_when>
    <automated>grep -c 'commit:' .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/capabilities.txt</automated>
    <fails_when>prints `0` -- the file exists but carries no provenance header, so it cannot be tied to a commit and is not evidence of anything.</fails_when>
    <automated>grep -c '_NET_CLIENT_LIST' .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/root-properties.txt</automated>
    <fails_when>prints `0` -- the window manager was not running, or was running and had not published its root properties, when the capture ran. A transcript of an unmanaged display is not a runtime smoke transcript.</fails_when>
    <automated>! grep -rnE 'API_KEY|ACCESS_KEY|SECRET|TOKEN|PRIVATE KEY|Bearer ' .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/</automated>
    <fails_when>non-zero exit, and the command prints the file and line it matched -- a credential pattern reached a transcript that is about to be committed. Read the match before doing anything else; do not delete the line and re-run.</fails_when>
  </verify>
  <acceptance_criteria>
    - All three files exist under `evidence/gates/runtime-smoke/` and each is non-empty
    - `capabilities.txt` carries the script's provenance header with a `commit:` line, and that commit is the one Task 4 records in `PROVENANCE.txt`
    - `root-properties.txt` contains a `_NET_CLIENT_LIST` entry with the test client in it, and the full `_NET_SUPPORTED` list
    - `window-tree.txt` shows the test client reparented inside a frame, with the tab and button children present
    - `window-tree.txt` was read before commit and contains no window title, host name or user name from the operator's real desktop -- only the window manager's own windows and the run's test client (T-8-REMOTE)
    - The credential screen over `evidence/gates/runtime-smoke/` exits 0 with no output
    - The same screen run over the whole of `evidence/gates/` either exits 0, or every match is quoted in the summary with the reason it is benign
    - The display was started with no network-exposing and no access-control-disabling flag, and was stopped by PID
  </acceptance_criteria>
  <done>Blocker 4 has a transcript taken against a binary built in this phase, on a display started for the run, screened before commit.</done>
</task>

<task type="auto">
  <name>Task 4: PROVENANCE.txt -- bind the bundle to one commit and prove nothing moved after it</name>
  <files>.planning/phases/08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt</files>
  <read_first>
    - `.planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/gates/PROVENANCE.txt` -- all six lines. It records the full commit hash, the branch, the date and one exit-code line per gate. That shape is the minimum; this phase's file adds the source-drift proof, because drift is the thing success criterion 7 is about.
    - `.planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/README.md:1-20` -- the snapshot caveat, which states in Phase 8's own words that nothing re-validates a bundle when later commits land and that refreshing it is a manual per-release action. That caveat is why this task exists.
    - `.planning/phases/08.5-v1.0-closeout/08.5-VERIFICATION.md:59-88` -- gap 2's `reason` and `missing[]`, which name PROVENANCE.txt and the commit it must record.
  </read_first>
  <precondition>Tasks 1, 2 and 3 have all completed and their artifacts are on disk. `PROVENANCE.txt` records exit codes for gates that have already run; written first, it would record predictions.</precondition>
  <action>
Write `evidence/gates/PROVENANCE.txt` binding the whole bundle to one commit.

It carries, at minimum:

- the full commit hash and the short form, and the branch, taken from `git rev-parse` at the moment of writing;
- the date in the same ISO form Phase 8's file used;
- one line per gate with its recorded exit code: debug, release, asan, static-analysis, and the runtime smoke capture;
- the tree state at capture, from `git status --porcelain`, stated as clean or dirty with the dirty paths named;
- the test totals the three trees reported, quoted from the logs rather than from memory;
- an explicit line stating whether `build/asan/sanitizer-reports/` produced files, and if it did not, that no `sanitizer-reports/` directory exists in this bundle and why -- absence recorded is evidence; absence unmentioned looks like an oversight.

**The line that makes this criterion checkable.** Add a statement, with the command that produced it, that no tracked file under `src/`, `include/`, `tests/`, `scripts/` or `CMakeLists.txt` differs between the recorded commit and the phase's final commit. This is the difference between "gates were run" and "gates were run against the tree that shipped". Documentation commits landing afterwards -- plan 08.5-05 rewrites the checklist and writes the evidence index -- do not invalidate a gate result, and stating exactly that, with the command a reader can re-run, is what stops the next reader from having to guess.

**Name the phase.** The bundle must be identifiable as this phase's without reading a path: `PROVENANCE.txt` states the phase directory it belongs to. The gate rows plan 08.5-05 rewrites will cite it by phase-qualified path for the same reason.

Do not carry over any figure or commit reference from the Phase 8 bundle. Every number in this file comes from a log produced in Tasks 1 to 3 or from a git command run now.
  </action>
  <verify>
    <automated>test -s .planning/phases/08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt</automated>
    <fails_when>non-zero exit -- the file is missing or zero-length.</fails_when>
    <automated>grep -cF "$(git rev-parse HEAD)" .planning/phases/08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt</automated>
    <fails_when>prints `0` -- the recorded commit is not HEAD, which means the bundle was written before the gates' commit landed or the hash was transcribed rather than computed. If the phase has committed since the gates ran, the file must instead carry the gate commit plus the drift proof below; a `0` here with no drift proof is a fail.</fails_when>
    <automated>! grep -qF 'c61cb4b' .planning/phases/08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt</automated>
    <fails_when>non-zero exit -- the Phase 8 snapshot commit appears in this phase's provenance, which means the bundle inherited rather than measured. This is precisely what success criterion 7 forbids.</fails_when>
    <automated>test -z "$(git diff --name-only "$(grep -oE '\b[0-9a-f]{40}\b' .planning/phases/08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt | head -1)"..HEAD -- src include tests scripts CMakeLists.txt)"</automated>
    <fails_when>non-zero exit -- a tracked source, test or script file changed between the commit the gates ran at and HEAD, so the bundle certifies a tree that no longer exists. The command also fails non-zero if no 40-character hash is present in the file at all, which is the same defect one step earlier.</fails_when>
    <automated>for f in build-all-debug.log build-all-release.log build-all-asan.log compiler-debug.log compiler-release.log compiler-asan.log static-analysis.log preflight-versions.log ldd-release.txt PROVENANCE.txt runtime-smoke/capabilities.txt runtime-smoke/root-properties.txt runtime-smoke/window-tree.txt; do test -s ".planning/phases/08.5-v1.0-closeout/evidence/gates/$f" || echo "MISSING: $f"; done</automated>
    <fails_when>prints any `MISSING:` line -- the bundle is incomplete and the gate rows plan 08.5-05 rewrites would cite a file that is not there.</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/gates/PROVENANCE.txt` exists and names the full commit hash, the short hash, the branch and the date
    - It carries one exit-code line per gate: debug, release, asan, static-analysis, runtime-smoke
    - It states the tree state at capture and, if dirty, names the dirty paths
    - It quotes each tree's test total from that tree's log, and the three agree
    - It states in one line, with the command, that no tracked file under `src/`, `include/`, `tests/`, `scripts/` or `CMakeLists.txt` changed between the recorded commit and HEAD, and running that command prints nothing
    - It states whether the ASan run produced sanitizer report files, and the bundle's contents match that statement
    - It names the phase directory the bundle belongs to
    - `grep -cF 'c61cb4b' PROVENANCE.txt` prints `0` -- no reference to the Phase 8 snapshot commit
    - The twelve-file inventory check prints nothing
  </acceptance_criteria>
  <done>The bundle is bound to one commit, and the claim that the gates ran against the shipping tree is checkable by a reader with one git command rather than believable only on assertion.</done>
</task>

</tasks>

## Artifacts this phase produces

Produced by this plan (08.5-04), all under `.planning/phases/08.5-v1.0-closeout/evidence/gates/`:

| Artifact | Blocker / row it satisfies |
|---|---|
| `build-all-debug.log` | Blocker 1 -- Debug configure, build, full ctest |
| `build-all-release.log` | Blocker 1 -- Release, plus the link audit |
| `build-all-asan.log` | Blocker 2 -- AddressSanitizer/UBSan |
| `compiler-debug.log`, `compiler-release.log`, `compiler-asan.log` | The "no new compiler or linker warnings" row |
| `sanitizer-reports/` | Blocker 2, **only if the ASan run produced files**; absence is recorded in `PROVENANCE.txt` instead |
| `static-analysis.log` | Blocker 3 -- cppcheck against the baseline plus clang-tidy |
| `preflight-versions.log` | Environment Preflight rows; Release Evidence "dependency/package list and tool versions" |
| `ldd-release.txt` | Release Evidence "links only to intended libraries" |
| `runtime-smoke/capabilities.txt`, `runtime-smoke/root-properties.txt`, `runtime-smoke/window-tree.txt` | Blocker 4 -- runtime smoke transcript at this phase's commit |
| `PROVENANCE.txt` | Release Evidence "commit hash and branch", and the drift proof success criterion 7 asks for |

Produced by the rest of this gap-closure set: `docs/RELEASE-NOTES.md`'s rewritten
remote-desktop section (08.5-03); `COMPILED_CODE_BEHAVIOR_CHECKLIST.md`'s gate
rows, test-surface row, User Interaction Checklist rows and Release Evidence rows,
plus `.planning/phases/08.5-v1.0-closeout/evidence/README.md` (08.5-05). The
full table is in `08.5-03-PLAN.md`.

## Flagged assumptions (spec-less edge probe)

No `08.5-SPEC.md` exists, so the deterministic edge probe ran over the phase
requirement IDs. All three rows returned `unresolved`; none was auto-resolved
with a backstop and none was dropped. The row bound to this plan:

| Requirement | Category | Probe | Why it stays unresolved |
|---|---|---|---|
| TEST-08 | unclassified | "unclassified -- review manually" | **Assumption, not a verified property.** The probe could not classify the requirement. Manual reading of what is unresolved here: a gate bundle is a snapshot, and nothing in this repository re-validates it when a later commit lands -- Phase 8's own index says so in its opening paragraph. `PROVENANCE.txt` narrows the exposure by proving no source moved between the gate commit and HEAD, but it cannot make the bundle self-refreshing. Refreshing it stays a manual per-release action, and that is recorded rather than resolved. |

Set-wide equality for the gap-closure plan set: 3 probe-surfaced edge items ==
0 authored into `must_haves.truths` + 3 surfaced as flagged assumptions
(RULES-01 and XDIS-05 in `08.5-03-PLAN.md`, TEST-08 here and in
`08.5-05-PLAN.md`, the same item recorded against both plans that touch its
record). No silent drops.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build tree -> committed evidence | Logs are copied out of a gitignored build tree into tracked source; what is copied is unverified unless checked |
| Another phase's bundle -> this phase's bundle | A copied log is indistinguishable from a produced one by inspection alone |
| Validation host display -> committed transcript | `xwininfo -root -tree` records every window title on a workstation that also runs the operator's real desktop |
| Forked test children -> filesystem | Sanitizer reports from WM children are written to files during the ASan gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8.5-GATE-01 | Tampering | `evidence/gates/*` | **high** | mitigate | Every log is the teed output of a command run in this plan. A prohibition forbids copying from the Phase 8 bundle and an acceptance criterion checks with `cmp` that no file is byte-identical to its Phase 8 counterpart. |
| T-8.5-GATE-02 | Repudiation | `PROVENANCE.txt` commit claim | **high** | mitigate | The recorded hash is computed by `git rev-parse`, the Phase 8 snapshot commit is negative-grepped out, and a `git diff --name-only <commit>..HEAD` over the source paths must print nothing. A reader re-runs one command to check the whole claim. |
| T-8.5-GATE-03 | Repudiation | Attributing a red gate to the known intermittent | **high** | mitigate | A failing case must be reproduced in isolation over at least twelve runs and its measured split recorded next to the claim. Both the red and the green log stay in the bundle. |
| T-8-REMOTE | Information Disclosure | `runtime-smoke/window-tree.txt` | medium | mitigate | Captured on a display started for the run carrying only the WM's own windows and one test client; read before commit; credential-pattern screen over the transcript directory as a gate. |
| T-8-XSESSION | Information Disclosure | `~/.xsession-errors` | **high** | mitigate | Never read, copied, quoted or committed. Carried as a prohibition. The operator has declined rotation, so handling is the only control left. |
| T-8-AC | Elevation of Privilege | The smoke display's X server invocation | medium | mitigate | No network-exposing and no access-control-disabling flag. The capture script deliberately suggests no server flags so none can be copied out of it into a deployment recipe. |
| T-8-TMP | Tampering | Sanitizer report sinks | low | accept | Already mitigated in `scripts/gates/build-all.sh`, which sinks both report streams under the CMake binary directory rather than a fixed `/tmp` name. This plan changes nothing about it and inherits the control. |
</threat_model>

<verification>
- `bash scripts/gates/build-all.sh <tree>` exits 0 for debug, release and asan, and each tee'd log ends with its own `build-all OK` line.
- `bash scripts/analysis/run-static-analysis.sh` exits 0 and its log ends with `static-analysis OK`.
- All twelve required files exist and are non-empty under `evidence/gates/`.
- No file in `evidence/gates/` is byte-identical to its Phase 8 counterpart.
- `evidence/gates/ldd-release.txt` contains no `libXtst` line.
- `PROVENANCE.txt` names a 40-character commit hash, and `git diff --name-only <that commit>..HEAD -- src include tests scripts CMakeLists.txt` prints nothing.
- `PROVENANCE.txt` contains no reference to the Phase 8 snapshot commit.
- The credential screen over `evidence/gates/runtime-smoke/` exits 0.
</verification>

<success_criteria>
The capture half of success criterion 7 is closed: all four hard blockers have
logs produced during this phase, the bundle is bound to one commit, and the claim
that the gates ran against the shipping tree is checkable rather than asserted.
The record half -- repointing `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` at this
bundle -- is plan 08.5-05.
</success_criteria>

<output>
Create `.planning/phases/08.5-v1.0-closeout/08.5-04-SUMMARY.md` when done.
Record in it: each gate's exit code and test total quoted from its log; the
`cmp` results against the Phase 8 bundle; whether the ASan run produced
sanitizer report files and what one of them contains if so; any tree that needed
a re-run, the case that failed and its isolated pass/fail split; and the exact
`git diff` command and output backing the no-source-drift claim.
</output>
