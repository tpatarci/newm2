---
phase: 08.5-v1.0-closeout
plan: 8
type: execute
wave: 5
depends_on: ["08.5-07"]
files_modified:
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-debug.log
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-release.log
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-asan.log
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-debug.log
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-release.log
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-asan.log
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/sanitizer-reports/
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/static-analysis.log
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/preflight-versions.log
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/ldd-release.txt
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/DOC-GUARDS.txt
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/capabilities.txt
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/root-properties.txt
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/window-tree.txt
  - .planning/phases/08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt
autonomous: true
gap_closure: true
requirements: [TEST-08, XDIS-05, RULES-01]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the four hard blockers has a log under `.planning/phases/08.5-v1.0-closeout/evidence/gates/` produced by a command run in this plan, not copied or adapted from Phase 8's bundle."
    - "`PROVENANCE.txt` names the commit the gates ran at, and no tracked file under `src/`, `include/`, `tests/`, `scripts/` or `CMakeLists.txt` differs between that commit and the phase's final commit — checkable by a reader with one git command."
    - "Each tree's log is independently readable and closes with its own `build-all OK: <tree>` line, so a tree that goes red leaves a complete record of itself and does not truncate the other two."
    - "If the ASan run produced no sanitizer report files, no `sanitizer-reports/` directory exists in the bundle and `PROVENANCE.txt` records that absence explicitly. An empty or fabricated directory would misrepresent what the gate found."
    - "The criterion-7 bundle and the three measurement directories coexist without merging: the bundle's named artifacts sit at the top level of `evidence/gates/`, `flake-measurement/`, `attribution/` and `fix-measurement/` each keep an opening that says what they are not, and no file is shared between them."
    - "`DOC-GUARDS.txt` records, at the capture commit, that gap 1 of `08.5-VERIFICATION.md` is closed — both of that report's named replacement guards run against `docs/RELEASE-NOTES.md` with their readings — together with the rule-key parity guard for RULES-01, and states that `08.5-VERIFICATION.md` predates the commit that closed it."
    - "The RULES-01 key-parity guard fails loudly rather than silently on an empty side: it compares two extracted key sets and reports a non-empty symmetric difference, and an empty extraction from either side is reported as a guard failure rather than as parity."
    - statement: "`rule-match-title` compares BYTES, not code points or grapheme clusters. The title read is capped at 400 bytes (`src/Client.cpp:875`) and a config value at 256 characters (`src/Config.cpp:129-131`), so the two limits do not line up for multi-byte titles and an exact-mode rule against a longer title cannot match. This limit is recorded in DOC-GUARDS.txt, not closed in this phase."
      verification: backstop
  artifacts:
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-debug.log
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-release.log
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-asan.log
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/static-analysis.log
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/preflight-versions.log
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/ldd-release.txt
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/DOC-GUARDS.txt
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/capabilities.txt
    - .planning/phases/08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt
  key_links:
    - "scripts/gates/build-all.sh -> evidence/gates/build-all-{debug,release,asan}.log (blockers 1 and 2)"
    - "scripts/analysis/run-static-analysis.sh -> evidence/gates/static-analysis.log (blocker 3)"
    - "scripts/capture-display-capabilities.sh under a WM2_EVIDENCE_ROOT override -> evidence/gates/runtime-smoke/ (blocker 4)"
    - "git HEAD at capture time -> evidence/gates/PROVENANCE.txt -> the gate rows the already-written 08.5-05 repoints"
    - "docs/RELEASE-NOTES.md and src/Config.cpp at the capture commit -> evidence/gates/DOC-GUARDS.txt (XDIS-05 gap-1 closure, RULES-01 key parity)"
  prohibitions:
    - "Do not copy, adapt or partially reuse any file from `.planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/gates/`. Inheriting that snapshot is the exact thing success criterion 7 forbids, and a copied log is worse than a missing one because it looks like evidence."
    - "Do not transcribe a figure from `08.5-VERIFICATION.md`, from any summary, or from this plan into a log or into `PROVENANCE.txt`. Every number in this bundle is the output of a command run in this plan."
    - "Do not create `sanitizer-reports/` if the ASan run produced no report files. An empty or fabricated directory misrepresents what the gate found. Record the absence in `PROVENANCE.txt` instead."
    - "Do not re-run a red gate until it happens to pass and commit that. If a tree goes red, both logs stay in the bundle under distinguishable names and the failing case is named with the window manager's stderr from its failure block, which case 198 can now supply."
    - "Do not attribute a failing test to a known intermittent without a log line beside the claim. That is what this whole round exists to have made possible."
    - "Do not run a bare `ctest --test-dir build/asan`. LSan suppressions are not wired into the Catch2 binaries (deferred item 5), so a bare run is red for reasons that are not findings. The asan tree is gated only through `scripts/gates/build-all.sh`."
    - "Do not pass `--regenerate-baseline` to the static-analysis gate. Regenerating the baseline at signoff time absorbs a new finding into it silently, which is the opposite of what a gate is for."
    - "Do not read, copy, quote or commit `~/.xsession-errors` or any part of it."
    - "Do not use a process-name pattern to stop an X server. The pattern matches the killing command's own wrapper and took this host's shell down twice on 2026-08-30. Kill by PID."
    - "Do not start the smoke display with any network-exposing or access-control-disabling flag (threat T-8-AC)."
    - "Do not touch `COMPILED_CODE_BEHAVIOR_CHECKLIST.md`, `evidence/README.md` or `evidence/INTERACTION-CHECKLIST.md`. Those are the already-written 08.5-05's scope and it runs after this plan."
    - "Do not delete, rename or rewrite `evidence/gates/flake-measurement/`, `evidence/gates/attribution/` or `evidence/gates/fix-measurement/`. They are the phase's measurement record and each states what it is not; the bundle lands beside them, not over them."
---

<!-- planner-discipline-allow: libXtst -->
<!-- planner-discipline-allow: evidence/gates/ -->

<objective>
Capture the durable gate record this phase has never produced, at a commit that
carries the flake fix, and record at the same commit the two documentation guards
that prove gap 1 is closed.

Purpose: success criterion 7 asks for the four hard blockers re-run at the final
commit rather than inherited from Phase 8's snapshot, and
`.planning/phases/08.5-v1.0-closeout/evidence/gates/` still holds only
measurement directories. Plan 08.5-04 tried this and halted on the operator's
ruling that a release signoff whose gate needs retries is weaker evidence than no
signoff. Plans 08.5-06 and 08.5-07 diagnosed and fixed the substrate and
re-measured the rate; this plan is that capture, retried against a gate that has
now been shown green over ten consecutive unretried runs.

The second half is a one-file record that costs a few commands and closes a
loose end honestly: `08.5-VERIFICATION.md` reports gap 1 open against
`docs/RELEASE-NOTES.md`, but it was written before plan 08.5-03 landed at
`195ac51`. Both of the guards that report named as the replacements for its
vacuous check are re-run here, at the capture commit, with their readings
recorded — so the closure is visible rather than assumed. The rule-key parity
guard for RULES-01 (D-8.5-01's clean rename, no alias) is captured beside them
for the same reason.

Output: the twelve-artifact bundle Phase 8 established the layout for, a
`PROVENANCE.txt` that binds it to one commit and proves nothing in the source
moved after it, and a `DOC-GUARDS.txt` recording three documentation guards with
their readings.

Sequencing: depends on 08.5-07, because a gate bundle captured before the fix
certifies a tree that is about to change and a bundle captured against a suite
that is red in two of five runs is what the operator already declined once. The
already-written 08.5-05 consumes this bundle last; its own precondition — that
`PROVENANCE.txt` exists and is non-empty — is what has been holding it, and this
plan releases it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/README.md
@.planning/phases/08.5-v1.0-closeout/08.5-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Blockers 1 and 2 — build and full ctest in all three trees, logs captured</name>
  <files>.planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-debug.log, .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-release.log, .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-asan.log, .planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-debug.log, .planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-release.log, .planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-asan.log, .planning/phases/08.5-v1.0-closeout/evidence/gates/sanitizer-reports/</files>
  <read_first>
    - `scripts/gates/build-all.sh:1-45` — the header's two design points. `--no-tests=error` is mandatory and there is exactly one test-runner invocation carrying it; sanitizer reports from forked children are sunk under the build tree rather than a fixed `/tmp` name (threat T-8-TMP). Read this before deciding what a green run means.
    - `scripts/gates/build-all.sh` — the per-tree section where build output is teed to `build/<tree>/build.log` with a warning count printed. Those files become `compiler-*.log` in the bundle.
    - `scripts/gates/build-all.sh` — `clear_sanitizer_reports()` and the report predicate. The script clears `build/asan/sanitizer-reports/` itself before each ASan run and fails the gate if any report file exists afterwards.
    - `.planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/gates/build-all-debug.log` — the last dozen lines only, for the shape of a completed log and the `build-all OK: <tree>` line that closes it. Read for shape; copy nothing.
    - `.planning/phases/08.5-v1.0-closeout/evidence/gates/fix-measurement/README.md` — the ten-run after-table and its rates. This is the evidence that the debug gate is reliably green in one shot, which is the condition the operator held this capture on.
    - `.planning/phases/08.5-v1.0-closeout/08.5-CONTEXT.md` "Deferred items still open" — item 5 records that a bare ctest against the asan tree is red for reasons that are not findings.
  </read_first>
  <precondition>Plan 08.5-07's summary records ten consecutive green `build-all.sh debug` runs with no run omitted and none retried, and `.planning/phases/08.5-v1.0-closeout/evidence/gates/fix-measurement/README.md` carries their per-run table. The operator's binding ruling for this round is "fix the flake, then capture"; a capture taken before that gate is met is the record they already declined once. Additionally `git status --porcelain -- src include tests scripts CMakeLists.txt` prints nothing — a gate run against a dirty source tree certifies a state no commit holds.</precondition>
  <reversibility rating="reversible">New log files under a new directory. Deleting them restores the prior state exactly; nothing consumes them until plan 08.5-05 repoints the checklist at them.</reversibility>
  <action>
Create `.planning/phases/08.5-v1.0-closeout/evidence/gates/` and run the build-and-test gate once per tree, capturing each run's full output.

Run the three trees as **three separate invocations**, not one — `bash scripts/gates/build-all.sh debug`, then `release`, then `asan`, each with `set -o pipefail` and its combined output teed into `evidence/gates/build-all-<tree>.log`. The separation is what makes each tree's log independently readable: a tree that goes red leaves a complete record of itself and does not truncate the two that follow it. The gate script configures, builds and runs the unfiltered suite in each tree.

After each tree completes, copy that tree's `build/<tree>/build.log` — the compiler and linker warning capture the script writes — to `evidence/gates/compiler-<tree>.log`. The Release Evidence section has a row pointing at those files for the no-new-warnings claim, and the already-written plan 08.5-05 repoints it at this bundle.

**Sanitizer reports.** After the asan tree finishes, look at `build/asan/sanitizer-reports/`. If it holds files, copy them into `evidence/gates/sanitizer-reports/` in full and **read one before committing**: Phase 8's six were LeakSanitizer suppression accounting rather than findings, and the reason they were committed in full was so that distinction could be checked rather than believed. If the directory is empty or absent, **create nothing** — record the absence in the summary and leave it for Task 3 to state in `PROVENANCE.txt`. An absence recorded is evidence; an empty directory is a misrepresentation and an absence unmentioned looks like an oversight.

**If a tree goes red.** Do not re-run until it happens to pass and commit that. Name the failing case, its line and its elapsed time, and quote the window manager's stderr from the failure block — plan 08.5-06 hoisted case 198's stderr into its asserting scope precisely so that a red run at this gate can now say why. Then stop and report: after ten consecutive greens, a red run here is a finding about the fix, not noise to be retried away. **Keep the red log** in the bundle under a distinguishable name alongside whatever follows it, with the reason in the summary. A bundle showing only the run that passed is the same class of record as the snapshot this criterion was written to eliminate.
  </action>
  <verify>
    <automated>set -o pipefail && bash scripts/gates/build-all.sh debug 2>&1 | tee .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-debug.log</automated>
    <fails_when>non-zero exit; or the log's last line is not `build-all OK: debug`; or the log contains a line matching `tests failed` or `: FAILED`. A silently-empty run is caught by the gate's own mandatory `--no-tests=error`, which makes a zero-test run exit non-zero rather than green.</fails_when>
    <automated>set -o pipefail && bash scripts/gates/build-all.sh release 2>&1 | tee .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-release.log</automated>
    <fails_when>non-zero exit; or the log's last line is not `build-all OK: release`; or the log's link audit section reports `libXtst`, which is test-only and must not be linked into the shipped binary.</fails_when>
    <automated>set -o pipefail && bash scripts/gates/build-all.sh asan 2>&1 | tee .planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-asan.log</automated>
    <fails_when>non-zero exit; or the log's last line is not `build-all OK: asan`; or the log reports a sanitizer finding rather than the no-findings verdict.</fails_when>
    <automated>for t in debug release asan; do test -s ".planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-$t.log" && test -s ".planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-$t.log" || echo "MISSING: $t"; done</automated>
    <fails_when>prints any `MISSING:` line — a tree ran without leaving both of its logs in the bundle, or a log was created empty.</fails_when>
    <automated>for t in debug release asan; do tail -1 ".planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-$t.log" | grep -q "build-all OK: $t" || echo "UNCLOSED: $t"; done</automated>
    <fails_when>prints any `UNCLOSED:` line — that tree's log does not close with its own success line, so either the tree went red or one invocation's output overwrote another's and the three logs are not independently readable.</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/gates/build-all-{debug,release,asan}.log` all exist, are non-empty, and each closes with its own `build-all OK: <tree>` line
    - `evidence/gates/compiler-{debug,release,asan}.log` all exist and are non-empty
    - The three logs' ctest totals agree with each other and with `ctest --test-dir build/debug -N --no-tests=error | tail -1` run after the gate; any disagreement is investigated, not averaged
    - No file in `evidence/gates/` is byte-identical to its counterpart in `.planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/gates/` — checked with `cmp` and the result recorded
    - `evidence/gates/sanitizer-reports/` exists only if `build/asan/sanitizer-reports/` produced files; if it exists, at least one file was read and its content class (suppression accounting versus finding) is stated in the summary
    - If any tree went red, both logs are in the bundle, the failing case is named with its line and elapsed time, and the window manager's stderr from that failure block is quoted
    - `git status --porcelain -- src include tests scripts CMakeLists.txt` printed nothing before the first gate ran, and the ten-green precondition was checked against `fix-measurement/README.md` rather than assumed
  </acceptance_criteria>
  <done>Blockers 1 and 2 have logs produced by runs in this phase against this phase's tree, each tree's log independently readable and closing with its own success line.</done>
</task>

<task type="auto">
  <name>Task 2: Blocker 3, the environment evidence, and the documentation guards at the capture commit</name>
  <files>.planning/phases/08.5-v1.0-closeout/evidence/gates/static-analysis.log, .planning/phases/08.5-v1.0-closeout/evidence/gates/preflight-versions.log, .planning/phases/08.5-v1.0-closeout/evidence/gates/ldd-release.txt, .planning/phases/08.5-v1.0-closeout/evidence/gates/DOC-GUARDS.txt</files>
  <read_first>
    - `scripts/analysis/run-static-analysis.sh:1-30` — why the two tools are handled differently: cppcheck gets a content-hash baseline at `scripts/analysis/cppcheck-suppressions.xml`, clang-tidy has no baseline concept and gets an explicit fatal-check allowlist in `.clang-tidy`. The gate exits non-zero the moment a finding appears that is not already in the baseline.
    - `.planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/gates/static-analysis.log` — the shape: a cppcheck block, a clang-tidy block, a closing `static-analysis OK`. Read for shape; copy nothing.
    - `.planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/gates/ldd-release.txt:1-4` — the two header comment lines stating what the file is for and the one library that must not appear in it.
    - `scripts/preflight.sh:60-110` — the pkg-config module list and the toolchain version lines that become `preflight-versions.log`.
    - `.planning/phases/08.5-v1.0-closeout/08.5-VERIFICATION.md:8-58` — gap 1 in full: the five artifact issues against `docs/RELEASE-NOTES.md` and, in its `missing[]`, the two replacement guards it names for the vacuous `grep -c 'validation pending'` check. Those two guards are what this task re-runs. Note the report's own date: it predates `195ac51`.
    - `.planning/phases/08.5-v1.0-closeout/08.5-03-SUMMARY.md` `coverage` block — plan 08.5-03's recorded before/after readings for each guard, which is the independent corroboration that the closure is real rather than coincidental.
    - `src/Config.cpp:280-320` — `isRuleMatchKey` and the dispatch below it: the five shipped `rule-match-*` keys, with no `rule-match-name` (D-8.5-01, the clean rename with no alias).
    - `docs/RELEASE-NOTES.md` rules section — the shipped description of the same key set. The parity guard compares these two extractions.
    - `src/Client.cpp:870-880` and `src/Config.cpp:125-135` — the 400-byte title read cap and the 256-character config value cap. These are the two limits the byte-semantics note records.
  </read_first>
  <precondition>`cppcheck` and `clang-tidy` are both on PATH — `command -v cppcheck && command -v clang-tidy` exits 0. Plan 08-01 recorded a host where neither was installed, and the gate degrades rather than failing loudly if a tool is missing, which would produce a green log that measured half of what it claims.</precondition>
  <reversibility rating="reversible">Four new files under a new directory. Nothing is modified; deleting them restores the prior state.</reversibility>
  <action>
**Static analysis.** Run `bash scripts/analysis/run-static-analysis.sh` with its combined output teed to `evidence/gates/static-analysis.log`. Do **not** pass `--regenerate-baseline`. If a new finding appears, stop and report it rather than baselining it — the flake fix touched `src/Manager.cpp`, so a new finding here is about this round's work.

Record in the summary the two figures the log states — the cppcheck finding count against the baseline, and whether any enforced clang-tidy check fired — and the tool versions the log names, because the baseline is keyed on `(id, fileName)` and a different cppcheck version can change what it reports.

**Tool and dependency versions.** Run `bash scripts/preflight.sh` with its combined output teed to `evidence/gates/preflight-versions.log`. This backs both the Environment Preflight rows and the Release Evidence dependency-and-tool-versions row.

**Release link surface.** With the release tree built by Task 1, capture `ldd build/release/wm2-born-again` into `evidence/gates/ldd-release.txt`, preceded by two comment lines in the same form Phase 8 used: the command that produced it, and the statement that test-only libraries must not appear. Then check the file for the test-only X testing library — if it appears outside those leading comment lines, that is a real finding and the phase does not close on it.

**The documentation guards.** Write `evidence/gates/DOC-GUARDS.txt`, capturing three guards run at this commit with their commands and their readings. Every number in it is the output of a command run now; nothing is transcribed from `08.5-VERIFICATION.md`, from `08.5-RESEARCH.md` or from this plan.

1. **Gap-1 guard A (XDIS-05).** The first replacement guard `08.5-VERIFICATION.md`'s `missing[]` names, run against `docs/RELEASE-NOTES.md`, with its count. Expected zero.
2. **Gap-1 guard B (XDIS-05).** The second replacement guard, likewise, with its count. Expected zero.
3. **Rule-key parity (RULES-01).** Extract the shipped `rule-match-*` key set from `src/Config.cpp`'s `isRuleMatchKey`, extract the key set the release notes describe, and report the **symmetric difference** of the two, both directions. The expected result is a difference of exactly one member — the old spelling, which survives only in a migration note saying it is gone (D-8.5-01) — and the guard states that expectation explicitly so a reader can tell an expected asymmetry from a regression.

   **The guard must fail loudly on an empty side.** If either extraction produces no keys — a renamed function, a restructured section, a broken regular expression — the symmetric difference is trivially small or empty and reads as parity when nothing was compared. So the guard reports the size of each extracted set alongside the difference, and states that a zero on either side is a guard failure rather than a pass. That is the same defect class as the vacuous `grep -c 'validation pending'` this phase is still paying for.

Then state, in the file's own words, three things:

- That `08.5-VERIFICATION.md` records gap 1 as open, that it was written before plan 08.5-03 landed at `195ac51`, and that these readings — taken at this bundle's commit — are what closes it. The report is stale on this point rather than wrong when it was written, and saying which is the difference between a correction and a contradiction.
- **The byte-semantics limit, recorded and not closed.** `rule-match-title` compares bytes: the title read is capped at 400 bytes (`src/Client.cpp:875`) and a config value at 256 characters (`src/Config.cpp:129-131`), so the two caps do not line up for multi-byte titles and an exact-mode rule against a longer title can never match, silently. This is narrow and it is recorded here because it is exactly the "a control that looks available and does nothing" class this phase set out to remove. It is **not** fixed in this phase; name it as a v1.1 line.
- That a guard reading is a snapshot bound to this commit and that nothing in this repository re-runs it when a later commit lands.
  </action>
  <verify>
    <automated>set -o pipefail && bash scripts/analysis/run-static-analysis.sh 2>&1 | tee .planning/phases/08.5-v1.0-closeout/evidence/gates/static-analysis.log</automated>
    <fails_when>non-zero exit, or the log does not end with `static-analysis OK`. A new finding outside the baseline exits non-zero and names the file it came from.</fails_when>
    <automated>set -o pipefail && bash scripts/preflight.sh 2>&1 | tee .planning/phases/08.5-v1.0-closeout/evidence/gates/preflight-versions.log</automated>
    <fails_when>non-zero exit, or the resulting log contains no `pkg-config modules` section — preflight ran in a mode that suppressed the version lines, leaving a file that records nothing.</fails_when>
    <automated>ldd build/release/wm2-born-again | tee -a .planning/phases/08.5-v1.0-closeout/evidence/gates/ldd-release.txt</automated>
    <fails_when>non-zero exit — the release binary is absent, meaning Task 1's release tree did not build.</fails_when>
    <automated>! grep -v '^#' .planning/phases/08.5-v1.0-closeout/evidence/gates/ldd-release.txt | grep -q 'libXtst'</automated>
    <fails_when>non-zero exit — the shipped binary links a test-only library. This is a finding, not a bundling problem. The `grep -v '^#'` filter is load-bearing: the file's own two leading comment lines name the excluded library, so an unfiltered gate would fail against a correct file and pass only if the header were deleted.</fails_when>
    <automated>test -s .planning/phases/08.5-v1.0-closeout/evidence/gates/DOC-GUARDS.txt</automated>
    <fails_when>non-zero exit — the guard record is missing or zero-length. This is the state before this task.</fails_when>
    <automated>grep -c '195ac51' .planning/phases/08.5-v1.0-closeout/evidence/gates/DOC-GUARDS.txt</automated>
    <fails_when>prints `0` — the record does not name the commit that closed gap 1, so a reader cannot tell why a verification report calling it open is no longer current.</fails_when>
    <automated>grep -oE 'rule-match-(class|instance|title)' .planning/phases/08.5-v1.0-closeout/evidence/gates/DOC-GUARDS.txt | sort -u | wc -l</automated>
    <fails_when>prints anything other than `3` — the record does not name all three shipped match keys, so its symmetric-difference figure has nothing a reader can check it against. `-o` with `sort -u` counts **distinct keys**, which is the property being gated; a line-counting `grep -c` would fail a correct file that names all three on one line and pass a wrong file that names one key on three lines — wrong in both directions, and the same guard-vacuity class the parity section itself exists to defend against.</fails_when>
    <automated>grep -c 'src/Client.cpp:875' .planning/phases/08.5-v1.0-closeout/evidence/gates/DOC-GUARDS.txt</automated>
    <fails_when>prints `0` — the byte-semantics limit is not recorded with the line that imposes it, so it reads as an opinion rather than as a measured constraint.</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/gates/static-analysis.log` exists, is non-empty, and its last line is `static-analysis OK`
    - The log names both tool versions, and the cppcheck finding count and clang-tidy verdict are quoted in the summary from the log rather than from any prior phase's figure
    - `scripts/analysis/cppcheck-suppressions.xml` is unmodified: `git status --porcelain -- scripts/analysis/cppcheck-suppressions.xml` prints nothing
    - `evidence/gates/preflight-versions.log` exists, is non-empty, and contains a `pkg-config modules` section listing `x11`, `xext`, `xft` and `fontconfig` with versions
    - `evidence/gates/ldd-release.txt` opens with the two comment lines naming the command and the excluded library, and the filtered check finds no test-only library line outside them
    - `evidence/gates/DOC-GUARDS.txt` records three guards, each with the literal command run and the reading it produced, and every reading is the output of a command run in this task
    - Both gap-1 guards read zero, and the file names `195ac51`, states that `08.5-VERIFICATION.md` predates it, and states that these readings are what close gap 1
    - The parity guard reports the size of each extracted key set as well as the symmetric difference, and states that a zero on either side is a guard failure rather than a pass
    - The expected one-member asymmetry — the old spelling surviving only in a migration note — is stated explicitly, so an expected difference cannot be mistaken for a regression
    - All three shipped match keys are named in `DOC-GUARDS.txt`, checked by counting **distinct** keys (`grep -oE ... | sort -u | wc -l` prints `3`) rather than matching lines, so neither a one-line listing nor a repeated single key can satisfy the check
    - The byte-semantics limit is recorded with both caps and both source lines, named as a v1.1 line and explicitly not fixed here
    - The file states that a guard reading is a snapshot bound to this commit and that nothing re-runs it when a later commit lands
    - No file written by this task is byte-identical to its counterpart in the Phase 8 bundle
  </acceptance_criteria>
  <done>Blocker 3 and the environment evidence exist for this phase's tree from fresh runs with the baseline untouched, and gap 1's closure is a recorded reading at the capture commit rather than an assumption.</done>
</task>

<task type="auto">
  <name>Task 3: Blocker 4 and PROVENANCE.txt — a runtime smoke transcript and the drift proof that binds the bundle</name>
  <files>.planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/capabilities.txt, .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/root-properties.txt, .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/window-tree.txt, .planning/phases/08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt</files>
  <read_first>
    - `scripts/capture-display-capabilities.sh:1-60` — the three design points in the header (a missing extension is a result and not a failure; no server flags are suggested or applied; the transcript contains window titles and is committed), the `<display> <label>` argument shape, and the `EVIDENCE_ROOT="${WM2_EVIDENCE_ROOT:-$PHASE_DIR/evidence}"` line. `PHASE_DIR` is hardcoded to the Phase 8 directory, so the override is mandatory or the output lands in the wrong bundle.
    - `.planning/phases/08.5-v1.0-closeout/evidence/x2go-nxagent/SCOPE.md` — the "What was run" block, for the shape of an Xvfb-plus-client invocation that produced a clean transcript on this host.
    - `.planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/gates/PROVENANCE.txt` — all six lines. Full commit hash, branch, date, one exit-code line per gate. That shape is the minimum; this phase's file adds the drift proof, because drift is what success criterion 7 is about.
    - `.planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/README.md:1-20` — the snapshot caveat in Phase 8's own words: nothing re-validates a bundle when later commits land, and refreshing it is a manual per-release action. That caveat is why this task exists.
    - `.planning/phases/08.5-v1.0-closeout/08.5-VERIFICATION.md:59-88` — gap 2's `reason` and `missing[]`, which name `PROVENANCE.txt` and the commit it must record.
    - `.planning/phases/08.5-v1.0-closeout/08.5-CONTEXT.md` "Operational gotchas" and security section — the host accumulates orphaned X servers so a low display number is not free; X servers are stopped by PID; and T-8-REMOTE, why a window tree captured on this workstation is read before it is committed.
  </read_first>
  <precondition>Tasks 1 and 2 have completed and their artifacts are on disk. `PROVENANCE.txt` records exit codes for gates that have already run; written first, it would record predictions. An unused X display number is also available — check `/tmp/.X11-unix/` first and pick a number no socket there claims, because this host accumulates orphaned servers from test runs and the low numbers are not free.</precondition>
  <reversibility rating="reversible">Four new files under the bundle. Nothing consumes them until plan 08.5-05 cites them; deleting them restores the prior state.</reversibility>
  <action>
**Blocker 4 — the runtime smoke transcript.** This is the one blocker whose Phase 8 artifact cannot simply be re-pointed, and the two transcripts this phase already holds were captured at `9869aea` and `f5bbea7`, both before the release-notes correction and both stamping their own commit in their provenance header. Capture a fresh one against the binary Task 1 built.

Start an Xvfb on a display number nothing else claims, with **no** network-exposing and **no** access-control-disabling flag. Start `build/release/wm2-born-again` on it, then one test client with a title chosen for this run, so the window tree carries nothing from the operator's real desktop.

Run the capture script with the evidence root overridden to this phase's gates directory and the label `runtime-smoke`, so the three files land at `evidence/gates/runtime-smoke/`. The script writes its own provenance header naming the commit, the branch and the tree state; that header is what ties this transcript to the same commit as the rest of the bundle.

**Read the window tree before committing it.** This workstation also runs the operator's real desktop and `xwininfo -root -tree` records every window's title. The transcript is taken on a display started for this run and should therefore carry only the window manager's own windows and the one test client — confirm that by reading it, not by assuming it.

Stop the Xvfb **by PID** when the capture is done. Not by process-name pattern: that matches the killing command's own wrapper and took this host's shell down twice on 2026-08-30.

A missing extension in this capture is a result, not a failure — the script is built on that principle, and this display is a plain Xvfb, so the matrix is expected to show the full set.

**`PROVENANCE.txt`.** Write it last, binding the whole bundle to one commit. It carries, at minimum:

- the full commit hash and the short form, and the branch, taken from `git rev-parse` at the moment of writing;
- the date in the same ISO form Phase 8's file used;
- one line per gate with its recorded exit code: debug, release, asan, static-analysis, and the runtime smoke capture;
- the tree state at capture, from `git status --porcelain`, stated as clean or dirty with the dirty paths named;
- the test totals the three trees reported, quoted from the logs rather than from memory;
- an explicit line stating whether `build/asan/sanitizer-reports/` produced files, and if it did not, that no `sanitizer-reports/` directory exists in this bundle and why — absence recorded is evidence; absence unmentioned looks like an oversight;
- a line naming `DOC-GUARDS.txt` and the fact that the documentation guards were run at this same commit, so a reader arriving at the bundle finds the whole capture-commit record from one file.

**The line that makes this criterion checkable.** Add a statement, with the command that produced it, that no tracked file under `src/`, `include/`, `tests/`, `scripts/` or `CMakeLists.txt` differs between the recorded commit and the phase's final commit. This is the difference between "gates were run" and "gates were run against the tree that shipped". Documentation commits landing afterwards — the already-written plan 08.5-05 rewrites the checklist and writes the evidence index — do not invalidate a gate result, and stating exactly that, with the command a reader can re-run, is what stops the next reader from having to guess.

**Name the phase.** The bundle must be identifiable as this phase's without reading a path: `PROVENANCE.txt` states the phase directory it belongs to. The gate rows plan 08.5-05 rewrites cite it by phase-qualified path for the same reason.

**Name what the top level of `evidence/gates/` also contains.** Three measurement directories now sit beside the bundle — `flake-measurement/` (the before rate, plan 08.5-04), `attribution/` (the diagnosis, plan 08.5-06) and `fix-measurement/` (the after rate, plan 08.5-07). Each opens by saying what it is not. State in `PROVENANCE.txt` that they are the phase's measurement record and are **not** gate results, so the two kinds of artifact cannot be conflated by a reader who found the directory before finding the index. Do not merge, move or rename any of them.

Do not carry over any figure or commit reference from the Phase 8 bundle. Every number in this file comes from a log produced in Tasks 1 and 2 or from a git command run now.
  </action>
  <verify>
    <automated>test -s .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/capabilities.txt && test -s .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/root-properties.txt && test -s .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/window-tree.txt</automated>
    <fails_when>non-zero exit — one of the three transcript files is missing or zero-length, which happens when `WM2_EVIDENCE_ROOT` was not exported and the capture landed in the Phase 8 bundle instead.</fails_when>
    <automated>grep -c 'commit:' .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/capabilities.txt</automated>
    <fails_when>prints `0` — the file exists but carries no provenance header, so it cannot be tied to a commit and is not evidence of anything.</fails_when>
    <automated>grep -c '_NET_CLIENT_LIST' .planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/root-properties.txt</automated>
    <fails_when>prints `0` — the window manager was not running, or was running and had not published its root properties, when the capture ran. A transcript of an unmanaged display is not a runtime smoke transcript.</fails_when>
    <automated>! grep -rnE 'API_KEY|ACCESS_KEY|SECRET|TOKEN|PRIVATE KEY|Bearer ' .planning/phases/08.5-v1.0-closeout/evidence/gates/</automated>
    <fails_when>non-zero exit, and the command prints the file and line it matched — a credential pattern reached the bundle. Read the match before doing anything else; do not delete the line and re-run.</fails_when>
    <automated>test -s .planning/phases/08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt</automated>
    <fails_when>non-zero exit — the file is missing or zero-length. This is the state before this task, and it is also the precondition the already-written plan 08.5-05 has been blocked on.</fails_when>
    <automated>! grep -qF 'c61cb4b' .planning/phases/08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt</automated>
    <fails_when>non-zero exit — the Phase 8 snapshot commit appears in this phase's provenance, which means the bundle inherited rather than measured. This is precisely what success criterion 7 forbids.</fails_when>
    <automated>C="$(grep -oE '\b[0-9a-f]{40}\b' .planning/phases/08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt | head -1)" && test -n "$C" && git rev-parse --verify --quiet "$C^{commit}" >/dev/null && test -z "$(git diff --name-only "$C"..HEAD -- src include tests scripts CMakeLists.txt)"</automated>
    <fails_when>non-zero exit — a tracked source, test or script file changed between the commit the gates ran at and HEAD, so the bundle certifies a tree that no longer exists. The two guards ahead of the diff catch the same defect one step earlier, each on its own exit code: `test -n "$C"` exits non-zero when the file is absent or holds no 40-character hash, and `git rev-parse --verify` exits non-zero when the hash is 40 hex characters but names no commit in this repository. Both guards are load-bearing rather than decorative — without them the empty substitution degenerates to `..HEAD`, git resolves that as `HEAD..HEAD`, and `test -z` reports success on the empty output, so the whole check passes against a file that does not exist.</fails_when>
    <automated>for f in build-all-debug.log build-all-release.log build-all-asan.log compiler-debug.log compiler-release.log compiler-asan.log static-analysis.log preflight-versions.log ldd-release.txt DOC-GUARDS.txt PROVENANCE.txt runtime-smoke/capabilities.txt runtime-smoke/root-properties.txt runtime-smoke/window-tree.txt; do test -s ".planning/phases/08.5-v1.0-closeout/evidence/gates/$f" || echo "MISSING: $f"; done</automated>
    <fails_when>prints any `MISSING:` line — the bundle is incomplete and the gate rows plan 08.5-05 rewrites would cite a file that is not there.</fails_when>
    <automated>for d in flake-measurement attribution fix-measurement; do test -s ".planning/phases/08.5-v1.0-closeout/evidence/gates/$d/README.md" || echo "LOST: $d"; done</automated>
    <fails_when>prints any `LOST:` line — a measurement directory was merged into the bundle, renamed or deleted. Each is the record of a distinct measurement and each states what it is not.</fails_when>
  </verify>
  <acceptance_criteria>
    - All three runtime-smoke files exist and are non-empty; `capabilities.txt` carries the script's provenance header with a `commit:` line, and that commit is the one `PROVENANCE.txt` records
    - `root-properties.txt` contains a `_NET_CLIENT_LIST` entry with the test client in it and the full `_NET_SUPPORTED` list
    - `window-tree.txt` shows the test client reparented inside a frame with the tab and button children present, was read before commit, and contains no window title, host name or user name from the operator's real desktop (T-8-REMOTE)
    - The smoke display was started with no network-exposing and no access-control-disabling flag, and was stopped by PID
    - `PROVENANCE.txt` names the full commit hash, the short hash, the branch and the date, and carries one exit-code line per gate: debug, release, asan, static-analysis, runtime-smoke
    - It states the tree state at capture and, if dirty, names the dirty paths; it quotes each tree's test total from that tree's log and the three agree
    - It states in one line, with the command, that no tracked file under `src/`, `include/`, `tests/`, `scripts/` or `CMakeLists.txt` changed between the recorded commit and HEAD, and running that command prints nothing
    - It states whether the ASan run produced sanitizer report files, and the bundle's contents match that statement exactly
    - It names the phase directory the bundle belongs to, names `DOC-GUARDS.txt`, and names the three measurement directories as measurement record rather than gate results
    - `grep -cF 'c61cb4b' PROVENANCE.txt` prints `0` — no reference to the Phase 8 snapshot commit
    - The fourteen-file inventory check prints nothing, and all three measurement directories still carry their `README.md`
    - The credential screen over the whole of `evidence/gates/` exits 0, or every match is quoted in the summary with the reason it is benign
  </acceptance_criteria>
  <done>The bundle is bound to one commit, and the claim that the gates ran against the shipping tree is checkable by a reader with one git command rather than believable only on assertion.</done>
</task>

</tasks>

## Artifacts this phase produces

Produced by this plan (08.5-08), all under `.planning/phases/08.5-v1.0-closeout/evidence/gates/`. Every path below is created here and does not exist at HEAD:

| Artifact | Blocker / row it satisfies |
|---|---|
| `build-all-debug.log` | Blocker 1 — Debug configure, build, full ctest |
| `build-all-release.log` | Blocker 1 — Release, plus the link audit |
| `build-all-asan.log` | Blocker 2 — AddressSanitizer/UBSan |
| `compiler-debug.log`, `compiler-release.log`, `compiler-asan.log` | The "no new compiler or linker warnings" row |
| `sanitizer-reports/` | Blocker 2, **only if the ASan run produced files**; absence is recorded in `PROVENANCE.txt` instead |
| `static-analysis.log` | Blocker 3 — cppcheck against the baseline plus clang-tidy |
| `preflight-versions.log` | Environment Preflight rows; Release Evidence dependency-and-tool-versions row |
| `ldd-release.txt` | Release Evidence "links only to intended libraries" |
| `DOC-GUARDS.txt` | XDIS-05 gap-1 closure at the capture commit; RULES-01 rule-key parity (D-8.5-01); the recorded byte-semantics limit |
| `runtime-smoke/capabilities.txt`, `runtime-smoke/root-properties.txt`, `runtime-smoke/window-tree.txt` | Blocker 4 — runtime smoke transcript at this phase's commit |
| `PROVENANCE.txt` | Release Evidence "commit hash and branch", and the drift proof success criterion 7 asks for |

Created earlier in this round and unchanged here: the bounded `timestamp()` wait, the `[wm_timestamp]` cases and `scripts/gates/flake-run.sh` (08.5-06, 08.5-07); the three measurement directories `flake-measurement/`, `attribution/` and `fix-measurement/`. Consumed last, by the already-written 08.5-05: `COMPILED_CODE_BEHAVIOR_CHECKLIST.md`, `evidence/README.md` and `evidence/INTERACTION-CHECKLIST.md`. **Carry-forward for that plan:** its `gates/` index table needs a row for `DOC-GUARDS.txt`, which did not exist when it was written.

## Accepted deviations (recorded so they are not re-litigated at verify time)

| Deviation | Why it stands |
|---|---|
| **Nyquist check 8b — feedback latency far exceeds the 30 s threshold.** Task 1 runs three full trees, each a configure, a build and an unfiltered suite. | Inherent to the artifact. Success criterion 7 asks for the four hard blockers **re-run at the final commit**; a faster proxy would be a different measurement and is what the inherited Phase 8 snapshot already was. `08.5-VALIDATION.md` records the run counts as operator-selected on 2026-08-30, and `08.5-CONTEXT.md` deferred item 5 forbids the one shortcut available (a bare ctest against the asan tree). The plan checker raised this and marked it as requiring no change. Not a defect. |

<assumption_delta_decision>
The deterministic assumption-delta probe fired on this phase with one
`pluralization` signal: the term `fallback`, from the ROADMAP success criterion
"`_NET_WM_NAME` preferred, `WM_NAME` as fallback".

- **The noun that is now primary:** *the window title*, as an identity independent of the property that carries it. `Client::getWindowTitle()` (`src/Client.cpp:916-921`) reads `Atoms::net_wmName` with type `Atoms::utf8_string` and falls back to `XA_WM_NAME` on an **empty** result rather than on status.
- **Decision: `promote`, already taken and shipped.** The generalized representation was promoted to primary and the older specific one demoted to a fallback of one variant — the correct move under the checkpoint's own guidance, and not the add-alongside that would have left the legacy property still required. It was decided as D-8.5-02 and is verified as truth 1 of `08.5-VERIFICATION.md`. **No change is made in this round**; the decision is recorded here because the probe surfaced it and a probe that fires without a recorded answer is the debt this checkpoint exists to prevent.
- **The companion invariant test already exists** in both directions: case #127 ("a window that advertises its title only through `_NET_WM_NAME`") and case #204 ("a `WM_NAME`-only client is unaffected by the EWMH title read"), the second of which reddens if a future phase reintroduces the singular assumption from either side.
- **Accepted residue, named:** the two reads are capped differently in bytes and characters (`src/Client.cpp:875`, `src/Config.cpp:129-131`), which is recorded in `DOC-GUARDS.txt` as a v1.1 line rather than closed here.

Advisory checkpoint; it informs and records and does not block the phase.
</assumption_delta_decision>

## Flagged assumptions (spec-less edge probe)

No `08.5-SPEC.md` exists, so the deterministic edge probe ran over the phase
requirement IDs and returned ten applicable items, all `unresolved`. Every one is
resolved into a `must_haves.truths` entry in one of the three plans of this round
— none is dismissed and none is dropped. Five are resolved here:

| Requirement | Category | Probe | Resolution in this plan |
|---|---|---|---|
| RULES-01 | empty | "What is the result for empty, single-element, or null input?" | Resolved, verification: explicit. The rule-key parity guard reports the size of each extracted key set alongside the symmetric difference, and a zero on either side is stated to be a guard failure rather than parity — the vacuous-guard defect this phase already paid for once. |
| RULES-01 | encoding | "Whose definition of length/equality applies — bytes, code points, grapheme clusters, or normalized form?" | Resolved, **verification: backstop**. `rule-match-title` compares bytes; the 400-byte title read and the 256-character config cap do not line up for multi-byte titles. Recorded in `DOC-GUARDS.txt` with both source lines and named as a v1.1 line. No test pins this behaviour in this phase, so a verifier that cannot confirm it with explicit evidence abstains rather than passing it silently. |
| XDIS-05 | adjacency | "When two things are exactly equal or just touch, do they merge, collide, or separate?" | Resolved, verification: explicit. The bundle and the three measurement directories touch at the top level of `evidence/gates/` and must not merge: the bundle's named artifacts sit at the top level, each measurement directory keeps a README opening that says what it is not, and no file is shared. Gated by the inventory check and the `LOST:` check. |
| XDIS-05 | empty | "What is the result for empty, single-element, or null input?" | Resolved, verification: explicit. An ASan run that produced no sanitizer report files creates no `sanitizer-reports/` directory; the absence is recorded explicitly in `PROVENANCE.txt` and the bundle's contents must match that statement. |
| XDIS-05 | ordering | "When elements compare equal, is output order specified and stable?" | Resolved, verification: explicit. The three trees are run as three separate invocations and each log closes with its own `build-all OK: <tree>` line, so each is independently readable regardless of the order it was produced in and a red tree cannot truncate the others. Gated by the `UNCLOSED:` check in Task 1. |

Set-wide equality for this round: **10 probe-surfaced edge items == 10 authored
into `must_haves.truths` (2 in `08.5-06-PLAN.md`, 3 in `08.5-07-PLAN.md`, 5 here)
+ 0 surfaced as flagged assumptions.** Exactly one carries
`verification: backstop`. No silent drops.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build tree -> committed evidence | Logs are copied out of a gitignored build tree into tracked source; what is copied is unverified unless checked |
| Another phase's bundle -> this phase's bundle | A copied log is indistinguishable from a produced one by inspection alone |
| Measurement directories -> the gate bundle | Three directories that are explicitly not gate results now sit beside artifacts that are |
| Validation host display -> committed transcript | `xwininfo -root -tree` records every window title on a workstation that also runs the operator's real desktop |
| Forked test children -> filesystem | Sanitizer reports from window-manager children are written to files during the ASan gate |
| A stale verification report -> a reader | A report that records gap 1 as open is still on disk after the commit that closed it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8.5-GATE-01 | Tampering | `evidence/gates/*` | **high** | mitigate | Every log is the teed output of a command run in this plan. A prohibition forbids copying from the Phase 8 bundle and an acceptance criterion checks with `cmp` that no file is byte-identical to its Phase 8 counterpart. |
| T-8.5-GATE-02 | Repudiation | `PROVENANCE.txt`'s commit claim | **high** | mitigate | The recorded hash is computed by `git rev-parse`, the Phase 8 snapshot commit is negative-grepped out, and a guarded `git diff --name-only <commit>..HEAD` over the source paths must print nothing. A reader re-runs one command to check the whole claim. |
| T-8.5-GATE-03 | Repudiation | Attributing a red gate to a known intermittent | **high** | mitigate | A red run here follows ten consecutive greens, so it is a finding about the fix. The failing case is named with its line and elapsed time and the window manager's stderr from its failure block is quoted — which case 198 can now supply because plan 08.5-06 hoisted it. Both logs stay in the bundle. |
| T-8.5-GATE-04 | Spoofing | The measurement directories read as gate results | medium | mitigate | Each keeps a README opening that states what it is not; `PROVENANCE.txt` names all three as measurement record rather than gate results; and a check fails if any of their READMEs disappears. |
| T-8.5-DOC-01 | Repudiation | Gap 1's closure | **high** | mitigate | Both replacement guards are re-run at the capture commit with their readings recorded in `DOC-GUARDS.txt`, alongside the commit that closed the gap and the statement that `08.5-VERIFICATION.md` predates it. Nothing is transcribed from the report or from research. |
| T-8.5-DOC-02 | Spoofing | A parity guard that compares nothing | **high** | mitigate | The guard reports the size of each extracted key set as well as the symmetric difference, and states that a zero on either side is a guard failure. This is the vacuous-guard class that let a blocker-grade gap survive a full verification pass in this very phase. |
| T-8-REMOTE | Information Disclosure | `runtime-smoke/window-tree.txt` | medium | mitigate | Captured on a display started for the run carrying only the window manager's own windows and one test client; read before commit; credential-pattern screen over the whole bundle as a gate. |
| T-8-XSESSION | Information Disclosure | `~/.xsession-errors` | **high** | mitigate | Never read, copied, quoted or committed. Carried as a prohibition. The operator has declined rotation, so handling is the only control left. |
| T-8-AC | Elevation of Privilege | The smoke display's X server invocation | medium | mitigate | No network-exposing and no access-control-disabling flag. The capture script deliberately suggests no server flags so none can be copied out of it into a deployment recipe. |
| T-8-KILL | Denial of Service | Stopping the smoke display | medium | mitigate | Stopped by PID. A process-name pattern matches the killing command's own wrapper and took this host's shell down twice on 2026-08-30. |
| T-8-TMP | Tampering | Sanitizer report sinks | low | accept | Already mitigated in `scripts/gates/build-all.sh`, which sinks both report streams under the CMake binary directory rather than a fixed `/tmp` name. This plan changes nothing about it and inherits the control. |
| T-8.5-SC | Tampering | Dependency surface | low | accept | No package-manager install occurs in this phase; `08.5-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable. The standing link audit is `ldd-release.txt`, captured here, and a new dependency at the signoff commit would invalidate it. |
</threat_model>

<verification>
- `bash scripts/gates/build-all.sh <tree>` exits 0 for debug, release and asan, and each tee'd log closes with its own `build-all OK: <tree>` line.
- `bash scripts/analysis/run-static-analysis.sh` exits 0, its log ends with `static-analysis OK`, and the cppcheck baseline is unmodified.
- All fourteen required files exist and are non-empty under `evidence/gates/`, and no file is byte-identical to its Phase 8 counterpart.
- `evidence/gates/ldd-release.txt` contains no test-only library line outside its own leading comment lines.
- `DOC-GUARDS.txt` records three guards with their commands and readings, names `195ac51`, reports both extracted key-set sizes, names all three shipped match keys as three distinct keys, and records the byte-semantics limit with both source lines.
- `PROVENANCE.txt` names a 40-character commit hash, and the guarded `git diff --name-only <that commit>..HEAD -- src include tests scripts CMakeLists.txt` prints nothing.
- `PROVENANCE.txt` contains no reference to the Phase 8 snapshot commit and names all three measurement directories as measurement record.
- All three measurement directories still carry their `README.md`.
- The credential screen over `evidence/gates/` exits 0.
</verification>

<success_criteria>
The capture half of success criterion 7 is closed: all four hard blockers have
logs produced during this phase against a gate that has been shown reliably green
in one shot, the bundle is bound to one commit, and the claim that the gates ran
against the shipping tree is checkable rather than asserted. Gap 1's closure is a
recorded reading at that same commit rather than an inference from a stale
report. The record half — repointing `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` at
this bundle and writing `evidence/README.md` — is the already-written plan
08.5-05, whose precondition this plan releases.
</success_criteria>

<output>
Create `.planning/phases/08.5-v1.0-closeout/08.5-08-SUMMARY.md` when done.
Record in it: each gate's exit code and test total quoted from its own log; the
`cmp` results against the Phase 8 bundle; whether the ASan run produced
sanitizer report files and what one of them contains if so; the cppcheck finding
count, the clang-tidy verdict and both tool versions quoted from the log; the
three documentation guards with their literal commands and readings, including
both extracted key-set sizes; and the exact `git diff` command and output backing
the no-source-drift claim.

Carry forward for plan 08.5-05, which runs last: its `gates/` index table needs a
row for `DOC-GUARDS.txt`, a file that did not exist when that plan was written;
and its gate rows should cite `build-all-<tree>.log` figures rather than
`PROVENANCE.txt`'s summary of them, as its own Task 1 already requires.
</output>
