---
phase: 08.5-v1.0-closeout
verified: 2026-08-30T16:05:00Z
status: gaps_found
score: 5/7 must-haves verified
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "Both Phase 8 deviations are retired on evidence or restated with a reason that is true — neither survives as written"
    status: partial
    reason: >-
      Retired/restated correctly in COMPILED_CODE_BEHAVIOR_CHECKLIST.md, but both
      deviations survive VERBATIM AS WRITTEN in the shipped user-facing document
      docs/RELEASE-NOTES.md, carrying four statements this phase's own committed
      evidence disproves. Plan 08.5-02 Task 4 explicitly required updating that
      table ("Update docs/RELEASE-NOTES.md's remote-target table to the four
      measured statuses"); the file was edited in this phase for other sections
      and this one was left untouched. The single acceptance criterion guarding
      it (`grep -c 'validation pending'` == 0) is vacuous — that string never
      appeared in the file.
    artifacts:
      - path: "docs/RELEASE-NOTES.md"
        issue: >-
          L314 says TightVNC "Not tested. Deliberately, with a recorded reason."
          — this phase committed evidence/tightvnc/{capabilities,root-properties,
          window-tree,wm-banner}.txt and the checklist records D-8-TIGHTVNC as
          RETIRED on evidence.
      - path: "docs/RELEASE-NOTES.md"
        issue: >-
          L322-323 "`x2goserver` was never installed on the validation host" —
          independently disproved: /var/log/dpkg.log records
          `install x2goserver-common:all 4.1.0.3-5` at 2026-08-29 17:43:22, and
          `dpkg -l` shows `ii x2goserver 4.1.0.3-5`. The checklist (L603) and the
          Phase 8 evidence README (L95) were both corrected; the release notes
          were not.
      - path: "docs/RELEASE-NOTES.md"
        issue: >-
          L332-333 "Nothing in the TigerVNC or XRDP results transfers to it."
          Contradicted by evidence/x2go-nxagent/capabilities.txt — SHAPE, RANDR
          and RENDER all present, the same surface.
      - path: "docs/RELEASE-NOTES.md"
        issue: >-
          L344-348 the TightVNC rationale still asserts the extension sets
          "substantially overlap". evidence/tightvnc/capabilities.txt measures
          7 extensions, SHAPE present, RANDR ABSENT, RENDER ABSENT — the phase's
          own headline finding, disproving exactly this sentence.
      - path: "docs/RELEASE-NOTES.md"
        issue: >-
          L317-319 tells the user "Two of the four stated targets are exercised"
          and that the four-target requirement "is held **unmet** rather than
          ticked", while .planning/REQUIREMENTS.md L56/L180 now marks XDIS-05
          Complete, all four measured. The ledger and the shipping document
          contradict each other on the phase's own headline requirement.
    missing:
      - "Rewrite docs/RELEASE-NOTES.md:304-359 from the checklist's current D-8-TIGHTVNC / D-8-X2GO text: TightVNC = validated by capability capture (7 extensions, no RANDR, no RENDER, nothing judged by eye); X2Go = partially exercised (nxagent headless capture; NX compression proxy untested)."
      - "Delete or rewrite the two narrative subsections that repeat the retracted claims."
      - "Correct the 'Two of the four stated targets are exercised / held unmet' paragraph to the four-of-four count now recorded in REQUIREMENTS.md."
      - "Replace the vacuous `grep -c 'validation pending'` guard with real ones: `grep -c 'never installed' docs/RELEASE-NOTES.md` == 0 and `grep -c 'Not tested' docs/RELEASE-NOTES.md` == 0."
  - truth: "The four hard blockers are re-run at the final commit, not inherited from Phase 8's snapshot"
    status: failed
    reason: >-
      No gate evidence exists for this phase at any commit. Plan 08.5-02 Task 4
      required `evidence/gates/` carrying the build, ctest, sanitizer and
      static-analysis logs with a PROVENANCE.txt naming the final commit; that
      directory was never created. Every gate row in
      COMPILED_CODE_BEHAVIOR_CHECKLIST.md still cites Phase 8's
      `evidence/gates/build-all-{debug,release,asan}.log` at `c61cb4b` reporting
      295/295 — which is precisely the inherited snapshot this success criterion
      names and forbids. The final commit `ec84a44` touched five files, none of
      them the checklist and none of them an evidence/gates artifact. The gates
      themselves do pass today (I ran `scripts/analysis/run-static-analysis.sh`
      → exit 0; the session's regression gate reported 313/313), so this is a
      missing durable record, not a red gate — but the record is what the
      criterion asks for and what a release signoff is checkable against.
    artifacts:
      - path: ".planning/phases/08.5-v1.0-closeout/evidence/gates/"
        issue: "Does not exist. No build-all logs, no sanitizer reports, no static-analysis log, no PROVENANCE.txt for the final commit."
      - path: "COMPILED_CODE_BEHAVIOR_CHECKLIST.md"
        issue: "Lines 133-231 and 637-650: all gate rows cite Phase 8's evidence/gates at c61cb4b, 295/295. `git log 3bbacf9^..ec84a44 -- COMPILED_CODE_BEHAVIOR_CHECKLIST.md` shows the gate rows were never touched by this phase."
      - path: "COMPILED_CODE_BEHAVIOR_CHECKLIST.md"
        issue: "Lines 159-201, the test-surface row that says in bold 'RECOMPUTE, DO NOT TRANSCRIBE' and 'This line rotted once already', states 293 Catch2 cases / 292 registered / Total Tests: 295. Recomputed at HEAD with the row's own commands: 311 cases, Total Tests: 313. The per-file table is stale for the three files this phase touched."
      - path: ".planning/phases/08.5-v1.0-closeout/evidence/README.md"
        issue: "Does not exist. Plan 08.5-02 Task 4 required it (the ~/.xsession-errors warning, T-8-REMOTE, the layout, and a 'what this bundle does not establish' section)."
    missing:
      - "Run `bash scripts/gates/build-all.sh` in all three trees and `bash scripts/analysis/run-static-analysis.sh` at the final commit, on a clean tree, and commit the logs under .planning/phases/08.5-v1.0-closeout/evidence/gates/ with a PROVENANCE.txt naming that commit."
      - "Recompute the COMPILED_CODE_BEHAVIOR_CHECKLIST.md test-surface row and per-file table with the row's own three commands; replace every 295/295 gate claim with the current figure and repoint the log paths at this phase's bundle."
      - "Write .planning/phases/08.5-v1.0-closeout/evidence/README.md following the Phase 8 layout."
      - "Tick the 'Manual interaction checklist results' row in Release Evidence Required (the table now exists with a tester name and a date) and update the User Interaction Checklist rows from the filled table, recounting the totals as the plan required."
---

# Phase 8.5: v1.0 Closeout Verification Report

**Phase Goal:** v1.0 ships with zero requirements silently unmet — window rules match by title under keys named for what they compare, all four remote-desktop targets have committed transcripts, and the interaction record is a per-item table rather than a verdict.
**Verified:** 2026-08-30T16:05:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

The phase's *engineering* half is delivered and behaviourally proven. `rule-match-title`
works against the real compiled binary, the EWMH-first title read is correct in
both directions, the key rename shipped without an alias, and all four
remote-desktop targets have committed transcripts. The interaction checklist
exists as a per-item table with a tester name and a date.

The phase's *record-keeping* half — which is what two of the seven success
criteria are entirely about, and what the phase goal's opening clause ("zero
requirements silently unmet") depends on — is not delivered. The shipped
`docs/RELEASE-NOTES.md` still tells a user that two of the four targets are
untested and that the four-target requirement is "held unmet", while
`.planning/REQUIREMENTS.md` marks XDIS-05 Complete on the strength of transcripts
committed in this very phase. And no gate evidence was captured at the final
commit; the release signoff document still points at Phase 8's snapshot.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A rule can match a window by its title, `_NET_WM_NAME` preferred, `WM_NAME` as fallback | ✓ VERIFIED | `Client::getWindowTitle()` (`src/Client.cpp:916-921`) reads `Atoms::net_wmName` with type `Atoms::utf8_string`, falls back to `XA_WM_NAME` on **empty** rather than on status. `getProperty()` gained a type parameter defaulted to `XA_STRING` (`include/Client.h:234`), so the silent UTF8_STRING/XA_STRING type mismatch is closed. Behavioural: ran test #127 "A title rule matches a window that advertises its title only through `_NET_WM_NAME`" → Passed; #133 "A title rule places a window whose title matches" → Passed; #189 "A title advertised only through `_NET_WM_NAME` reaches the sideways tab" → Passed; #204 "A WM_NAME-only client is unaffected by the EWMH title read" → Passed (the fallback direction). |
| 2 | The three match keys are named for what they compare; `rule-match-name` gone, not aliased | ✓ VERIFIED | `src/Config.cpp:287-288` — `isRuleMatchKey` contains `rule-match-class`, `rule-match-instance`, `rule-match-title`, `rule-match-type`, `rule-match-mode`; no `rule-match-name`. Repo-wide grep: `rule-match-name` survives only in the negative test (`tests/test_rules.cpp:846`), a migration note (`docs/RELEASE-NOTES.md:217`) and two explanatory comments. Behavioural: #269 "rule-match-name is no longer a rule key" → Passed; #265 "rule-match-instance matches the instance name only" → Passed; #270 "The class criterion is tested against both the instance and the class name" → Passed; #278 "rule-match-title parses and opens a rule group" → Passed. |
| 3 | The map-time fold semantic is stated in the requirement, the release notes and the code | ✓ VERIFIED | Requirement: `.planning/REQUIREMENTS.md:85` ("rules fold once, at manage time … D-8.5-03"). Release notes: `docs/RELEASE-NOTES.md:199-208` ("**A title rule looks at the title the window has when it opens** … renaming a document will not make its window jump"). Code: the comment block above `Client::applyWindowRules()` (`src/Client.cpp:1090-1105`) and `include/Rules.h:54-58`. Behavioural: #131 "A title changed after mapping does not re-apply the rule" → Passed — the pin that reddens if a re-fold is added. |
| 4 | All four PROJECT.md remote-desktop targets have a committed capability transcript at a known commit | ✓ VERIFIED | PROJECT.md:59 names TigerVNC, TightVNC, XRDP, X2Go. TigerVNC @ `dba2f30`, XRDP @ `26344b4` (Phase 8 bundle); TightVNC @ `9869aea`, x2go-nxagent @ `f5bbea7` (this phase). Each carries the script's provenance header. The TightVNC transcript substantiates the phase's headline finding independently: 7 extensions, SHAPE present, RANDR ABSENT, RENDER ABSENT. Per-target `SCOPE.md` files state what each does not establish. |
| 5 | The User Interaction Checklist exists as a per-item pass/fail table with a tester name and a date | ✓ VERIFIED | `evidence/INTERACTION-CHECKLIST.md` — tester `tpatarci (operator)`, date `2026-08-30`, target TigerVNC `:11`, commit `0244c06`. 31 numbered rows, each with a result drawn from four kept-apart values (PASS / PASS (verdict) / DEFERRED / n/a) and a note. Coverage summary: 9 / 7 / 11 / 4 = 31, arithmetic checks out. |
| 6 | Both Phase 8 deviations are retired on evidence or restated with a reason that is true — neither survives as written | ✗ FAILED | Half done. `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:664-758` retires D-8-TIGHTVNC on evidence and restates D-8-X2GO with a narrower, true reason, both with owner and follow-up — that part is exemplary. But `docs/RELEASE-NOTES.md:315-359` carries **both deviations verbatim as Phase 8 wrote them**, including the "substantially overlaps" rationale this phase's own transcript disproves and the "`x2goserver` was never installed" claim I disproved independently from `/var/log/dpkg.log`. In the document a user actually reads, both survive as written. See Gaps. |
| 7 | The four hard blockers are re-run at the final commit, not inherited from Phase 8's snapshot | ✗ FAILED | `.planning/phases/08.5-v1.0-closeout/evidence/gates/` does not exist. Every gate row in `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` (L133-231, L637-650) still cites Phase 8's `evidence/gates/*` at `c61cb4b` with 295/295 — the inherited snapshot the criterion names. The test-surface row that says "RECOMPUTE, DO NOT TRANSCRIBE" still reads 293/292/295; recomputed at HEAD: **311 cases, Total Tests: 313**. `git show --stat ec84a44` confirms the final commit touched five files, none of them the checklist or an evidence artifact. See Gaps. |

**Score:** 5/7 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/Client.cpp` | EWMH-first title read, title supplied to the fold | ✓ VERIFIED | `getWindowTitle()` at 916; `facts.title = m_name` at 1114; `eventProperty()` routes both `XA_WM_NAME` (2084-2092) and `Atoms::net_wmName` (2109-2110) through it |
| `include/Client.h` | typed `getProperty`, `getWindowTitle` declared | ✓ VERIFIED | L234 `getProperty(Atom, Atom type = XA_STRING)`; L238 `getWindowTitle()` |
| `include/Rules.h` / `src/Rules.cpp` | title criterion joined to the AND fold and the no-criteria guard | ✓ VERIFIED | `hasMatchTitle` present in the criterion-free guard (`src/Rules.cpp:39`) as well as the match branch (61-63) — the failure mode that no positive test can see |
| `src/Config.cpp` | renamed + new keys inside `isRuleMatchKey`, grouping comment updated | ✓ VERIFIED | L287-288 key set, L304-318 dispatch, L259-269 worked example rewritten to the new spelling |
| `tests/test_wm_rules.cpp` | process-level proof against the real binary | ✓ VERIFIED | 13 registered `[wm_rules]` cases (was 8); I ran #126-133 → 8/8 Passed |
| `docs/RELEASE-NOTES.md` (rules section) | shipped key set described exactly, both directions | ✓ VERIFIED | Both-directions audit run independently: every `rule-*` key in `src/Config.cpp` appears in the notes and vice versa; the sole asymmetry is `rule-match-name`, present only in the migration note that says it is gone |
| `docs/RELEASE-NOTES.md` (remote-target section) | four measured statuses, nested-vs-real distinction visible | ✗ STUB | Untouched by this phase. Still carries the Phase 8 text with four disproved claims |
| `evidence/tightvnc/` | 4 files with provenance | ✓ VERIFIED | capabilities, root-properties, window-tree, wm-banner, plus SCOPE.md |
| `evidence/x2go-nxagent/` | 4 files with provenance | ✓ VERIFIED | Same shape, plus SCOPE.md stating the nested-local vs network-with-proxy limit |
| `evidence/INTERACTION-CHECKLIST.md` | per-item table, tester, date | ✓ VERIFIED | 31 rows, tester and date present |
| `evidence/README.md` | bundle index required by plan Task 4 | ✗ MISSING | Not created |
| `evidence/gates/` | build/ctest/sanitizer/static-analysis logs + PROVENANCE.txt at final commit | ✗ MISSING | Not created |
| `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` | deviations retired/restated; gate rows and interaction rows refreshed | ⚠️ PARTIAL | Deviations section done and done well. Gate rows, test-surface row, User Interaction Checklist rows and the Release-Evidence interaction row all untouched |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `_NET_WM_NAME` (UTF8_STRING) | `m_name` | `Client::getWindowTitle()` | ✓ WIRED | `src/Client.cpp:109` at manage time, 2092/2110 on property change |
| `m_name` | tab label | `setLabel()` / `rename()` | ✓ WIRED | Test #189 proves the EWMH title reaches the painted tab |
| `m_name` | rule fold | `facts.title` | ✓ WIRED | `src/Client.cpp:1114`; ordering guaranteed by the title read at L109 preceding `applyWindowRules()` |
| `rule-match-title` | `WindowRule::matchTitle` → `ruleMatches()` | `src/Config.cpp:316` → `src/Rules.cpp:61` | ✓ WIRED | Test #261, #268 |
| title change | rule re-fold | (deliberately absent, D-8.5-03) | ✓ WIRED (as designed) | Test #131 reddens if a re-fold is added |
| `scripts/capture-display-capabilities.sh` | `evidence/<label>/*` | script invocation | ✓ WIRED | Both new target dirs carry the script's provenance header |
| four transcripts | XDIS-05 Complete, no amendment | `.planning/REQUIREMENTS.md:56,180` | ✓ WIRED | Ledger updated |
| four transcripts | user-facing remote-target table | `docs/RELEASE-NOTES.md:304-359` | ✗ NOT_WIRED | The evidence exists; the shipped document was never repointed at it |
| interaction table | `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` unticked rows | Task 4 checklist walk | ✗ NOT_WIRED | The table exists; the checklist rows it was meant to close are unchanged, and the Release-Evidence row still asserts the table does not exist |
| final commit | gate evidence | `evidence/gates/PROVENANCE.txt` | ✗ NOT_WIRED | No such artifact |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Title rule places a window (real binary) | `ctest --test-dir build/debug -I 126,133` | 9/9 passed, incl. #127, #131, #133 | ✓ PASS |
| EWMH title reaches the tab | `ctest --test-dir build/debug -I 189,189` | Passed | ✓ PASS |
| WM_NAME-only client unaffected (fallback direction) | `ctest --test-dir build/debug -I 204,204` | Passed | ✓ PASS |
| Rename complete, no alias | `ctest --test-dir build/debug -I 265,270` | 6/6 passed, incl. #269 | ✓ PASS |
| `rule-match-title` opens a rule group | `ctest --test-dir build/debug -I 278,278` | Passed | ✓ PASS |
| Static analysis gate | `bash scripts/analysis/run-static-analysis.sh` | exit 0 — cppcheck 15 findings all baselined, clang-tidy no enforced check fired | ✓ PASS |
| Test surface recount | `grep -c '^TEST_CASE' tests/*.cpp` / `ctest -N \| tail -1` | 311 cases / Total Tests: 313 (checklist claims 293/292/295) | ✗ FAIL — record stale |
| `x2goserver` install claim | `grep x2goserver /var/log/dpkg.log*` / `dpkg -l x2goserver` | `install x2goserver-common:all 4.1.0.3-5` 2026-08-29 17:43:22; `ii x2goserver 4.1.0.3-5` | ✗ FAIL — release notes claim it was never installed |
| `vncserver` alternative restored | `update-alternatives --display vncserver` | points at `/usr/bin/tigervncserver`, auto mode | ✓ PASS |
| No network-exposing VNC flags in evidence | `grep -c 'localhost no\|localhost=0' evidence/tightvnc/*` | 0 in all five files | ✓ PASS |
| No credentials in the bundle | `grep -rnE 'API_KEY\|ACCESS_KEY\|SECRET\|TOKEN\|PRIVATE KEY\|Bearer '` over the evidence dir | No hits in any transcript (only the pre-existing key *names* in 08.5-CONTEXT.md's security note) | ✓ PASS |
| T-8-REMOTE title screening | quoted strings in both `window-tree.txt` | Only `wm2-born-agai`, `wm2-{tightvnc,x2go}-validation`, `xclock`, `XClock` | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` exist in this repository and neither PLAN declares a probe. The project's equivalent runnable gates are `scripts/gates/build-all.sh` and `scripts/analysis/run-static-analysis.sh`; the latter was executed above (exit 0). The former was not re-run here — the session's regression gate already reported 313/313 at HEAD — but its *committed evidence* is the subject of gap 2.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| RULES-01 | 08.5-01 | Window matching rules by WM_CLASS, WM_NAME, window type | ✓ SATISFIED | Met as written, not amended. `rule-match-title` + EWMH-first read + key rename all verified in code and by nine passing behavioural cases I ran. Traceability row: Complete. |
| XDIS-05 | 08.5-02 | Compatible with TigerVNC, TightVNC, XRDP, X2Go out of the box | ⚠️ SATISFIED WITH DEFECT | Four transcripts committed; the requirement's substance is met and the ledger correctly says so. But `docs/RELEASE-NOTES.md` still tells the reader the requirement is "held **unmet**" and that two targets are "Not tested" — the ledger and the shipping document disagree about this requirement's own status. That is "a requirement silently mis-stated to the user", which is what the phase goal set out to eliminate. |
| TEST-08 | 08.5-02 | Runtime smoke evidence incl. interaction checklist results and accepted deviations | ⚠️ SATISFIED WITH DEFECT | The last open component — the per-item table — exists with a tester name and a date. But `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:651-658`, the project's own signoff document, still carries the "Manual interaction checklist results" row **unticked**, with the sentence "What does NOT exist is a per-item pass/fail table … Remains open." That sentence is now false, and the row is the release gate for exactly this deliverable. |

**Orphaned requirements:** none. `REQUIREMENTS.md` maps RULES-01, XDIS-05 and TEST-08 to Phase 8.5; all three are claimed by a plan.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `docs/RELEASE-NOTES.md` | 314, 317-319, 322-323, 332-333, 344-348 | False statements in a shipped document, each contradicted by evidence committed in the same phase | 🛑 Blocker | Gap 1. Same defect class the project treated as blocker-grade in Phase 8's VERIFICATION (`rule-match-name` documented as matching the title) |
| `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` | 133-231, 637-650 | Release-gate rows cite another phase's evidence snapshot (`c61cb4b`, 295/295) after five source files changed | 🛑 Blocker | Gap 2. A gate reporting 295 against a tree of 313 cannot detect a file that silently stopped being registered |
| `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` | 159-201 | The row that says in bold "RECOMPUTE, DO NOT TRANSCRIBE" was transcribed, not recomputed | 🛑 Blocker | Gap 2. Measured: 311/313 vs the stated 293/292/295 |
| `.planning/phases/08.5-v1.0-closeout/evidence/` | — | `gates/` and `README.md` required by plan 08.5-02 Task 4 were never created | 🛑 Blocker | Gap 2 |
| `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` | 651-658 | Signoff row asserts an artifact "does NOT exist" that this phase created and committed | ⚠️ Warning | The interaction table is delivered but its own gate row still reads open |
| `src/Manager.cpp` | 720-728 | `allocateShadeOf()` calls `fatal()` when `XAllocNamedColor` fails. That call both parses *and allocates*, so it returns zero on a saturated read/write colormap — killing the WM at startup on a low-depth PseudoColor session. `include/Border.h:183-184` promises the opposite: "Either GC may be null when the colormap is full … no bevel is the correct degradation, not a fatal error" | ⚠️ Warning | A documented promise the code contradicts, on the deployment class (8-bit remote sessions) this project exists for. Aggravated rather than introduced — `allocateColour()` has the same shape — but the new call doubles the exposure |
| `src/Border.cpp` | 79-104 | Pixel `0` used as the allocation-failure sentinel; on TrueColor `0` is a legal pixel (pure black) | ⚠️ Warning | A dark palette blending to black yields a *successful* allocation the caller silently discards, producing a half-drawn bevel with no diagnostic. Same sentinel-collision class the codebase already guards against in `include/Rules.h:66-75` |
| `docs/RELEASE-NOTES.md` | 71-76 | "Ubuntu, **in bold**, for tab labels and menus" — `src/Manager.cpp:673-674` requests `"Ubuntu,Noto Sans,DejaVu Sans,Sans:size=12"` with no `:bold`. Only the tab font is bold (`src/Border.cpp:241-242`) | ⚠️ Warning | The stated legibility rationale ("bold survives unantialiased rendering on a RENDER-less server — TightVNC, for one") is not applied to the menu, which is the surface a user reads most on exactly that server |
| `docs/RELEASE-NOTES.md` | — | `destroy-window-delay` default cut 1500 → 400 ms (a 3.75x reduction in the guard on a destructive action) with no release note. `grep -c destroy-window-delay docs/RELEASE-NOTES.md` = 0 | ⚠️ Warning | `include/Config.h:87-91` states the risk itself ("A false DELETE sends WM_DELETE_WINDOW and can lose the user's work"). The notes document the bevel and the colour cast in detail and omit this |
| `src/Client.cpp` | 875 | `getProperty_aux(..., 8, 100L, ...)` caps a title read at 400 bytes. That was harmless while the title only fed the tab; it now feeds `rule-match-title` | ⚠️ Warning | `rule-match-mode = exact` against a title longer than 400 bytes can never match, silently. Config values are capped at 256 chars (`src/Config.cpp:129-131`), so the two limits do not line up for multi-byte titles. Narrow, but it is the "control that looks available and does nothing" class this phase set out to remove |
| `scripts/start-validation-session.sh` | 120-121 | A committed script prints "including the four API keys you have chosen not to rotate" | ⚠️ Warning | Publishes one host's secret-management posture in a repository the project describes as a showcase, and the sentence is false for every other user |
| `scripts/stress-exerciser.sh` | 61, 64, 108, 115 | Fixed, predictable `/tmp/wm2-stress-wm.log` with `>` and no `set -C` (CWE-59/CWE-377) | ⚠️ Warning | Symlink-follow truncation of an operator-owned file on a shared host; concurrent runs also cross-count each other's errors |
| `evidence/INTERACTION-CHECKLIST.md` | 24 | "the **seven** DEFERRED rows" — the table and the coverage summary both say eleven | ℹ️ Info | Internal inconsistency in an evidence artifact whose value is its precision |
| `evidence/{tightvnc,x2go-nxagent}/capabilities.txt` | 15 | `tree-state: DIRTY` | ℹ️ Info | Plan 08.5-02 Task 1's acceptance criterion asked for "a commit hash and a clean tree state". All four Phase 8 transcripts are also DIRTY, so this is the established convention rather than a regression — but the exact source state behind a transcript cannot be reconstructed from its commit hash |

### Human Verification Required

None blocking. The two gaps are both observable in the codebase and require no human judgement to confirm; the failing statements were checked against committed evidence and, for the `x2goserver` claim, against `/var/log/dpkg.log` and `dpkg -l` directly.

### Gaps Summary

Five of seven success criteria are verified, and the parts that are verified are
verified well: the title-matching work is behaviourally proven against the real
binary in both the EWMH and the fallback direction, the rename carries a negative
test that would catch an alias, the map-time semantic is stated in all three
required places *and* pinned by a test that reddens if it is violated, and the
TightVNC transcript did the genuinely valuable thing of disproving the deviation
that was written to excuse skipping it.

Both failures are the same failure, in two documents: **the phase produced the
evidence and did not propagate it.**

`docs/RELEASE-NOTES.md` is the more serious of the two, because it ships. Its
remote-desktop section was left at the Phase 8 text while
`COMPILED_CODE_BEHAVIOR_CHECKLIST.md` was updated around it in the same commit
range. The result is that the user-facing document tells a reader TightVNC is
untested — in a repository that contains its transcript — repeats a claim about
the validation host that the adjacent signoff file explicitly names as false, and
states that the four-target requirement is "held **unmet**" while
`REQUIREMENTS.md` marks it Complete. The phase goal's first clause is "zero
requirements silently unmet"; here a requirement that *is* met is documented to
the user as unmet, which is the same drift running the other way. Plan 08.5-02
Task 4 named this edit explicitly. The only acceptance criterion guarding it was
`grep -c 'validation pending'` == 0, and that string never appeared in the file —
a vacuous check of exactly the kind this project's own summaries keep warning
about.

`COMPILED_CODE_BEHAVIOR_CHECKLIST.md`'s gate rows are the second. Success
criterion 7 exists because Phase 8's own bundle noted that nothing re-validates a
snapshot when later commits land. This phase changed `src/Client.cpp`,
`src/Rules.cpp`, `src/Config.cpp`, `src/Border.cpp` and `src/Manager.cpp`, and the
signoff document still cites Phase 8's logs at `c61cb4b`. The gates themselves are
green — I ran static analysis to exit 0, and the session's regression gate
reported 313/313 — so closing this is capture-and-record work, not a fix. But
until it is captured, the release signoff certifies a tree that no longer exists,
and the row designed to catch that drift is itself carrying stale numbers under a
bolded instruction not to transcribe them.

Neither gap is deferred: Phase 9 (Config GUI + IPC) covers neither release-note
accuracy nor gate evidence.

---

_Verified: 2026-08-30T16:05:00Z_
_Verifier: Claude (gsd-verifier)_
