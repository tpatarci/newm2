---
phase: 08.5-v1.0-closeout
verified: 2026-09-05T16:00:00Z
re_checked_at_final_commit: 2026-09-05T17:10:00Z
status: human_needed
score: 7/7 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 5/7
  previous_verified: 2026-08-30T16:05:00Z
  gaps_closed:
    - "Both Phase 8 deviations are retired on evidence or restated with a reason that is true — neither survives as written (docs/RELEASE-NOTES.md:303-421 rewritten by 08.5-03 at 195ac51; guards `never installed` = 0 and `Not tested` = 0 re-read at HEAD)"
    - "The four hard blockers are re-run at the final commit, not inherited from Phase 8's snapshot (evidence/gates/ bundle bound to f54de6e by PROVENANCE.txt; no source drift f54de6e..HEAD; checklist gate rows repointed and test surface recomputed by 08.5-05)"
  gaps_remaining: []
  regressions: []
verified_how:
  ran: "git diff/log/merge-base, grep, sed, ctest -N on the idle release tree, one gsd-tools query"
  did_not_run: "cmake, make, ctest execution, build-all.sh, run-static-analysis.sh — a ten-run measurement was executing in build/debug; every gate result below is read from a committed log, not reproduced"
human_verification:
  - test: "Ratify or reverse the two `blocking-human` rulings taken by the delegated project lead rather than by the operator: 08.5-10 Task 3 (`Attribution outcome: ATTRIBUTED`, `Terminal disposition: FIX-PLAN`, evidence/gates/wakeup/README.md:545-548) and 08.5-12 Task 5 Decision A (08.5-07 SUPERSEDED, 08.5-08 and 08.5-05 RELEASED, 08.5-12-SUMMARY.md:93-102)"
    expected: "The operator either types the ruling into the record (one line each) or reverses it as a recorded decision. 08.5-12-PLAN.md:43 says Task 5 'writes no file until the operator replies' and that the gate 'always surfaces, in every mode'; both summaries state the operator did not type the word."
    why_human: "A checkpoint whose contract is an operator reply cannot be closed by reading; the plan's own text makes a specific person's reply the gate."
  - test: "Confirm the interaction table's coverage split before it is quoted further: count the Result column of evidence/INTERACTION-CHECKLIST.md rows 1-31"
    expected: "10 PASS / 6 PASS (verdict) / 11 DEFERRED / 4 n/a = 31. The file's own Coverage summary, COMPILED_CODE_BEHAVIOR_CHECKLIST.md:755, .planning/REQUIREMENTS.md:97 and evidence/README.md:125 all say 9 / 7 / 11 / 4. Row 23 ('The sideways tab looks right') is recorded PASS, filled in at ec84a44 — the summary was never recounted after it."
    why_human: "Which of the two is the operator's intent for row 23 (walked, or covered by verdict) is a fact only the tester can state; the arithmetic is settled either way, the label is not."
coincidental_reliance_items: []
---

# Phase 8.5: v1.0 Closeout Verification Report

**Phase Goal:** v1.0 ships with zero requirements silently unmet — window rules match by title under keys named for what they compare, all four remote-desktop targets have committed transcripts, and the interaction record is a per-item table rather than a verdict.
**Verified:** 2026-09-05T16:00:00Z at HEAD `e244492` (branch `worktree-agent-a0ef668e51c39c8dd`)
**Status:** human_needed
**Re-verification:** Yes — after gap closure (previous report 2026-08-30, 5/7, gaps_found)

## How this was verified, and what was deliberately not done

A ten-run measurement was executing in `build/debug` while this report was written, so
**no build, test, sanitizer or static-analysis command was run.** Every gate reading below
is taken from a committed log in `evidence/gates/` and the binding between those logs and
the shipping tree was checked with git, not by rebuilding. What was run: `git diff`,
`git log`, `git merge-base`, `grep`, `sed`, `ctest --test-dir build/release -N` (listing
only; that tree was idle), and one `gsd-tools` query. The report says "read" where it means
read and "ran" where it means ran.

The old report's two gaps were both about the record lagging the evidence. Both are closed.
What this re-verification adds is a set of new record defects that arrived with the closure
— none of them fails a success criterion, several of them are exactly the kind of drift the
phase exists to remove, and one of them is a governance question only the operator can answer.

## Goal Achievement

### Observable Truths

| # | Truth (ROADMAP success criterion) | Status | Evidence — path / command | Note |
|---|---|---|---|---|
| 1 | A rule can match a window by its title, `_NET_WM_NAME` preferred, `WM_NAME` as fallback | ✓ VERIFIED | Read: `src/Client.cpp:1049-1054` (`getWindowTitle()` reads `Atoms::net_wmName` typed `utf8_string`, falls back to `XA_WM_NAME` on empty), `:117` (manage-time read), `:1247` (`facts.title = m_name`), `:2266,2284` (both property-change paths); `include/Client.h:243` (`getProperty(Atom, Atom type = XA_STRING)`); `src/Rules.cpp:39,61-63` (title in the no-criteria guard and the AND fold). Behavioural: `evidence/gates/build-all-debug.log:366,370,500,504` — tests #150, #152, #217, #219 recorded `Passed` at `f54de6e`; `grep -c Failed` over all three gate logs = 0 | Behaviour-dependent truth. The behavioural evidence is a **committed log**, not a run I made; it is admissible because `git diff --name-only f54de6e..HEAD -- src include tests scripts CMakeLists.txt` prints nothing (ran it) and `f54de6e` is an ancestor of HEAD (ran it). |
| 2 | Keys named for what they compare; `rule-match-name` gone, not aliased | ✓ VERIFIED | Read: `src/Config.cpp:287-289` (`isRuleMatchKey` = class, instance, title, type, mode), `:310,316` dispatch; `tests/test_rules.cpp:846` negative case. Ran: repo-wide `grep -rn rule-match-name` outside `.planning/` → only `src/Config.cpp:269` (comment), `include/Rules.h:91` (comment), `docs/RELEASE-NOTES.md:217` (migration note), `tests/test_rules.cpp:840-862` (the negative test). `evidence/gates/DOC-GUARDS.txt:15-38` parity PASS/PASS at `f54de6e`. Behavioural: `build-all-debug.log:664,666` — #299 "rule-match-name is no longer a rule key", #300 "rule-match-instance matches the instance name only", `Passed` | Plan 08.5-01's literal criterion `grep -c rule-match-name docs/RELEASE-NOTES.md == 0` is not met (it is 1, the migration note). DOC-GUARDS names that one asymmetry as expected; the old report accepted it. Recorded, not counted against. |
| 3 | Map-time fold semantic stated in the requirement, the release notes and the code | ✓ VERIFIED | Read: `.planning/REQUIREMENTS.md:85` ("rules fold once, at manage time … D-8.5-03"); `docs/RELEASE-NOTES.md:199-211` ("A title rule looks at the title the window has when it opens … renaming a document will not make its window jump"); `src/Client.cpp:1221-1238` (comment block above `applyWindowRules()`), `include/Rules.h:50-58`. Behavioural pin: `build-all-debug.log:356` — #145 "A title changed after mapping does not re-apply the rule", `Passed` | — |
| 4 | All four PROJECT.md remote-desktop targets have a committed capability transcript at a known commit | ✓ VERIFIED | Ran: `test -s` and `grep commit-short/tree-state` — `evidence/tightvnc/capabilities.txt` @ `9869aea`, `evidence/x2go-nxagent/capabilities.txt` @ `f5bbea7`, Phase 8 `tigervnc/` @ `dba2f30`, `xrdp/` @ `26344b4`; all four `tree-state: DIRTY`; all four commits resolve in `git log`. Each of the two new dirs has 4 transcript files + `SCOPE.md`. `tightvnc/capabilities.txt:52,88` — SHAPE listed, "RANDR is ABSENT" | DIRTY is the established convention across all six transcripts (Phase 8 and this phase); `evidence/README.md:44-52` states it. The x2go transcript is a floor, not a tick, and every document that cites it says so. |
| 5 | The User Interaction Checklist exists as a per-item pass/fail table with a tester name and a date | ✓ VERIFIED | Read: `evidence/INTERACTION-CHECKLIST.md:3-7` — Tester `tpatarci (operator)`, Date `2026-08-30`, Target TigerVNC `:11`, Commit `0244c06`; 31 numbered rows each with a Result and a Note. Ran: awk over the Result column → **10 PASS, 6 PASS (verdict), 11 DEFERRED, 4 n/a = 31** | **Warning:** the file's own Coverage summary (L120-127) says 9 / 7 / 11 / 4. Total and the DEFERRED count — the figures that carry the honest gap — are right; the PASS split is off by one (row 23). The wrong split is repeated verbatim in `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:755`, `.planning/REQUIREMENTS.md:97`, `evidence/README.md:125`. The old report said "arithmetic checks out"; it does not. Routed to human verification for the label, not the count. |
| 6 | Both Phase 8 deviations are retired on evidence or restated with a reason that is true — neither survives as written | ✓ VERIFIED (gap 1 closed) | Ran: `grep -c 'never installed' docs/RELEASE-NOTES.md` = 0; `grep -c 'Not tested'` = 0; `grep -nE 'unmet|substantially overlap|Two of the four|validation pending'` = no matches. Read: `docs/RELEASE-NOTES.md:303-421` — table row TightVNC "Validated … 7 extensions, SHAPE present, RANDR absent, RENDER absent", X2Go "Partially validated … NX compression proxy … not exercised", L316-321 "requirement … is **met**", two rewritten subsections L331-420; `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:776-860` retire D-8-TIGHTVNC on evidence and restate D-8-X2GO with owner and follow-up. `DOC-GUARDS.txt:5-11` records both guards 0/0 at `f54de6e` | Every one of the old report's five artifact issues is gone at HEAD. One residual: `docs/RELEASE-NOTES.md:346` cites the dpkg stamp as `x2goserver-common:all 4.1.0.3-5` while `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:836` cites `x2goserver:amd64 4.1.0.3-5` — both packages were installed in the same minute per the old report's own `dpkg` reading, so neither is false, but the two documents name different packages for the same fact. Info. |
| 7 | The four hard blockers are re-run at the final commit, not inherited from Phase 8's snapshot | ✓ VERIFIED (gap 2 closed) | Read: `evidence/gates/PROVENANCE.txt` — commit `f54de6efec4057ec9259d0200f70b93fae5cc4f4`, `debug/release/asan/static-analysis/preflight/runtime-smoke exit=0`; `build-all-{debug,release,asan}.log` lines 736/743/743 "100% tests passed, 0 tests failed out of 336"; `build-all-asan.log:815` "no sanitizer findings"; `build-all-release.log:814` "link audit OK (24 entries …)"; `static-analysis.log` "cppcheck OK … clang-tidy OK … static-analysis OK"; six `sanitizer-reports/gate.*` each the 189-byte LSan suppression stanza only. Ran: all 14 bundle files `test -s` non-empty; `git diff --name-only f54de6e..HEAD -- src include tests scripts CMakeLists.txt` → **empty**; `git log f54de6e..HEAD` → two docs-only commits (`19c6dee`, `e244492`); `git merge-base --is-ancestor` → yes; `ctest --test-dir build/release -N | tail -1` → `Total Tests: 336`; `grep -cE '\`evidence/gates/' COMPILED_CODE_BEHAVIOR_CHECKLIST.md` = 0; `grep -c '^- \[ \]'` = 13; `grep -c '^TEST_CASE' tests/*.cpp` summed = 334 across 23 files, and the per-file table at checklist L179-193 matches my counts file for file; the hidden `[.][wm_resource_calibration]` case is at `tests/test_wm_resource.cpp:455-456` | **Warning (see Anti-Patterns):** the gate build was incremental, not clean — `build-all-debug.log` has 0 `Building CXX` lines and the release/asan logs 4 each (`Client.cpp`, `Border.cpp`, `test_eventloop`, link), and `scripts/gates/build-all.sh:115-139` re-runs `cmake -S -B` without wiping the tree. So the "0 compiler/linker warning line(s)" reading (`build-all-debug.log:60`) certifies only the TUs that recompiled — none, in the debug tree. Build, ctest, sanitizer and static analysis genuinely re-ran at `f54de6e`; the *warning* gate at that commit is narrower than the checklist row at L138-146 implies. Not inherited from Phase 8 — that is the criterion — but the row's word "clean" (L133) is not what the log shows. |

**Score:** 7/7 truths verified (0 present, behavior-unverified). Truth 1 and truth 3 are
behaviour-dependent; their behavioural evidence is the committed debug gate log at a commit
with zero source drift to HEAD, which is the strongest evidence available without running
anything, and is weaker than a run I made myself. Stated as such.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/Client.cpp`, `include/Client.h`, `src/Rules.cpp`, `include/Rules.h`, `src/Config.cpp` | title read, typed `getProperty`, title criterion in guard and fold, renamed keys | ✓ VERIFIED | Line references in truths 1-3; unchanged since `f54de6e` |
| `tests/test_wm_rules.cpp` | process-level proof against the real binary | ✓ VERIFIED | 17 `[wm_rules]` cases (was 13 at the old report; +4 from the review-fixes rounds, each named in `evidence/gates/review-fixes/README.md`) |
| `docs/RELEASE-NOTES.md` (rules section) | shipped key set, both directions | ✓ VERIFIED | `DOC-GUARDS.txt` parity PASS/PASS; the one asymmetry is the migration note |
| `docs/RELEASE-NOTES.md` (remote-target section) | four measured statuses, nested-vs-real kept apart | ✓ VERIFIED | Was ✗ STUB. Rewritten at `195ac51`; the two guards the old report asked for are 0/0 |
| `evidence/tightvnc/`, `evidence/x2go-nxagent/` | 4 files + SCOPE each, provenance header | ✓ VERIFIED | Unchanged since the old report |
| `evidence/INTERACTION-CHECKLIST.md` | per-item table, tester, date | ✓ VERIFIED | Coverage-summary split is wrong (10/6 vs stated 9/7); see truth 5 |
| `evidence/README.md` | bundle index, credential warning first, limits section | ✓ VERIFIED | Was ✗ MISSING. Credential warning at L16; layout tables; "What this bundle does not establish" at L143-165 lists six items, none softened |
| `evidence/gates/` (14 files) + `PROVENANCE.txt` | build/ctest/sanitizer/static-analysis at the final commit | ✓ VERIFIED | Was ✗ MISSING. All 14 non-empty; bound to `f54de6e` |
| `evidence/gates/DOC-GUARDS.txt` | gap-1 guards and rule-key parity at the capture commit | ✓ VERIFIED | Readings 0 / 0 / PASS / PASS; also records the 400-byte title cap as v1.1, not fixed |
| `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` | gate rows repointed, test surface recomputed, interaction rows citing the table | ✓ VERIFIED | Was ⚠️ PARTIAL. 0 bare `evidence/gates/` refs; 336 everywhere a total appears; per-file table exact; Release-Evidence interaction row ticked (L751-767); the seven declined User Interaction rows deliberately left open with the table row cited (L469-560). The "Configure a **clean** Debug build" row (L133) is ticked against an incremental log — see truth 7 |
| `.planning/WINDOWS.md` | ledger closed | ✓ VERIFIED | `open_count: 0` (6 fixed, 8 waived with reasons) |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `_NET_WM_NAME` (UTF8_STRING) | `m_name` → tab label and rule fold | `getWindowTitle()` at `src/Client.cpp:117,2266,2284`; `facts.title` at `:1247` | ✓ WIRED | Tests #152, #219 in the committed debug log |
| title change | rule re-fold | deliberately absent (D-8.5-03) | ✓ WIRED as designed | Test #145 reddens if a re-fold is added |
| four transcripts | user-facing remote-target table | `docs/RELEASE-NOTES.md:307-314` | ✓ WIRED | Was ✗ NOT_WIRED. Each row now states the measured status and the transcript path |
| four transcripts | `XDIS-05 Complete` | `.planning/REQUIREMENTS.md:56,180` | ✓ WIRED | Ledger and shipping document now agree ("met", L321) |
| interaction table | checklist rows | `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:439-560, 751-767` | ✓ WIRED | Was ✗ NOT_WIRED. Twelve rows cite the table row by row; exactly one box flipped |
| final commit | gate evidence | `PROVENANCE.txt` + no-drift diff | ✓ WIRED | Was ✗ NOT_WIRED. Diff empty at HEAD (ran it) |
| `ROADMAP.md` 08.5-08 line | the bundle it describes | text at `.planning/ROADMAP.md:352` | ⚠️ STALE | Says "bundle bound to `d08b6e5`, all three trees 335/335". PROVENANCE says `f54de6e`, 336. `d08b6e5` is attempt 3, superseded (`capture-attempts/README.md`). The roadmap's own status paragraph above it does not repeat the error |
| `STATE.md` item 5 | `fix-measurement/README.md` | "a third series at the final capture commit is recorded in the same README" (`.planning/STATE.md:55-56`) | ⚠️ NOT TRUE YET | The README has two sections (`70fbd8f`, `5ffee20`) and says itself (L8-11) "until that section exists, nothing here speaks for the tree that shipped." The third series is the measurement running now: top-level `LEDGER.txt` shows 2 of 10 done at 15:52Z, `run-03.log` partial; `LEDGER.txt`, `run-02.log`, `run-03.log` are untracked and `run-01.log` differs from the copy committed in `19c6dee` — a tracked top-level `run-01.log` no README describes |

### Data-Flow Trace (Level 4)

Not applicable in the UI sense; the phase renders no dynamic data. The analogous trace — does each
figure in the signoff document come from a real reading — was run on the checklist's test-surface
row: the three commands at L159-167 produce 334 / 23 / 336 at HEAD (ran the first two; read the
third from `ctest -N` on the release tree), which is what the row states.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| No source drift between gate commit and HEAD | `git diff --name-only f54de6e..HEAD -- src include tests scripts CMakeLists.txt` | empty | ✓ PASS (ran) |
| Gate commit is in HEAD's history; later commits docs-only | `git merge-base --is-ancestor`; `git log --oneline f54de6e..HEAD` | yes; `19c6dee`, `e244492` | ✓ PASS (ran) |
| Gap-1 guards | `grep -c 'never installed'`, `grep -c 'Not tested'` on `docs/RELEASE-NOTES.md` | 0, 0 | ✓ PASS (ran) |
| Checklist repointed | `grep -cE '\`evidence/gates/' COMPILED_CODE_BEHAVIOR_CHECKLIST.md` | 0 | ✓ PASS (ran) |
| Open boxes | `grep -c '^- \[ \]' COMPILED_CODE_BEHAVIOR_CHECKLIST.md` | 13 | ✓ PASS (ran) |
| Test surface | `grep -c '^TEST_CASE' tests/*.cpp` summed; per file; `ctest --test-dir build/release -N \| tail -1` | 334; table matches; `Total Tests: 336` | ✓ PASS (ran) |
| Bundle completeness | `test -s` on all 14 bundle files | all non-empty | ✓ PASS (ran) |
| All three trees green at `f54de6e` | read `build-all-*.log` verdict lines; `grep -c Failed` | 336/336 ×3; 0 | ✓ PASS (read) |
| ASan clean | read `build-all-asan.log:815`; six `sanitizer-reports/gate.*` | "no sanitizer findings"; suppression stanza only | ✓ PASS (read) |
| Static analysis | read `static-analysis.log` | cppcheck 15 baselined, clang-tidy no enforced check, `static-analysis OK` | ✓ PASS (read) |
| Title-rule, EWMH, no-refold, rename cases | read `build-all-debug.log` for #145, #150, #152, #217, #219, #299, #300 | all `Passed` | ✓ PASS (read) |
| Gate build clean vs incremental | `grep -c 'Building CXX' build-all-*.log` | debug 0, release 4, asan 4 | ⚠️ incremental — see truth 7 |
| Interaction table arithmetic | awk over the Result column | 10/6/11/4 vs stated 9/7/11/4 | ✗ FAIL — summary line stale |
| Ledger closed | `.planning/WINDOWS.md` frontmatter | `open_count: 0` | ✓ PASS (read) |

### Probe Execution

No `scripts/*/tests/probe-*.sh` exist and no plan declares one. The project's runnable gates are
`scripts/gates/build-all.sh` and `scripts/analysis/run-static-analysis.sh`; **neither was run here**
(hard constraint: a measurement was executing in `build/debug`). Their committed outputs at
`f54de6e` are the evidence for truth 7, and the old report did run static analysis itself at the
previous HEAD (exit 0).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| RULES-01 | 08.5-01 (+ 08.5-03, 08.5-08 guards) | Window matching rules by WM_CLASS, WM_NAME, window type | ✓ SATISFIED | Met as written, not amended. Truths 1-3. `REQUIREMENTS.md:85,183` Complete. Parity guard PASS/PASS at the gate commit. One recorded, unfixed limit: `rule-match-title` compares bytes and the title read is capped at 400 bytes (`src/Client.cpp:1009`) against a 256-character config cap — `DOC-GUARDS.txt:27-31` records it as v1.1, not closed |
| XDIS-05 | 08.5-02, 08.5-03 | Compatible with TigerVNC, TightVNC, XRDP, X2Go out of the box | ✓ SATISFIED | Was "satisfied with defect". Four transcripts at four commits; `docs/RELEASE-NOTES.md:316-321` and `REQUIREMENTS.md:56,180` now say the same thing. What it does **not** establish is written in three places and not softened: X2Go through its NX proxy, anything judged by eye on TightVNC |
| TEST-08 | 08.5-02, 08.5-05, 08.5-08 | Runtime smoke evidence incl. interaction checklist results and accepted deviations | ✓ SATISFIED, with a declared gap and one arithmetic defect | Per-item table with tester and date; this phase's own `runtime-smoke/` at the gate commit; deviations each with reason/owner/follow-up; Release-Evidence rows 0 open. Declared gap: 11 DEFERRED rows, 9 with no coverage of any kind (`INTERACTION-CHECKLIST.md:21-28`, `README.md:150-155`). Defect: the 9/7 PASS split, repeated in four documents |

**Orphaned requirements:** none. `REQUIREMENTS.md:180,183,197` map exactly RULES-01, XDIS-05, TEST-08 to Phase 8.5; all three are claimed by plans.

### Decision Coverage

`gsd_run query check.decision-coverage-verify` returned `could-not-parse` on `08.5-CONTEXT.md`'s
`<decisions>` block (the decisions are `###` headings, not list items). Checked by hand instead:
D-8.5-01 (rename, no alias) → `src/Config.cpp:287`, `tests/test_rules.cpp:846`; D-8.5-02
(EWMH-first) → `src/Client.cpp:1049`; D-8.5-03 (fold once) → three places named in truth 3;
D-8.5-04 (X2Go validated, not descoped) → `evidence/x2go-nxagent/`, `REQUIREMENTS.md:56`;
D-8.5-05 (table, per item) → `evidence/INTERACTION-CHECKLIST.md`; D-8.5-06 (deviations retired
or restated) → checklist L776-860, release notes L331-420. 6 of 6 honoured. Warning-only gate.

### The three success criteria most at risk

**Criterion 7 — the gate bundle.** Closed, and the closure is better than the plan asked for in
one respect and weaker in another. Better: the capture was stopped three times on findings its
own logs surfaced (five cppcheck findings, `_NET_WM_NAME` one byte short, one new `-Wunused-result`
in the release tree), each fixed at source and the whole capture re-run, with the red logs kept
under `capture-attempts/` — that is the gate doing its job. Weaker: `build-all.sh` reconfigures
without wiping, and at `f54de6e` the debug tree recompiled nothing, so the "0 warning lines" reading
there is a reading of an empty build. The `-Wunused-result` finding was caught precisely because the
release tree *did* recompile the file that carried it; a warning in a TU that was already up to date
in all three trees would not have been seen at this commit. The checklist row (L133) says "clean";
the log does not. This does not make the criterion inherited — nothing here is Phase 8's — but
the next capture should wipe `build/` first or the row should stop saying "clean". Two records also
lag the bundle: `ROADMAP.md:352` still names `d08b6e5`/335, and `08.5-05-SUMMARY.md:8-9` credits
the bundle to plan 08.5-04, which halted without producing one.

**TEST-08's interaction rows.** The table is real, per-item, signed and dated, and the seven
checklist rows that stay open stay open for the right reason — the operator declined them, and
ticking them would assert something nobody did. The eleven deferred rows are the honest gap and every
document that quotes them says so in the same words. The one defect is arithmetic: the table says
10 PASS and 6 PASS (verdict); its summary and three downstream documents say 9 and 7. The DEFERRED
count, which is the figure that matters, is right everywhere. The old report certified this
arithmetic and was wrong to.

**The flake attribution.** The chain now reads: 2-red-of-5 measured (08.5-04) → INCONCLUSIVE
(08.5-06) → REFUTED for the timestamp hypothesis (08.5-09, operator-ruled `397310a`) → ATTRIBUTED
to the readiness check before `poll()` (08.5-10, perfect 65/64 arm separation, then reproduced
deterministically by 08.5-11's forty-line probe) → fixed with RED-then-GREEN records
(`eventloop/README.md:90-99`: three of four cases FAILED at the Task 1 tip, all pass after;
`timestamp/README.md:108-125`; `menupaint/`; `modal/`) → 20 consecutive green debug runs at two
pre-capture trees → one green capture of all three trees at the final tree. What that chain
does **not** establish, in the record's own words and not softened here: a rate (`fix-measurement/
README.md:99-108` gives the Fisher arithmetic, p ≈ 0.095 for 0-of-10 against 2-of-5, and declines to
lean on it); ten green runs *at the shipping tree* (the third series is running now and is not yet
in the README; `STATE.md:55-56` says it is recorded, which is premature); and a randomisation
inference (the arm assignment alternated). Two things in this chain need a human. The ATTRIBUTED
ruling and Decision A were taken by the delegated project lead under a standing instruction, not
by the operator — both records say so plainly and in terms designed to be reversible
(`wakeup/README.md:550-553`, `08.5-12-SUMMARY.md:95`). The plans' own text (`08.5-12-PLAN.md:43`)
makes the operator's reply the gate. That is the human-verification item this report ends on.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `.planning/ROADMAP.md` | 352 | 08.5-08 plan line says bundle bound to `d08b6e5`, 335/335; `PROVENANCE.txt` says `f54de6e`, 336 | ⚠️ Warning | The roadmap names a superseded capture as the criterion-7 bundle |
| `.planning/STATE.md` | 55-56 | "a third series at the final capture commit is recorded in the same README" — it is not; the series is in progress (2/10 at 15:52Z) and its files are uncommitted | ⚠️ Warning | State claims a measurement that has not finished |
| `evidence/gates/fix-measurement/run-01.log` | — | Tracked at the top level (committed in `19c6dee`) but described by no README section; now differs from the working copy the running series wrote | ⚠️ Warning | A committed log with no provenance sentence, in a bundle whose rule is that every file says what it is |
| `evidence/gates/fix-measurement/README.md` | 32 | Cites `../capture-8e29d6d/README.md`; no such path. The record is `capture-attempts/README.md` | ℹ️ Info | Broken cross-reference |
| `evidence/INTERACTION-CHECKLIST.md` | 120-127 | Coverage summary 9 PASS / 7 PASS (verdict); the Result column has 10 / 6 (row 23 is PASS) | ⚠️ Warning | Repeated in `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:755`, `.planning/REQUIREMENTS.md:97`, `evidence/README.md:125` |
| `08.5-05-SUMMARY.md` | 8-9 | `requires: plan 08.5-04 … provides: the gate bundle at commit f54de6e` — 08.5-04 halted at Task 1; the bundle is 08.5-08's | ℹ️ Info | Wrong attribution in a summary's dependency graph |
| `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` | 133, 138-146 | "Configure a **clean** Debug build … DONE" and "Zero warning lines recorded in all three trees" against an incremental build (`build-all-debug.log`: 0 TUs compiled) | ⚠️ Warning | The warning gate at `f54de6e` covers only the four TUs that recompiled in release/asan and nothing in debug |
| `evidence/gates/build-all-{debug,release,asan}.log` | 1-5 | No commit stamp in the file itself; binding is by `PROVENANCE.txt` and mtime only. `08.5-08-SUMMARY.md` says logs without a commit "get a two-line header" — `preflight-versions.log` and `ldd-release.txt` do, these three do not | ℹ️ Info | Detached from a directory listing, a build-all log cannot say which commit it certifies |
| `evidence/gates/{attribution,wakeup,reproducer,flake-measurement}/README.md` | 3-11 | Each opens "That bundle was not produced. Its capture is still held. There is no build-all-debug.log … at the top level of evidence/gates/" — true when written, false now | ℹ️ Info | Frozen round records; `evidence/README.md:84-100` indexes them by their opening line without saying those openings are now historical |
| `evidence/gates/wakeup/README.md`; `08.5-12-SUMMARY.md` | 545-553; 93-102 | Two `blocking-human` checkpoints ruled by the delegated project lead under a standing instruction; `08.5-12-PLAN.md:43` says the task "writes no file until the operator replies" | ⚠️ Warning → human | Governance, not evidence: the rulings are well-founded and transparently recorded; they are not yet the operator's |
| `scripts/start-validation-session.sh` | ~120 | Still prints "including the four API keys you have chosen not to rotate" | ⚠️ Warning (carried) | Unchanged since the old report |
| `docs/RELEASE-NOTES.md` | — | `destroy-window-delay` default 1500 → 400 ms still has no release note (`grep -c` = 0) | ⚠️ Warning (carried) | Unchanged since the old report; the interaction table records the operator asked for it |
| `src/Manager.cpp` | 731-740 | `allocateShadeOf()` still calls `fatal()` on a failed `XAllocNamedColor`, against `include/Border.h`'s "no bevel is the correct degradation" | ⚠️ Warning (carried) | Unchanged since the old report; `:763` documents the opposite policy for the shade path |
| `src/Client.cpp` | 1009 | 400-byte title read vs 256-char config cap | ⚠️ Warning (carried) | Now recorded in `DOC-GUARDS.txt:27-31` as v1.1; not fixed |
| `scripts/stress-exerciser.sh` | 42-47 | Fixed `/tmp` log path | ✓ Fixed | Now `mktemp`; the old report's finding is closed |
| `docs/RELEASE-NOTES.md` / `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` | 346 / 836 | Same dpkg stamp cited as `x2goserver-common:all` in one and `x2goserver:amd64` in the other | ℹ️ Info | Neither false; two names for one fact |

No `TBD`, `FIXME` or `XXX` markers in the files this phase modified (`grep` over `src/`, `include/`,
`tests/`, `scripts/`, `docs/RELEASE-NOTES.md`, the checklist and the evidence tree).

### Human Verification Required

#### 1. Ratify the two delegated rulings

**Test:** Read `evidence/gates/wakeup/README.md:545-575` and `08.5-12-SUMMARY.md:93-102`. Either add the operator's own one-line confirmation beneath each, or reverse them as recorded decisions.
**Expected:** The `ATTRIBUTED` / `FIX-PLAN` ruling and Decision A (08.5-07 superseded; 08.5-08 and 08.5-05 released) carry the operator's word, or are reversed.
**Why human:** The plans define the gate as the operator's reply (`08.5-12-PLAN.md:43`, `08.5-10-PLAN.md:574`). Both records say the operator did not type it. Reading cannot close that.

#### 2. Settle row 23 and recount

**Test:** Confirm whether "The sideways tab looks right" (row 23) was walked (PASS) or covered by the general verdict (PASS (verdict)); then correct the Coverage summary in `INTERACTION-CHECKLIST.md`, and the copies at `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:755`, `.planning/REQUIREMENTS.md:97`, `evidence/README.md:125`.
**Expected:** One consistent split across all four documents that matches the Result column.
**Why human:** The count is mechanical; the label is the tester's.

### Gaps Summary

No gap blocks the phase goal. Both gaps from the 2026-08-30 report are closed on evidence I could
read and, where it was safe, re-run:

- **Gap 1** — `docs/RELEASE-NOTES.md` now carries the four measured statuses, says the four-target
  requirement is met, keeps headless-capture and real-session apart in its own table, and states in
  two subsections what neither new transcript settles. The old report's five artifact issues are
  all gone at HEAD; its two replacement guards read 0 and 0 both in `DOC-GUARDS.txt` at `f54de6e`
  and when I ran them.
- **Gap 2** — a fourteen-file gate bundle exists at the top level of `evidence/gates/`, bound to
  `f54de6e` by `PROVENANCE.txt`; the three build-all logs each record 336/336; ASan records no
  findings; static analysis exits OK; and there is no source diff between that commit and HEAD.
  The checklist cites this bundle by phase-qualified path in every gate row, its test-surface row
  was recomputed and matches my recount to the file, and `evidence/README.md` indexes the bundle and
  ends with a limits section that does not soften what the phase does not establish: X2Go through
  its proxy, anything judged by eye on TightVNC, the eleven deferred interaction rows, latency,
  permanence of any snapshot, and a rate for the flake fix.

What arrived with the closure is a new layer of record drift — a roadmap line naming a superseded
capture, a state file claiming a measurement that is still running, a coverage split that never
matched its table, a summary crediting the bundle to the plan that halted, and a "clean build" row
ticked against an incremental log. None of these fails a success criterion, and each is a
one-line fix. They are listed above with file and line because the phase's own thesis is that a
record which lags its evidence is the failure mode, and a verification report that let them pass
unnamed would be doing the thing the phase was inserted to stop.

The status is `human_needed` rather than `passed` for one reason: two `blocking-human`
checkpoints were closed by the delegated lead. The rulings are well-evidenced and the records are
candid about who took them; they still need the operator's word or the operator's reversal.

---

_Verified: 2026-09-05T16:00:00Z_
_Verifier: Claude (gsd-verifier) — read-only re-verification; no build or test executed_


## Re-check at the final commit (project lead, 2026-09-05T17:10Z)

This report was written at `e244492`, when the gate bundle was bound to `f54de6e` (334 cases / 336
registered). Two review passes and two more captures followed (`evidence/gates/capture-attempts/README.md`,
attempts 4 and 5; `evidence/gates/review-fixes/README.md`): a managed client now has no X border on the
frameless path either (`9e45b48`, RED/GREEN), and the first capture built from empty build directories
exposed four pre-existing `-Wunused-result` warnings in `tests/test_config.cpp`'s shell-outs, replaced
with `std::filesystem` calls (`39de548`). The final bundle is bound to **`39de548`**: all three trees
`337/337`, 149 translation units compiled per tree (recorded in `PROVENANCE.txt`, so the warning gate
is no longer vacuous — this report's finding about attempt 4's incremental debug build is what led to
the empty-directory rule), zero warning lines, static analysis OK, headers agreeing.

Re-run at that commit, all passing: the 14-file inventory; `git diff --name-only 39de548..HEAD -- src
include tests scripts CMakeLists.txt` prints nothing; the checklist's self-computing source and
registered checks (335 cases in 23 files, `Total Tests: 337`) and per-file staleness loop; 13 / 7 / 0
open boxes. The inconsistencies listed above were fixed in commits `4749b1b` and later (interaction
split recounted to 10 / 6 and propagated; 08.5-05's provenance line; the fix-measurement link; historical
notes on the four pre-bundle READMEs; the summary's header-pattern wording; the stray run log untracked).
The two human items stand: the delegated `blocking-human` rulings await the operator's ratification or
reversal, and row 23's label is the tester's to confirm. Score and status unchanged: 7/7, `human_needed`.
