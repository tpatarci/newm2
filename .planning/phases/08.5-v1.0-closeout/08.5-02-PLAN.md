---
phase: 08.5-v1.0-closeout
plan: 2
type: execute
wave: 2
depends_on: ["08.5-01"]
files_modified:
  - COMPILED_CODE_BEHAVIOR_CHECKLIST.md
  - docs/RELEASE-NOTES.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/08.5-v1.0-closeout/evidence/
autonomous: false
requirements: [XDIS-05, TEST-08]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "All four remote-desktop targets named in PROJECT.md have a committed capability transcript captured at a known commit."
    - "The User Interaction Checklist exists as a per-item pass/fail table with a tester name and a date, not as a general verdict."
    - "Both Phase 8 deviations are either retired on evidence or restated with a fresh reason -- neither survives this plan as written."
    - "Every statement in the evidence bundle about what was and was not installed is true at the commit that carries it."
    - "A missing extension on any target is recorded as a measured result, never as a failure of the capture."
  artifacts:
    - .planning/phases/08.5-v1.0-closeout/evidence/tightvnc/
    - .planning/phases/08.5-v1.0-closeout/evidence/x2go-nxagent/
    - .planning/phases/08.5-v1.0-closeout/evidence/INTERACTION-CHECKLIST.md
    - COMPILED_CODE_BEHAVIOR_CHECKLIST.md
  key_links:
    - "`scripts/capture-display-capabilities.sh <display> <label>` -> `evidence/<label>/{capabilities,root-properties,window-tree}.txt`"
    - "the four transcripts -> XDIS-05 met as written, with no requirement amendment"
    - "the per-item table -> TEST-08's last open component -> the unticked rows in COMPILED_CODE_BEHAVIOR_CHECKLIST.md"
  prohibitions:
    - "Do not use -localhost no, or any other flag that exposes a VNC port on the network. The capture script deliberately suggests no server flags (threat T-8-AC) and that must not be worked around here."
    - "Do not read, copy, quote or commit ~/.xsession-errors or any part of it. It holds live credentials in plaintext and the operator has decided not to rotate them, which makes the handling rule MORE binding, not less."
    - "Do not commit a window tree or a screenshot without reading it first (T-8-REMOTE). The validation host also runs the operator's real desktop."
    - "Do not tick a checklist row that was not exercised. An untested row stays unticked with its reason inline -- that convention is why the Phase 8 record survived verification."
    - "Do not treat an absent extension as a capture failure; record it and continue."
    - "Do not leave the system vncserver alternative pointing somewhere the operator did not put it."
    - "Do not fabricate a transcript for a target whose session could not be established. Record the failure and why."
---

<objective>
Close the two requirements that are short only of evidence: capture the two
remaining remote-desktop targets, and turn the interaction record from a verdict
into a table.

Purpose: Phase 8 recorded TightVNC and X2Go as accepted deviations on the
reasoning that validating them cost a human session each. Measurement on
2026-08-30 falsified that for X2Go — `nxagent` is installed and runs headless
nested on an Xvfb, and the whole capture takes about two minutes with nobody
watching (D-8.5-04). TightVNC's server is equally headless. So the cheaper and
more honest move is to validate both rather than to reword the requirement.
Output: four target transcripts, a per-item interaction table, two retired or
restated deviations, and XDIS-05 met as written.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.5-v1.0-closeout/08.5-CONTEXT.md
@.planning/phases/08-xrandr-vnc-compatibility-focus-rules/08-14-SUMMARY.md
@.planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate TightVNC headlessly</name>
  <files>.planning/phases/08.5-v1.0-closeout/evidence/tightvnc/</files>
  <read_first>
    - scripts/capture-display-capabilities.sh (the header's three design points, especially "a missing extension is a RESULT, not a failure" and "no server flags are suggested or applied")
    - COMPILED_CODE_BEHAVIOR_CHECKLIST.md (D-8-TIGHTVNC — the deviation this task exists to close or restate, and its claim that a TigerVNC result is genuine evidence about TightVNC)
    - .planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/tigervnc/capabilities.txt (the comparison baseline — the transcript this one gets diffed against)
  </read_first>
  <action>
**Record the vncserver alternative before installing anything.** `/usr/bin/vncserver` is a Debian alternatives symlink currently pointing at `tigervncserver`, set 2026-08-29. `tightvncserver` registers a competing alternative for the same name, and the operator runs `vncserver :11` for their daily session. Capture `update-alternatives --display vncserver` first, drive this task through the explicit `Xtightvnc`/`tightvncserver` binaries rather than through the generic `vncserver` name, and restore the recorded alternative afterwards. Note the before and after state in the summary — a validation task that silently repoints the operator's daily command has caused a worse problem than the one it solved.

Install `tightvncserver` (`1:1.3.10-5`, verified available). Start a TightVNC server on an unused display — check `/tmp/.X11-unix/` first, which carried 23 sockets on 2026-08-30, so low numbers are not free. **No network-exposing flags.**

Run `scripts/capture-display-capabilities.sh <display> tightvnc`, writing under this phase's evidence directory.

Then run the window manager inside that session with a client, and capture the same three artifacts a real target gets: the startup banner (which reports the detected extensions and the running focus policy), the root EWMH properties, and the window tree. Confirm the WM starts, frames a client, and publishes `_NET_CLIENT_LIST`.

**Expect degradation and record it rather than routing around it.** TightVNC's Unix server is a much older `Xvnc` and may advertise no RANDR and no RENDER. That is the interesting outcome, not the bad one: it exercises the XDIS-02/03/04 fallback paths against a genuinely degraded server instead of against the `WM2_FORCE_NO_*` env vars, and it would make D-8-TIGHTVNC's "substantially overlapping extension set" rationale measurably wrong — which is worth knowing before a user finds it. Diff the extension matrix against the committed TigerVNC transcript and record what differs.

If the WM misbehaves in a way the existing fallbacks do not cover, stop and report it. That grows 8.5 by a fix plan, and that trade was accepted when validation was chosen over amendment.
  </action>
  <verify>
    <automated>test -f .planning/phases/08.5-v1.0-closeout/evidence/tightvnc/capabilities.txt</automated>
  </verify>
  <acceptance_criteria>
    - All three transcript files exist under `evidence/tightvnc/` and each carries the script's provenance header with a commit hash and a clean tree state
    - The extension matrix records a present/absent verdict for SHAPE, RANDR and RENDER — any combination is an acceptable result
    - The WM's startup banner from inside the session is captured, and its extension lines agree with the capture script's matrix
    - `_NET_CLIENT_LIST` is non-empty with a client mapped, and the window tree shows a frame with the client reparented inside it
    - `grep -c 'localhost no\|localhost=0' evidence/tightvnc/*` equals 0
    - `update-alternatives --display vncserver` reports the same selection as it did before this task
    - The window tree was read before commit and contains no title from the operator's real desktop (T-8-REMOTE)
  </acceptance_criteria>
  <done>TightVNC is a measured target rather than an inference from its successor.</done>
</task>

<task type="auto">
  <name>Task 2: Validate X2Go's nxagent headlessly, and correct the record about it (D-8.5-04)</name>
  <files>.planning/phases/08.5-v1.0-closeout/evidence/x2go-nxagent/, .planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/README.md</files>
  <read_first>
    - .planning/phases/08.5-v1.0-closeout/08.5-CONTEXT.md ("What changed since the handoff" — the three measured facts, and the explicit limit on what a nested nxagent proves)
    - COMPILED_CODE_BEHAVIOR_CHECKLIST.md (D-8-X2GO — note its factual claim that "`x2goserver` was never installed on the validation host", which is false as of 2026-08-29 17:43 per `/var/log/dpkg.log`)
    - .planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/README.md (the per-target status table, which repeats the same false claim)
  </read_first>
  <action>
Start an Xvfb, then `nxagent` nested on it as a second display. The invocation that worked on 2026-08-30 was `DISPLAY=:<host> nxagent -R -name <label> -geometry <WxH> :<nested>`; plain `nxagent :<nested>` with no host DISPLAY aborts with `Unable to open display ''`, because nxagent is an X proxy and wants a server to attach to.

Run `scripts/capture-display-capabilities.sh :<nested> x2go-nxagent`, then run the window manager inside it with a client and capture the banner, root properties and window tree, exactly as Task 1 does.

**Write the transcript's own scope limit into `capabilities.txt` as a note, or alongside it.** A nested nxagent on a local Xvfb is not X2Go over a network link with its compression proxy in the path — and that proxy is the layer D-8-X2GO is actually worried about. This transcript is the automated floor: it settles the extension surface and the framing, and it does not settle behaviour under compression and latency. Task 3's human sitting covers the rest. Recording that distinction inside the artifact is what stops a future reader over-reading it, and it is the same discipline the Phase 8 bundle used for its `sanitizer-reports/` note.

**Correct the false statements about the install.** Both D-8-X2GO and `evidence/README.md` state that `x2goserver` was never installed. `/var/log/dpkg.log` stamps `x2goserver:amd64 4.1.0.3-5` at 2026-08-29 17:43 — during Phase 8's own manual-pass window. Fix both, and say what the record now is: the package was present and the session was not run, which is a different and less flattering fact than the one that was written down. Getting this right matters more than it looks: Phase 8's closing lesson was that documentation drifts in exactly the direction that makes the work look more complete than it was.
  </action>
  <verify>
    <automated>test -f .planning/phases/08.5-v1.0-closeout/evidence/x2go-nxagent/capabilities.txt</automated>
  </verify>
  <acceptance_criteria>
    - All three transcript files exist under `evidence/x2go-nxagent/` with the provenance header
    - The extension matrix records SHAPE, RANDR and RENDER verdicts (measured present on 2026-08-30; re-measure, do not copy)
    - The WM's banner from inside the nested session is captured and agrees with the matrix
    - The window tree shows a framed client with the standard `+24+8` client offset inside its frame
    - The transcript carries its own scope limit: nested-local, not network-with-proxy
    - `grep -c 'was never installed' COMPILED_CODE_BEHAVIOR_CHECKLIST.md` equals 0
    - `grep -c 'was never installed' .planning/phases/08-*/evidence/README.md` equals 0
    - The dpkg install timestamp is cited wherever the corrected claim is made, so the correction is checkable rather than asserted
  </acceptance_criteria>
  <done>The one target Phase 8 called untestable-without-a-human is measured, and the sentence that justified skipping it is no longer false.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The interaction checklist as a per-item table, plus real client sessions (TEST-08, D-8.5-05)</name>
  <what-built>
Four remote-desktop targets now have capability transcripts: TigerVNC and XRDP from Phase 8, TightVNC and nxagent captured headlessly in Tasks 1 and 2. Window rules match by title as of plan 08.5-01. What does not exist is the thing TEST-08 actually asks for: the User Interaction Checklist as a **per-item pass/fail record with a tester name and a date**. Two manual passes were run on 2026-08-29 and 2026-08-30; the second produced "all works like a charm", which is a verdict rather than a result per row. The 14 unticked rows in `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` are the honest consequence of that.

This is the last blocking gate before v1.0 closes. It is being asked for after you already declared testing sufficient, and the reason is narrow: that judgement was about *how much* to test, and this is about the *form of the record*. In a project whose stated purpose includes showcasing the process, the evidence bundle is a deliverable in its own right.
  </what-built>
  <how-to-verify>
**One sitting, one table.** Open `evidence/INTERACTION-CHECKLIST.md` (created empty by this task with a row per item) and fill in a result for every row of the checklist's **User Interaction Checklist** section, on **one** target — TigerVNC or XRDP, whichever you already have running. Pass, fail, or not-exercised, with a note where a note helps. Put your name and the date at the top.

The rows are already written; the work is walking them:
the root menu appearing, fitting at screen edges, highlighting and unmapping; the `New` entry in both plain and shell modes; hidden-client restore through the menu row; the exit entry at the lower-right; right-click circulation with zero, one, several and hidden clients; a tab click raising and focusing per policy; a tab drag moving a window; the resize handles including the constrained directions; the tab button's short press to hide and long press to delete; middle-click to maximize; the right-button circular gesture for fullscreen; and pointer grabs releasing after every cancel path.

**Then, if you are willing, two short extras** — each about ten minutes, and each closing something a headless capture cannot:

1. **TightVNC through a real viewer.** Task 1 captured its extension matrix headlessly. Connecting a viewer tells us whether it is usable, which is a different question.
2. **X2Go through the real client.** This is the one that matters most. Task 2's nested nxagent settles the extension surface; it says nothing about behaviour with the compression proxy and network latency in the path, which is precisely what D-8-X2GO was worried about. Menus, drag-move, and the long-press delete are the three that would show it.

If you decline either extra, say so — it is recorded as a scope decision with your name on it, exactly as the X2Go descope was, and the nested transcript stands as the floor.

**A standing caution, now more binding rather than less.** You have decided not to rotate the four keys in `~/.xsession-errors`. That is your call and it is recorded. It also means that file keeps holding live credentials, and this task is the one that puts two more remote sessions on this host — the situation where someone reaches for exactly that file when a session misbehaves. It must not enter the bundle, be quoted, or be pasted into this conversation. If a session fails and you want help diagnosing it, paste the window manager's own stderr, which is safe.

**And on titles:** anything captured here records window names. This workstation also runs your real desktop, with your project names in the window list. Read a tree or a screenshot before it is committed.
  </how-to-verify>
  <resume-signal>Type "approved" when the table is filled in and saved, or tell me which rows failed and on which target. If you are declining either optional session, say which.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Retire the deviations, close the ledger, re-run the gates</name>
  <files>COMPILED_CODE_BEHAVIOR_CHECKLIST.md, docs/RELEASE-NOTES.md, .planning/REQUIREMENTS.md, .planning/phases/08.5-v1.0-closeout/evidence/</files>
  <read_first>
    - COMPILED_CODE_BEHAVIOR_CHECKLIST.md (the "Accepted Deviations" preamble — reason, owner, follow-up, all three or it is "an omission wearing the word accepted"; and both deviations' follow-up clauses, which each say to delete the entry or restate it with a fresh reason)
    - COMPILED_CODE_BEHAVIOR_CHECKLIST.md ("Release Evidence Required" — the `Manual interaction checklist results` row, the last unticked one there)
    - .planning/REQUIREMENTS.md (XDIS-05 at line 56 and TEST-08 at line 97, with their Traceability rows at 180 and 197)
    - docs/RELEASE-NOTES.md (the remote-target table, corrected in `66591ec` and needing another pass now)
    - .planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/README.md (the layout and framing this phase's own evidence index should follow)
  </read_first>
  <action>
**Retire or restate both deviations on the evidence, whichever the results support.** D-8-TIGHTVNC is deleted if TightVNC passed, or restated with what actually failed. D-8-X2GO is the one to be careful with: if Task 3's real client session did not happen, it does **not** get deleted — it gets restated with the true, narrower reason (extension surface and framing measured on nested nxagent; network-with-proxy behaviour not exercised), a named owner, and a follow-up. A deviation that survives the evidence meant to close it must say so plainly.

Update the User Interaction Checklist rows from the filled table. Rows that passed get ticked with a citation to the table; rows that were not exercised stay unticked with the reason inline. Tick the `Manual interaction checklist results` row in Release Evidence Required only if the table has a tester name and a date. Recount the totals — Phase 8's own walk got its arithmetic wrong twice and the verifier caught it both times, so recount rather than adjust.

Update `docs/RELEASE-NOTES.md`'s remote-target table to the four measured statuses, with the nested-versus-real distinction visible for X2Go rather than smoothed into a tick.

Update `.planning/REQUIREMENTS.md`: XDIS-05 to Complete if all four targets have transcripts — **met as written, with no amendment**, which is worth stating because Phase 8 expected to have to reword it. TEST-08 to Complete once the table exists and the transcript count is four. If either falls short, say exactly which component is short, in the style the current entries already use.

Write `evidence/README.md` for this phase, following Phase 8's: the `~/.xsession-errors` warning first because it is the file anyone reaches for, then T-8-REMOTE, then the layout, then a "what this bundle does not establish" section that names the network-with-proxy gap if it is still open.

Then re-run the four hard blockers at the final commit and commit their logs alongside: `bash scripts/gates/build-all.sh` (all three trees) and `bash scripts/analysis/run-static-analysis.sh`, plus the runtime smoke transcript. Phase 8's evidence bundle notes that nothing re-validates a snapshot when later commits land; this phase changed `src/Client.cpp`, `src/Rules.cpp` and `src/Config.cpp`, so the gate snapshot must be retaken rather than inherited.
  </action>
  <verify>
    <automated>bash scripts/gates/build-all.sh &amp;&amp; bash scripts/analysis/run-static-analysis.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/gates/build-all.sh` exits 0 with debug, release and asan all green and the case count equal in all three trees
    - `bash scripts/analysis/run-static-analysis.sh` exits 0
    - `evidence/gates/` for this phase carries the build, ctest, sanitizer and static-analysis logs with a `PROVENANCE.txt` naming the final commit and a clean tree
    - Four target directories exist across the two phases' evidence, one per target named in PROJECT.md
    - `evidence/INTERACTION-CHECKLIST.md` has a tester name, a date, and a result on every row
    - Every remaining deviation has all three of reason, owner and follow-up
    - The checklist's ticked/open totals are recomputed and stated, and the open rows each carry their reason inline
    - `grep -c 'validation pending' docs/RELEASE-NOTES.md` equals 0
    - XDIS-05 and TEST-08 Traceability rows read Complete, or name precisely what is short
    - No file in the bundle contains `API_KEY`, `ACCESS_KEY`, `SECRET`, `TOKEN`, a private-key header or a bearer token — screen before commit, as the Phase 8 XRDP transcript was
  </acceptance_criteria>
  <done>v1.0 closes with every requirement either delivered or carrying a deviation that states its own reason, owner and follow-up — and with the evidence to check both.</done>
</task>

</tasks>

<planner_assumptions>
- **TightVNC installs without disturbing the operator's TigerVNC workflow.** The
  alternatives conflict on `/usr/bin/vncserver` is real and Task 1 handles it
  explicitly, but "handled" is a plan, not a measurement. If the restore does not
  take, the operator's daily `vncserver :11` changes behaviour — flagged as the
  one way this plan can break something the operator uses.
- **A nested nxagent is representative of X2Go's server.** It is the same binary
  X2Go starts, but X2Go starts it through `x2gostartagent` with its own options
  and a proxy in front. Assumed close enough to be the automated floor; explicitly
  assumed NOT close enough to close D-8-X2GO by itself. Flagged for
  `/gsd-verify-work`.
- **The interaction checklist can be walked in one sitting.** Roughly 15 rows on
  one target. If it runs long, the fallback in D-8.5-05 applies: transcribe the
  two existing passes with `verified in pass 1/2` citations and mark the
  remainder not-exercised. That fallback is a worse record and is not the
  starting position.
- **No fix plan is needed.** Assumed both targets run acceptably. If either does
  not, 8.5 grows by a plan; the trade was accepted when validation was chosen
  over amendment (08.5-CONTEXT, open question 3).
</planner_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remote-desktop server → WM | Two new X server implementations, each advertising its own extension set |
| Validation host → committed repository | Transcripts and screenshots carry window titles and, on this host, sit beside a plaintext credential file |
| Package installation → operator's daily workflow | `tightvncserver` competes for the `vncserver` alternative the operator uses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8-XSESSION | Information Disclosure | `~/.xsession-errors` on the validation host | **high** | mitigate | Never read, copied, quoted or committed. The operator has decided against rotation, so the file remains live and the handling rule is the only control left — which makes it binding rather than advisory. Two new sessions in this plan are exactly the trigger for someone to open it. |
| T-8-REMOTE | Information Disclosure | `xwininfo -root -tree`, screenshots | medium | mitigate | Every tree and image read before commit. Captures taken on sessions carrying only test clients where possible. The host also runs the operator's real desktop, which is what makes this non-theoretical. |
| T-8-AC | Elevation of Privilege | VNC server invocation | medium | mitigate | No network-exposing flags, enforced by a prohibition and a grep in Task 1's criteria. The capture script suggests no server flags precisely so none can be copied out of it into a droplet recipe. |
| T-8.5-ALT | Denial of Service | The `vncserver` alternatives link | medium | mitigate | Recorded before install, driven through explicit binaries, restored and re-verified after. The failure mode is the operator's daily command silently changing servers. |
| T-8.5-SUPPLY | Tampering | `tightvncserver` from the Ubuntu archive | low | accept | Distribution package from the configured archive, same trust basis as every other build dependency in this project. No third-party repository is added. |
</threat_model>

<verification>
- Four remote-desktop targets have committed capability transcripts.
- `evidence/INTERACTION-CHECKLIST.md` carries a tester name, a date and a
  per-row result.
- Every surviving deviation has a reason, an owner and a follow-up.
- `bash scripts/gates/build-all.sh` exits 0 in all three trees at the final commit.
- `bash scripts/analysis/run-static-analysis.sh` exits 0.
- No committed file contains a credential pattern.
- `update-alternatives --display vncserver` matches its pre-task state.
</verification>

<success_criteria>
XDIS-05 is met as written — four targets, four transcripts, no amendment — and
TEST-08's interaction record is a per-item table rather than a verdict, with both
Phase 8 deviations retired on evidence or restated with a reason that is true.
</success_criteria>

<output>
Create `.planning/phases/08.5-v1.0-closeout/08.5-02-SUMMARY.md` when done
</output>
