---
phase: 08.5-v1.0-closeout
plan: 5
subsystem: testing
tags: [release-signoff, evidence, catch2, ctest, documentation]

requires:
  - phase: 08.5-v1.0-closeout (plan 08.5-04)
    provides: the gate bundle at commit f54de6e (evidence/gates/*) this plan cites
provides:
  - COMPILED_CODE_BEHAVIOR_CHECKLIST.md with every gate-log citation phase-qualified at this phase's bundle
  - the recomputed test-surface row (334 source cases / 23 files / 336 ctest tests (333 registered Catch2 cases + 3 fixture tests)) with self-computing acceptance criteria
  - User Interaction Checklist rows that cite INTERACTION-CHECKLIST.md's per-row operator decisions
  - the Release Evidence manual-interaction row ticked on the per-item table's existence
  - evidence/README.md as the bundle's index
affects: [release-signoff, evidence-bundle, TEST-08]

actuals:
  tokens: 9500
  tasks: 4
  commits: 0

tech-stack:
  added: []
  patterns: ["phase-qualified evidence citations (`<phase-dir>/evidence/gates/<file>`) instead of ambiguous relative paths", "self-computing checklist rows verified by running the row's own stated commands rather than transcribing a figure"]

key-files:
  created:
    - .planning/phases/08.5-v1.0-closeout/evidence/README.md
  modified:
    - COMPILED_CODE_BEHAVIOR_CHECKLIST.md
    - .planning/phases/08.5-v1.0-closeout/evidence/INTERACTION-CHECKLIST.md

key-decisions:
  - "No brace-expansion citations anywhere in the checklist: `build-all-{debug,release,asan}.log` was expanded to three individual file references everywhere it appeared, because the dangling-path verify's character class does not include `{` and would otherwise report a truncated, unopenable path."
  - "The screenshots row and the runtime-smoke row's Phase-8-inherited paths were qualified at `08-xrandr-vnc-compatibility-focus-rules/evidence/...` rather than repointed at this phase's bundle, since no screenshot was taken and the local-xephyr/tigervnc/xrdp/x11vnc-xvfb transcripts are Phase 8's, not this phase's; this phase's own runtime-smoke/ capture is cited separately."
  - "None of the seven open User Interaction Checklist boxes flipped to ticked. Only the Release Evidence manual-interaction row flipped, per the plan's explicit mapping table."
  - "In INTERACTION-CHECKLIST.md, only the preamble's declined-row-without-coverage count was corrected (seven -> nine, with row numbers #4,#5,#7,#8,#9,#10,#20,#21,#22 listed); no Result value, row, or the 31-row total was touched."

patterns-established:
  - "Gate-log citations in the signoff checklist are always phase-qualified (`<phase-dir>/evidence/gates/<file>`), never a bare `evidence/gates/...` path, and never brace-expanded."

requirements-completed: [TEST-08]

coverage:
  - id: D1
    description: "Every gate row in COMPILED_CODE_BEHAVIOR_CHECKLIST.md cites this phase's bundle by a phase-qualified path, with figures taken from this phase's logs"
    requirement: "TEST-08"
    verification:
      - kind: other
        ref: "grep -cE '`evidence/gates/' COMPILED_CODE_BEHAVIOR_CHECKLIST.md -> 0 (was 16)"
        status: pass
      - kind: other
        ref: "grep -v '^#' COMPILED_CODE_BEHAVIOR_CHECKLIST.md | grep -cF '295/295' -> 0 (was 5)"
        status: pass
      - kind: other
        ref: "grep -c '08.5-v1.0-closeout/evidence/gates/' COMPILED_CODE_BEHAVIOR_CHECKLIST.md -> 34 (>= 9 required)"
        status: pass
      - kind: other
        ref: "dangling-path loop over every cited 08.5-v1.0-closeout/evidence/gates/ path -> no DANGLING: lines"
        status: pass
    human_judgment: false
  - id: D2
    description: "The test-surface row is recomputed by its own three commands at HEAD (334 source cases / 23 files / 336 ctest tests (333 registered Catch2 cases + 3 fixture tests)), not transcribed"
    requirement: "TEST-08"
    verification:
      - kind: other
        ref: "grep -cF \"$(grep -c '^TEST_CASE' tests/*.cpp | awk -F: '{s+=$2} END {print s}') Catch2 test cases\" COMPILED_CODE_BEHAVIOR_CHECKLIST.md -> 1"
        status: pass
      - kind: other
        ref: "ctest --test-dir build/debug -N | tail -1 -> Total Tests: 336; grep -cF 'Total Tests: 336' COMPILED_CODE_BEHAVIOR_CHECKLIST.md -> 1"
        status: pass
      - kind: other
        ref: "per-file staleness loop against grep -c '^TEST_CASE' tests/*.cpp -> no STALE: lines"
        status: pass
    human_judgment: false
  - id: D3
    description: "User Interaction Checklist rows cite INTERACTION-CHECKLIST.md's per-row operator decisions; the Release Evidence manual-interaction row is ticked on the table's existence; totals recomputed as 13 open file-wide / 7 open in User Interaction / 0 open in Release Evidence"
    requirement: "TEST-08"
    verification:
      - kind: other
        ref: "grep -c '^- \\[ \\]' COMPILED_CODE_BEHAVIOR_CHECKLIST.md -> 13 (was 14)"
        status: pass
      - kind: other
        ref: "awk range over User Interaction Checklist | grep -c '^- \\[ \\]' -> 7"
        status: pass
      - kind: other
        ref: "awk range over Release Evidence Required | grep -c '^- \\[ \\]' -> 0 (was 1)"
        status: pass
      - kind: other
        ref: "awk range over User Interaction Checklist | grep -c 'INTERACTION-CHECKLIST.md' -> 12 (>= 7 required)"
        status: pass
    human_judgment: false
  - id: D4
    description: "evidence/README.md exists, leads with the credential warning and the operator's no-rotation decision, names both per-target transcript commits, indexes every bundle artifact, and closes with a limits section"
    requirement: "TEST-08"
    verification:
      - kind: other
        ref: "grep -n 'xsession-errors' evidence/README.md | head -1 -> line 16 (<= 30 required)"
        status: pass
      - kind: other
        ref: "grep -cF -e '9869aea' -e 'f5bbea7' evidence/README.md -> 3 (>= 2 required)"
        status: pass
      - kind: other
        ref: "unindexed-artifact loop over gates, tightvnc, x2go-nxagent, INTERACTION-CHECKLIST.md, STRESS-RESULTS.md -> no UNINDEXED: lines"
        status: pass
      - kind: other
        ref: "grep -c 'does not establish' evidence/README.md -> 3"
        status: pass
    human_judgment: false

duration: unknown
completed: 2026-09-05
status: complete
---

# Phase 08.5-v1.0-closeout Plan 5: Release signoff record repointed at this phase's bundle

**Every gate-log citation in `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` now points at the 08.5-v1.0-closeout bundle at commit `f54de6e` by an individually-named path; the test-surface row is recomputed (334 source cases / 23 files / 336 ctest tests (333 registered Catch2 cases + 3 fixture tests)) rather than carrying an 18-case-stale transcription; and a new `evidence/README.md` indexes the whole bundle.**

## Performance

- **Tasks:** 4 of 4 completed
- **Files modified:** 2 (`COMPILED_CODE_BEHAVIOR_CHECKLIST.md`, `evidence/INTERACTION-CHECKLIST.md`)
- **Files created:** 1 (`evidence/README.md`)
- **Commits:** 0 (project lead commits after review, per instructions)

## Accomplishments

- Repointed every one of the 16 unqualified `evidence/gates/` citations (plus two more introduced by the recompute itself, caught and fixed by re-running the verify) to `08.5-v1.0-closeout/evidence/gates/<file>` — no brace expansion anywhere, so every citation resolves to a single openable file.
- Replaced every stale `295/295` pass figure with this phase's actual figures: `100% tests passed, 0 tests failed out of 336` in all three trees (debug, release, asan), quoted from `evidence/gates/PROVENANCE.txt` and the `build-all-*.log` files it names.
- Qualified the screenshots row and part of the runtime-smoke row at Phase 8's directory (`08-xrandr-vnc-compatibility-focus-rules/evidence/...`) rather than repointing them at this phase's bundle, since no screenshot was taken this phase; added this phase's own `runtime-smoke/` capture as a separate, explicitly-dated entry.
- Recomputed the test-surface row from its own three commands at HEAD: **334 Catch2 test cases across 23 files**, of which **333 are registered with ctest** (`Total Tests: 336` including the three fixture tests: `preflight`, `start_xvfb`, `stop_xvfb`), the arithmetic being 334 − 1 hidden case = 333, + 3 fixtures = 336. Rebuilt the per-file table from scratch rather than patching it.
- Rewrote all twelve User Interaction Checklist rows to cite the numbered rows of `INTERACTION-CHECKLIST.md` that record the operator's actual per-item disposition, without flipping any of the seven open boxes.
- Ticked the single Release Evidence row this phase's artifact closes: "Manual interaction checklist results with tester name and date," now citing the table's tester, date, target, commit and four-value split (9 PASS / 7 PASS (verdict) / 11 DEFERRED / 4 n/a = 31).
- Corrected `INTERACTION-CHECKLIST.md`'s preamble, which had claimed seven DEFERRED rows had no automated coverage; recounted from the Notes column to nine (`#4, #5, #7, #8, #9, #10, #20, #21, #22`), with the two exceptions (`#6`, `#19`) named and why.
- Wrote `evidence/README.md`, mirroring the Phase 8 index's structure: credential warning first (line 16, well within the required first 30), the operator's recorded decision not to rotate the four exposed keys, the credential screen that was run (and its one, non-credential match), T-8-REMOTE, a `gates/` table with one row per bundle file, a "measurement/record directories — NOT gate results" table naming all eleven such directories by their own README's opening line, per-target transcript commits (`9869aea` for `tightvnc/`, `f5bbea7` for `x2go-nxagent/`), the dirty-tree-state convention, and a closing "what this bundle does not establish" section including the ten-run tables' non-rate-proof caveat inherited verbatim from `fix-measurement/README.md`.

## The three recomputed test-surface figures

Run at commit `f54de6e` (this phase's own commit, matching `PROVENANCE.txt`):

```
$ grep -c '^TEST_CASE' tests/*.cpp | awk -F: '{s+=$2} END {print s}'
334

$ ls tests/*.cpp | wc -l
23

$ ctest --test-dir build/debug -N | tail -1
Total Tests: 336
```

Arithmetic: 334 source cases − 1 deliberately hidden case (`[.][wm_resource_calibration]` in `tests/test_wm_resource.cpp`) = 333 registered Catch2 cases, + 3 fixture tests (`preflight`, `start_xvfb`, `stop_xvfb`) = 336 `Total Tests`.

## The recomputed ticked/open totals

- File-wide: `grep -c '^- \[ \]' COMPILED_CODE_BEHAVIOR_CHECKLIST.md` → **13** open (was 14 before this plan).
- User Interaction Checklist section: **7** open, unchanged in count — same seven rows, rewritten reasons.
- Release Evidence Required section: **0** open (was 1).

## The one box that flipped, and why

Only the Release Evidence row "Manual interaction checklist results with tester name and date" moved from `[ ]` to `[x]`. It previously asserted that no per-item table existed; `evidence/INTERACTION-CHECKLIST.md` (written by an earlier plan in this phase) is exactly that table, with a tester (tpatarci), a date (2026-08-30), a target and a commit, covering all 31 rows. No User Interaction Checklist row ticked, because the plan's own mapping table establishes that every one of the seven open rows corresponds to table rows the operator explicitly declined (DEFERRED) or only half-confirmed (the drag/ConfigureNotify row) — ticking any of them would assert something the operator did not do.

## Rows deliberately qualified at the Phase 8 bundle rather than this one

- **Screenshots** (Release Evidence): `08-xrandr-vnc-compatibility-focus-rules/evidence/screenshots/`, because no screenshot was taken in this phase.
- **Runtime smoke transcript** (Release Evidence): the Phase-8 `local-xephyr/` and the three Phase-8 per-target directories (`tigervnc/`, `xrdp/`, `x11vnc-xvfb/`) remain cited at Phase 8's directory as *inherited* evidence; this phase's own capture (`08.5-v1.0-closeout/evidence/gates/runtime-smoke/`) is cited alongside it as a separate, newly-captured entry, with the row stating which is which.

## The recount behind the INTERACTION-CHECKLIST.md preamble correction

The preamble said "the seven DEFERRED rows are the ones where no automated test exists either," but the table has 11 DEFERRED rows and the coverage summary already stated 11 — neither number matched the preamble's "seven." Recounting from the Notes column of all 11 DEFERRED rows:

- **9 rows have no automated coverage of any kind** (their Notes either say so explicitly or are silent about any coverage at all): **#4** (hidden-client restore), **#5** (menu exit item), **#7** (circulation, one client), **#8** (circulation, several normal clients — exercised manually in Phase 8, but manual is not automated), **#9** (circulation, hidden client), **#10** (circulation, transient/dialog), **#20**/**#21** (fullscreen gesture and its noise-rejection half — "zero coverage of any kind"), **#22** (pointer-grab release property).
- **2 rows are the exceptions**, each carrying some automated coverage in its own Notes even though the row itself was declined: **#6** (zero-client circulation — `[wm_circulate]` covers it directly) and **#19** (middle-click maximize — the resulting *state* is covered by `[wm_fsmax]`, though the gesture itself is not).

Corrected the preamble from "seven" to "nine," with the nine row numbers listed explicitly so the figure is checkable. No Result value, row, or the table's 31-row total was altered.

## Task Commits

Not committed. Per instructions, the project lead commits after review; committed by the project lead after review.

## Files Created/Modified

- `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` — Tasks 1-3: gate-row repointing, test-surface recompute, User Interaction / Release Evidence row rewrites.
- `.planning/phases/08.5-v1.0-closeout/evidence/INTERACTION-CHECKLIST.md` — Task 3: preamble recount only.
- `.planning/phases/08.5-v1.0-closeout/evidence/README.md` — Task 4: created.

## Decisions Made

See `key-decisions` in the frontmatter.

## Deviations from Plan

**One self-caught deviation, fixed within this plan's own execution.** Task 2's rewritten "moved since" note introduced a new unqualified citation, `` `evidence/gates/review-fixes/README.md` ``, which regressed Task 1's own verify (`grep -nE '`evidence/gates/' COMPILED_CODE_BEHAVIOR_CHECKLIST.md`, which must print nothing). Caught by re-running all four tasks' verify commands together after finishing Task 4, rather than trusting each task's isolated pass; fixed by qualifying the path to `08.5-v1.0-closeout/evidence/gates/review-fixes/README.md`. No other deviations — the four tasks were executed as the plan specified, including its explicit prohibition against ticking any declined User Interaction row.

## Issues Encountered

While verifying, `git status --porcelain` showed `src/Border.cpp`, `tests/test_eventloop.cpp`, `tests/test_wm_rules.cpp`, `.planning/phases/08.5-v1.0-closeout/evidence/gates/DOC-GUARDS.txt` and `.planning/phases/08.5-v1.0-closeout/evidence/gates/fix-measurement/README.md` as modified. None of these were touched by this plan's execution (confirmed — this plan's `files_modified` is exactly the three files listed above, and no Edit/Write call in this session targeted `src/`, `include/`, `tests/`, or `scripts/`). These are concurrent changes from other work in the same shared worktree, consistent with the git log's recent CodeRabbit-pre-flight-fix commits; they are outside this plan's scope and were left untouched.

## User Setup Required

None.

## Next Phase Readiness

The release signoff document now certifies the tree that ships, with citations a reader can open and figures produced by the document's own stated commands. The success-criterion-7 record half is closed. What remains open by design: eleven declined User Interaction Checklist rows (recorded, not silently absent), one Environment Preflight row (Catch2's `FetchContent` network dependency), one Robustness row (`ignoreBadWindowErrors` scoping), and the two ICCCM/EWMH `PARTIAL` rows — all pre-existing gaps this plan did not touch, per its own scope.

---
*Phase: 08.5-v1.0-closeout*
*Completed: 2026-09-05*

## Repointed to the final capture (project lead, 2026-09-05)

This plan first ran against the bundle at `d08b6e5` (333 cases / 335 registered). The two branch
reviews that followed (CodeRabbit CLI, Codex — `evidence/gates/review-fixes/README.md`) changed
`src/` and `tests/`, adding one `[wm_rules]` case, so the bundle was re-captured at `f54de6e` and
every figure above was re-run there: 334 source cases in 23 files, `Total Tests: 336`, all three
trees `100% tests passed, 0 tests failed out of 336`. The plan's verify commands were re-run after
the repoint and pass (recorded in the closeout's final commit message).
