---
phase: 08.5-v1.0-closeout
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/RELEASE-NOTES.md
autonomous: true
gap_closure: true
requirements: [XDIS-05, RULES-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Every status this phase measured for a remote-desktop target is the status docs/RELEASE-NOTES.md reports to a user for that target."
    - "No sentence in docs/RELEASE-NOTES.md asserts a fact that a file committed under .planning/phases/08.5-v1.0-closeout/evidence/ contradicts."
    - "A reader of docs/RELEASE-NOTES.md and a reader of .planning/REQUIREMENTS.md come away with the same count of measured remote-desktop targets."
    - "Both Phase 8 deviations appear in the shipped notes in the form COMPILED_CODE_BEHAVIOR_CHECKLIST.md now carries them: TightVNC retired on evidence, X2Go restated and narrowed to the NX compression proxy -- neither survives as Phase 8 wrote it."
    - "X2Go is described as partially exercised, not as validated: the nested nxagent floor is visible and the untested proxy layer is named."
    - "The rules-key record that success criterion 2 verified still holds after the edit -- every rule-* key in src/Config.cpp is documented in the notes and every rule-* key in the notes exists in src/Config.cpp."
  artifacts:
    - docs/RELEASE-NOTES.md
  key_links:
    - ".planning/phases/08.5-v1.0-closeout/evidence/tightvnc/capabilities.txt -> the TightVNC status cell in docs/RELEASE-NOTES.md"
    - ".planning/phases/08.5-v1.0-closeout/evidence/x2go-nxagent/capabilities.txt + SCOPE.md -> the X2Go status cell and narrative in docs/RELEASE-NOTES.md"
    - "COMPILED_CODE_BEHAVIOR_CHECKLIST.md:664-758 (D-8-TIGHTVNC retired, D-8-X2GO restated) -> the two narrative subsections in docs/RELEASE-NOTES.md"
    - ".planning/REQUIREMENTS.md:56 (XDIS-05, all four targets measured) -> the target-count paragraph in docs/RELEASE-NOTES.md"
    - "src/Config.cpp isRuleMatchKey -> the 'What you can match on' table in docs/RELEASE-NOTES.md (both directions)"
  prohibitions:
    - "Do not write an acceptance guard whose literal is absent from the file it guards. Every negative grep in this plan was confirmed to print a matching line against the file as it stands today; a guard that already passes measures nothing and is the precise defect this plan closes."
    - "Do not present X2Go as validated. A nested nxagent on a local Xvfb settles the extension surface and settles nothing about X2Go's NX compression proxy over a network link, which is the layer D-8-X2GO is about. The deviation is restated, not deleted."
    - "Do not state or imply that any target was judged by eye. No viewer was attached to the TightVNC session, no screenshot was taken, and the nxagent capture was headless."
    - "Do not read, copy, quote or commit ~/.xsession-errors or any part of it. It holds live credentials in plaintext on this host and the operator has decided not to rotate them, which makes the handling rule more binding rather than less."
    - "Do not quote window titles, host names or user names out of any transcript into the shipped notes (T-8-REMOTE). Extension names, extension counts, server version strings and geometry offsets are the safe payload."
---

<objective>
Bring the shipped user-facing document into line with the evidence this phase
committed. `docs/RELEASE-NOTES.md:303-367` still carries the Phase 8
remote-desktop text verbatim, and five of its statements are contradicted by
transcripts that live in this same phase's evidence directory.

Purpose: this is the whole of failed success criterion 6. The engineering half
of the criterion is done and done well -- `COMPILED_CODE_BEHAVIOR_CHECKLIST.md`
retires D-8-TIGHTVNC on evidence and restates D-8-X2GO with a narrower, true
reason. The document a user actually reads was left at the Phase 8 text while
the signoff file was corrected around it in the same commit range. The result is
that the shipped notes tell a reader TightVNC is untested, in a repository that
contains its transcript.

Output: a rewritten "Remote-desktop support status" section whose every status
is traceable to a committed capability transcript, plus a runnable both-
directions audit proving the edit did not damage the rules-key record that
success criterion 2 verified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.5-v1.0-closeout/evidence/tightvnc/SCOPE.md
@.planning/phases/08.5-v1.0-closeout/evidence/x2go-nxagent/SCOPE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: One claim, end to end -- the TightVNC status cell, corrected from its own transcript</name>
  <files>docs/RELEASE-NOTES.md</files>
  <read_first>
    - `docs/RELEASE-NOTES.md:303-320` -- the section heading, the six-row status table, and the paragraph directly beneath it. Read the TightVNC cell at L314 as it stands; the guard in this task is built to redden against exactly that cell's current text.
    - `.planning/phases/08.5-v1.0-closeout/evidence/tightvnc/capabilities.txt:36-38` -- the `== extension matrix (XDIS-05) ==` block. This is the measurement the corrected cell reports: `EXTENSION SHAPE:  present`, `EXTENSION RANDR:  ABSENT`, `EXTENSION RENDER: ABSENT`.
    - `.planning/phases/08.5-v1.0-closeout/evidence/tightvnc/SCOPE.md` -- the seven extension names, the server string `Xvnc TightVNC-1.3.10` from `tightvncserver 1:1.3.10-5`, the `+21+8` client offset, and the three "What this does NOT establish" bullets the cell must not out-run.
    - `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:669-710` -- D-8-TIGHTVNC as this phase rewrote it. The verifier called this text exemplary; it is the wording the shipped document is being brought into line with, not a second opinion to reconcile.
  </read_first>
  <reversibility rating="reversible">A prose cell in a tracked markdown file. `git revert` restores it and no build artifact or downstream consumer holds a copy.</reversibility>
  <action>
Prove the whole record-propagation path on one table cell before touching the rest of the section. Evidence file -> shipped sentence -> runnable guard, one path, wired end to end.

**Step 1 -- show the guard can fail.** Run the first verify command of this task against the file as it stands and record in the summary that it exits non-zero and prints line 314. This is not ceremony. Plan 08.5-02 Task 4 shipped a guard whose literal had never appeared in this file, so an untouched document scored green and the gap survived a full verification pass. A criterion that cannot fail against the current state is not a criterion.

**Step 2 -- rewrite the TightVNC row of the status table at L314, and nothing else.** The other five rows belong to Task 2. Keep the row's `| **TightVNC** | ... |` shape. The status cell must open with the exact token `**Validated.**` so the row reads uniformly with the TigerVNC and XRDP rows above it, then state, in the document's existing voice:

- the server measured was `TightVNC 1.3.10`, from `tightvncserver 1:1.3.10-5`, started directly and headlessly with no viewer attached and no network-exposing flag;
- it advertises seven extensions in total -- SHAPE present, RANDR absent, RENDER absent;
- the window manager started on it, both fallback ladders announced themselves on stderr, a client was framed, and no X protocol error was logged;
- the transcript is at `.planning/phases/08.5-v1.0-closeout/evidence/tightvnc/`, and that directory's `SCOPE.md` records what the capture does not settle.

This is the phase's headline finding and the cell should carry it as such: TightVNC is the only capture in the project taken against a server that genuinely lacks the extensions Phase 8 built fallbacks for.

**Do not claim anything was judged by eye.** No viewer was attached and no screenshot taken, so whether the core-X11 glyph path *looks* acceptable is unanswered. `SCOPE.md` states that limit and the cell must not exceed it.

**Step 3 -- run the same command again** and confirm it now exits 0, then run the second command and confirm the corrected row is present. The before/after pair is the deliverable of this task as much as the prose is: it demonstrates that a criterion in this plan reddens against the wrong state and greens against the right one, which is the property every remaining task in plans 03, 04 and 05 inherits.
  </action>
  <verify>
    <automated>! grep -v '^#' docs/RELEASE-NOTES.md | grep -nF -e 'Not tested. Deliberately'</automated>
    <fails_when>non-zero exit, and the pipeline prints a numbered line -- the retracted TightVNC verdict is still in the shipped file. Against the file as it stands today this command prints `295:| **TightVNC** | ...` and exits 1; that is the pre-work reading Step 1 must record.</fails_when>
    <automated>grep -c '^| \*\*TightVNC\*\* | \*\*Validated' docs/RELEASE-NOTES.md</automated>
    <fails_when>prints `0` -- the corrected row is absent or its status cell does not open with the uniform token. Prints `0` against the file as it stands today.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| \*\*TightVNC\*\* | \*\*Validated' docs/RELEASE-NOTES.md` prints `1` (prints `0` before this task)
    - `grep -v '^#' docs/RELEASE-NOTES.md | grep -cF 'Not tested. Deliberately'` prints `0` (prints `1` before this task)
    - `grep -cF 'TightVNC 1.3.10' docs/RELEASE-NOTES.md` prints at least `1` -- the exact server version the transcript's provenance names, so the claim is attributable rather than asserted (prints `0` before this task)
    - `grep -cF '08.5-v1.0-closeout/evidence/tightvnc' docs/RELEASE-NOTES.md` prints at least `1` -- the cell points at the transcript it rests on (prints `0` before this task)
    - The three extension verdicts in the cell match `evidence/tightvnc/capabilities.txt:36-38` exactly: SHAPE present, RANDR absent, RENDER absent
    - The cell contains no claim about appearance, legibility, interaction or resolution change -- the four things `tightvnc/SCOPE.md` records as unestablished
    - The summary records both readings of the first command: exit 1 with a printed line before the edit, exit 0 after
  </acceptance_criteria>
  <done>One target's shipped status is derived from its own committed transcript, and the guard that asserts it has been observed failing against the wrong state and passing against the right one.</done>
</task>

<task type="auto">
  <name>Task 2: The remaining status rows and the target-count paragraph</name>
  <files>docs/RELEASE-NOTES.md</files>
  <read_first>
    - `docs/RELEASE-NOTES.md:305-318` -- the intro line naming the stated target set, all six table rows, and the paragraph beneath the table. Read the X2Go cell at L313 and the paragraph at L316-318 before editing; both are guarded by literals confirmed present today.
    - `.planning/phases/08.5-v1.0-closeout/evidence/x2go-nxagent/capabilities.txt:36-38` -- `EXTENSION SHAPE:  present`, `EXTENSION RANDR:  present`, `EXTENSION RENDER: present`, and the `provenance` block naming commit `f5bbea7`.
    - `.planning/phases/08.5-v1.0-closeout/evidence/x2go-nxagent/SCOPE.md` -- the exact invocation, the 23-extension figure, the `+24+8` client offset, and the four specifically-untested items behind the NX proxy.
    - `.planning/REQUIREMENTS.md:56` -- the XDIS-05 entry, which states the requirement is met with all four targets measured. This paragraph is the shipped document's half of that same statement and the two must not disagree.
  </read_first>
  <action>
**Rewrite the X2Go row at L313.** Keep the `| **X2Go** | ... |` shape. The status cell must open with the exact token `**Partially validated.**` -- a token deliberately different from the `**Validated.**` the other four targets carry, because the difference is real and a uniform tick would erase it. The cell then states: `nxagent 3.5.99.26`, the X server X2Go uses, was captured headlessly nested on an Xvfb and advertises SHAPE, RANDR and RENDER (23 extensions); the window manager frames clients on it at the standard `+24+8` client offset; and X2Go's own path -- `x2gostartagent` with an NX compression proxy over SSH -- was not exercised. Point at `.planning/phases/08.5-v1.0-closeout/evidence/x2go-nxagent/` and its `SCOPE.md`.

**Leave the Xvfb, Xephyr, TigerVNC and XRDP rows alone** unless a phrase in them repeats a retracted claim. They were correct and remain correct.

**Replace the paragraph at L316-318 entirely.** Its two assertions -- an exercised count of two, and the four-target requirement being withheld rather than ticked -- are both false as of this phase, and the second one contradicts `.planning/REQUIREMENTS.md:56` on the phase's headline requirement. The replacement must open with the exact token `All four` and state the corrected position: all four stated targets now have a committed capability transcript captured at a known commit; two were exercised in real sessions with a human at the client (TigerVNC, XRDP) and two headlessly (TightVNC, nxagent); the requirement covering compatibility with all four is met on that evidence. Then keep the honesty the old paragraph had, pointed at what is actually short: the headless captures settle the extension surface and the framing, and they settle nothing about how each target feels to use, which is what the two subsections below and `INTERACTION-CHECKLIST.md` cover.

Do not soften the X2Go distinction into the count. The count is four transcripts; it is not four fully exercised targets, and the paragraph should say which two are which.
  </action>
  <verify>
    <automated>! grep -v '^#' docs/RELEASE-NOTES.md | grep -nF -e 'Not tested. An accepted deviation' -e 'Two of the four'</automated>
    <fails_when>non-zero exit, and the pipeline prints one or more numbered lines -- each printed line is a retracted claim still in the shipped file. Against the file as it stands today this prints lines 294 and 297 and exits 1.</fails_when>
    <automated>grep -c '^| \*\*X2Go\*\* | \*\*Partially validated' docs/RELEASE-NOTES.md</automated>
    <fails_when>prints `0` -- either the X2Go row was not rewritten, or it was collapsed into the same status token the fully-measured targets carry. Prints `0` against the file as it stands today.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| \*\*X2Go\*\* | \*\*Partially validated' docs/RELEASE-NOTES.md` prints `1` (prints `0` before this task)
    - `grep -v '^#' docs/RELEASE-NOTES.md | grep -cF 'Not tested. An accepted deviation'` prints `0` (prints `1` before this task)
    - `grep -v '^#' docs/RELEASE-NOTES.md | grep -cF 'Two of the four'` prints `0` (prints `1` before this task)
    - `grep -cF 'All four' docs/RELEASE-NOTES.md` prints at least `1` (prints `0` before this task)
    - `grep -cF 'nxagent 3.5.99.26' docs/RELEASE-NOTES.md` prints at least `1` -- the exact agent version the transcript names (prints `0` before this task)
    - `grep -cF '08.5-v1.0-closeout/evidence/x2go-nxagent' docs/RELEASE-NOTES.md` prints at least `1` (prints `0` before this task)
    - The status table has exactly one row per target named in `.planning/PROJECT.md:59` plus the two development servers, and no row's status cell asserts a target is unexercised
    - The replacement paragraph distinguishes the two real-session targets from the two headless ones rather than reporting a flat count of four
  </acceptance_criteria>
  <done>The status table and the count beneath it report what this phase measured, and the shipped document and the requirements ledger agree on XDIS-05.</done>
</task>

<task type="auto">
  <name>Task 3: The two narrative subsections, rewritten from the checklist's deviation text</name>
  <files>docs/RELEASE-NOTES.md</files>
  <read_first>
    - `docs/RELEASE-NOTES.md:320-367` -- the X2Go subsection, the TightVNC subsection, and the "A note on how compatible should be read" closer. Read all three before editing. The first two are the Phase 8 text verbatim; the third is still accurate and stays.
    - `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:669-710` -- D-8-TIGHTVNC, retired on evidence, including the four-column server comparison table and the "reasoned from ancestry to behaviour" lesson. This is the source of truth for the TightVNC subsection.
    - `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:712-758` -- D-8-X2GO, restated and narrowed, including the two corrections it makes to the Phase 8 text, the owner, and the follow-up. This is the source of truth for the X2Go subsection.
    - `.planning/phases/08.5-v1.0-closeout/evidence/tightvnc/SCOPE.md` and `.planning/phases/08.5-v1.0-closeout/evidence/x2go-nxagent/SCOPE.md` -- the per-target limits that must survive the rewrite into the user-facing voice.
  </read_first>
  <action>
Rewrite both subsections so that the shipped document says what the signoff document already says. The checklist text is the source of truth; this is a translation into the release notes' voice for a user rather than a fresh judgement.

**The X2Go subsection (L320-338).** Its three surviving problems: the claim that the server package was absent from the validation host, which `/var/log/dpkg.log` disproves with an `x2goserver-common:all 4.1.0.3-5` install stamped 2026-08-29 17:43; the bolded closing claim of the third paragraph, that no part of the TigerVNC or XRDP result carries over, which `evidence/x2go-nxagent/capabilities.txt` disproves by measuring the same three extensions present; and the framing of the whole target as unexercised. Replace it with the restated position: the extension surface and the framing are measured on `nxagent 3.5.99.26` nested on a local Xvfb, at about two minutes and no human; what remains untested is X2Go's own path, `x2gostartagent` plus an NX compression proxy over SSH, and specifically menus and drag-move under compression and latency, the long-press delete timing, resize on reconnect at a different geometry, and what the proxy forwards for SHAPE. Say plainly that this is a floor and not a tick, and point at `SCOPE.md`, which travels with the transcript for exactly that reason. Keep the closing "unexercised, not unsupported" guidance -- it is still true and still useful.

**The TightVNC subsection (L340-359).** The heading itself is now wrong: this is not a deviation any more, it is the most informative result in the bundle. Delete the three-bullet rationale block at L345-355 outright -- the first bullet's ancestry-to-behaviour argument is the sentence this phase's own transcript disproved, and the second bullet's proxy claim rests on it. Replace the subsection with the measured result and the lesson the checklist draws from it: TigerVNC 1.12.0 advertises the full set; `TightVNC 1.3.10`, whose Unix server side is from 2009, advertises seven; on the two extensions the retired rationale named by name they do not overlap at all. State what the window manager did about it -- both ladders announced themselves on stderr, the client was framed, no protocol errors, and one visible difference, the client sitting at `+21+8` rather than `+24+8` because the tab width follows the font fontconfig resolves. Then keep the honest remainder: nothing was judged by eye, no interaction was exercised over a real connection, and resolution changes were not tested, which on a server without RANDR is the interesting case.

**Do not reintroduce the retired argument in any form**, including as a hedge or an aside. It is kept in the signoff record at `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` precisely so it does not need to survive here.

**Leave L361-367 alone.** The "one server version, one host, one day" closer is accurate, is the correct caveat for four manual compatibility checks, and needs no change.
  </action>
  <verify>
    <automated>! grep -v '^#' docs/RELEASE-NOTES.md | grep -nF -e 'never installed' -e 'Nothing in the TigerVNC or XRDP' -e 'substantially' -e 'Closest tested proxy'</automated>
    <fails_when>non-zero exit, and the pipeline prints one or more numbered lines -- each is a retracted claim still in the shipped file. Against the file as it stands today this prints lines 302, 312, 326 and 328 and exits 1.</fails_when>
    <automated>grep -cF 'x2gostartagent' docs/RELEASE-NOTES.md</automated>
    <fails_when>prints `0` -- the X2Go subsection does not name the layer that is actually untested, which means the gap has been described in general terms rather than pinned. Prints `0` against the file as it stands today.</fails_when>
    <automated>grep -cF 'SCOPE.md' docs/RELEASE-NOTES.md</automated>
    <fails_when>prints `0` -- neither subsection points the reader at the per-target scope limits that travel with the transcripts. Prints `0` against the file as it stands today.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' docs/RELEASE-NOTES.md | grep -cF 'never installed'` prints `0` (prints `1` before this task)
    - `grep -v '^#' docs/RELEASE-NOTES.md | grep -cF 'Nothing in the TigerVNC or XRDP'` prints `0` (prints `1` before this task)
    - `grep -v '^#' docs/RELEASE-NOTES.md | grep -cF 'substantially'` prints `0` (prints `1` before this task)
    - `grep -v '^#' docs/RELEASE-NOTES.md | grep -cF 'Closest tested proxy'` prints `0` (prints `1` before this task)
    - `grep -cF 'x2gostartagent' docs/RELEASE-NOTES.md` prints at least `1` (prints `0` before this task)
    - `grep -cF 'SCOPE.md' docs/RELEASE-NOTES.md` prints at least `1` (prints `0` before this task)
    - The dpkg install timestamp `2026-08-29 17:43` is cited where the corrected install claim is made, so the correction is checkable rather than asserted
    - The X2Go subsection names all four specifically-untested items from `x2go-nxagent/SCOPE.md`
    - The TightVNC subsection states the seven-versus-full extension comparison and does not reason from shared server ancestry to behaviour anywhere
    - `docs/RELEASE-NOTES.md:361-367` (the "one server version, one host, one day" closer) is byte-identical to its pre-task content
  </acceptance_criteria>
  <done>Both Phase 8 deviations reach the shipped document in the form the signoff record now carries them: one retired on evidence, one restated and narrowed to the layer that is genuinely untested.</done>
</task>

<task type="auto">
  <name>Task 4: RULES-01 -- prove the rules-key record survived the edit, both directions</name>
  <files>docs/RELEASE-NOTES.md</files>
  <read_first>
    - `docs/RELEASE-NOTES.md:191-228` -- the "What you can match on" table and the migration note beneath it. Success criterion 2 verified this section against `src/Config.cpp` in both directions; this task turns that hand audit into a command and re-runs it after three tasks of editing in the same file.
    - `src/Config.cpp:287-318` -- `isRuleMatchKey` and the dispatch beneath it. This is the shipped key set; the notes are checked against it, never the reverse.
    - `docs/RELEASE-NOTES.md:217-221` -- the migration note. It names the removed spelling on purpose, to tell an existing config what to change. That single asymmetry is expected and must not be "fixed" by deleting the note.
  </read_first>
  <action>
This plan rewrites a large block of `docs/RELEASE-NOTES.md`. The rules section sits in the same file and its accuracy is the RULES-01 record that success criterion 2 verified by hand. Prove it is intact rather than assuming an edit 100 lines away could not have touched it.

Run the both-directions audit as a shell command and record its output in the summary:

- every `rule-` key that appears in `src/Config.cpp` also appears in `docs/RELEASE-NOTES.md`;
- every `rule-` key that appears in `docs/RELEASE-NOTES.md` also appears in `src/Config.cpp`, with exactly one expected exception -- the removed spelling named in the migration note at L217-221, which is present in the notes precisely because it is gone from the code.

The audit as written today produces no difference at all, because `src/Config.cpp` still carries the removed spelling in an explanatory comment. Record that reading. If the audit reports a key present on one side only, do not adjust the notes to match a guess: read `isRuleMatchKey` and fix the notes to the shipped key set.

Then sweep the whole file once for every retracted claim this plan removed, as a single command, so no task's edit can have reintroduced another task's defect. That sweep is the plan-level gate and it belongs here, at the end, rather than distributed.
  </action>
  <verify>
    <automated>comm -3 <(grep -oE 'rule-[a-z-]+' src/Config.cpp | sort -u) <(grep -oE 'rule-[a-z-]+' docs/RELEASE-NOTES.md | sort -u)</automated>
    <fails_when>prints any line at all. A line in column 1 is a shipped config key with no documentation; a line in column 2 is a documented key that does not exist in the parser -- the false-promise class this phase was created to remove. Prints nothing against the tree as it stands today.</fails_when>
    <automated>! grep -v '^#' docs/RELEASE-NOTES.md | grep -nF -e 'Not tested. Deliberately' -e 'Not tested. An accepted deviation' -e 'never installed' -e 'Nothing in the TigerVNC or XRDP' -e 'substantially' -e 'Two of the four' -e 'Closest tested proxy'</automated>
    <fails_when>non-zero exit, and the pipeline prints one or more numbered lines -- each printed line is one of the seven retracted claims still present. Against the file as it stands today this prints seven numbered lines and exits 1.</fails_when>
    <automated>grep -c 'rule-match-title' docs/RELEASE-NOTES.md</automated>
    <fails_when>prints `0` -- the rules table lost the key this phase added, which would mean the remote-desktop rewrite damaged the RULES-01 record. Prints a non-zero count against the file as it stands today.</fails_when>
  </verify>
  <acceptance_criteria>
    - The `comm -3` audit prints nothing, and its output is recorded verbatim in the summary
    - The seven-literal sweep exits 0 and prints nothing (prints seven numbered lines and exits 1 before this plan)
    - `grep -c 'rule-match-title' docs/RELEASE-NOTES.md` prints at least `1`
    - `docs/RELEASE-NOTES.md:191-228` is unchanged by this plan except where an audit finding required a correction, and any such correction is named in the summary with the `src/Config.cpp` line it was taken from
    - The migration note naming the removed key spelling is still present -- it is the one intentional asymmetry and deleting it would break an existing user's upgrade path
  </acceptance_criteria>
  <done>The RULES-01 record is provably intact after the remote-desktop rewrite, by a command rather than by a reading, and no retracted claim survives anywhere in the shipped document.</done>
</task>

</tasks>

## Artifacts this phase produces

Produced by this plan (08.5-03):

| Artifact | State after this plan |
|---|---|
| `docs/RELEASE-NOTES.md` -- "Remote-desktop support status" section (L303-367) | Rewritten: six-row status table with four measured target statuses, replacement target-count paragraph, X2Go subsection restated and narrowed, TightVNC subsection replaced by the measured result |
| `docs/RELEASE-NOTES.md` -- "What you can match on" section (L191-228) | Unchanged, and proven unchanged by a both-directions audit against `src/Config.cpp` |

Produced by the rest of this gap-closure set, listed here so the phase's full output is visible from any plan:

| Artifact | Plan |
|---|---|
| `.planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-debug.log` | 08.5-04 |
| `.planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-release.log` | 08.5-04 |
| `.planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-asan.log` | 08.5-04 |
| `.planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-debug.log` | 08.5-04 |
| `.planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-release.log` | 08.5-04 |
| `.planning/phases/08.5-v1.0-closeout/evidence/gates/compiler-asan.log` | 08.5-04 |
| `.planning/phases/08.5-v1.0-closeout/evidence/gates/sanitizer-reports/` | 08.5-04 (only if the ASan run produced files) |
| `.planning/phases/08.5-v1.0-closeout/evidence/gates/static-analysis.log` | 08.5-04 |
| `.planning/phases/08.5-v1.0-closeout/evidence/gates/preflight-versions.log` | 08.5-04 |
| `.planning/phases/08.5-v1.0-closeout/evidence/gates/ldd-release.txt` | 08.5-04 |
| `.planning/phases/08.5-v1.0-closeout/evidence/gates/runtime-smoke/{capabilities,root-properties,window-tree}.txt` | 08.5-04 |
| `.planning/phases/08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt` | 08.5-04 |
| `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` -- gate rows, test-surface row, User Interaction Checklist rows, Release Evidence rows | 08.5-05 |
| `.planning/phases/08.5-v1.0-closeout/evidence/README.md` | 08.5-05 |

## Multi-source coverage audit

Scope of this audit is the gap-closure set (plans 08.5-03, 08.5-04, 08.5-05). Success criteria 1-5 were verified and are deliberately excluded -- re-planning them would be re-litigating settled work.

**GOAL** -- ROADMAP Phase 8.5 success criteria:

| # | Item | Status | Where |
|---|---|---|---|
| 1 | Title rule, EWMH-first read | EXCLUDED -- verified | 08.5-01, criterion 1 VERIFIED |
| 2 | Match keys named for what they compare | EXCLUDED -- verified; record guarded | 08.5-01; guard in 08.5-03 Task 4 |
| 3 | Map-time fold semantic stated in three places | EXCLUDED -- verified | 08.5-01, criterion 3 VERIFIED |
| 4 | Four targets with committed transcripts | EXCLUDED -- verified | 08.5-02, criterion 4 VERIFIED |
| 5 | Interaction checklist as a per-item table | EXCLUDED -- verified | 08.5-02, criterion 5 VERIFIED |
| 6 | Both deviations retired or restated, neither surviving as written | COVERED | 08.5-03 Tasks 1, 2, 3 |
| 7 | Four hard blockers re-run at the final commit | COVERED | 08.5-04 (capture), 08.5-05 (record) |

**REQ** -- phase requirement IDs:

| ID | Record touched | Status | Where |
|---|---|---|---|
| RULES-01 | `docs/RELEASE-NOTES.md` rules section accuracy | COVERED | 08.5-03 Task 4 |
| XDIS-05 | `docs/RELEASE-NOTES.md` remote-target section | COVERED | 08.5-03 Tasks 1, 2, 3 |
| TEST-08 | Gate bundle, interaction rows, evidence index | COVERED | 08.5-04 all tasks; 08.5-05 Tasks 3, 4 |

**RESEARCH** -- no `08.5-RESEARCH.md` exists for this phase. No items.

**CONTEXT** -- `08.5-CONTEXT.md` decisions bearing on the two failed criteria:

| ID | Item | Status | Where |
|---|---|---|---|
| D-8.5-04 | X2Go is validated, not descoped; XDIS-05 met as written | COVERED | 08.5-03 Task 2 (status cell), Task 3 (narrative) |
| D-8.5-06 | Deviations retired on evidence or restated with a fresh reason; neither stays as written | COVERED | 08.5-03 Task 3 |
| Security section | `~/.xsession-errors` never enters the bundle; T-8-REMOTE title screening | COVERED | prohibitions and `<threat_model>` in all three plans; screening criteria in 08.5-04 Task 3 |
| D-8.5-01, D-8.5-02, D-8.5-03, D-8.5-05 | Implemented and verified in 08.5-01 / 08.5-02 | EXCLUDED -- settled | criteria 1, 2, 3, 5 VERIFIED |

**VERIFICATION** -- every `missing[]` entry from `08.5-VERIFICATION.md`:

| Gap | `missing[]` entry | Status | Where |
|---|---|---|---|
| 1 | Rewrite L304-359 from the checklist's D-8-TIGHTVNC / D-8-X2GO text | COVERED | 08.5-03 Tasks 1, 2, 3 |
| 1 | Delete or rewrite the two narrative subsections | COVERED | 08.5-03 Task 3 |
| 1 | Correct the two-of-four / held-unmet paragraph | COVERED | 08.5-03 Task 2 |
| 1 | Replace the vacuous guard with real ones | COVERED | 08.5-03 Tasks 1-4, all criteria confirmed failing against the current file |
| 2 | Run build-all in three trees and static analysis at the final commit; commit logs with PROVENANCE.txt | COVERED | 08.5-04 Tasks 1, 2, 4 |
| 2 | Recompute the test-surface row and per-file table; replace stale gate figures; repoint log paths | COVERED | 08.5-05 Tasks 1, 2 |
| 2 | Write `evidence/README.md` following the Phase 8 layout | COVERED | 08.5-05 Task 4 |
| 2 | Tick the Release Evidence interaction row and update the User Interaction Checklist rows, recounting totals | COVERED | 08.5-05 Task 3 |

No item is MISSING. No item is deferred.

## Flagged assumptions (spec-less edge probe)

No `08.5-SPEC.md` exists, so the deterministic edge probe was run over the phase
requirement IDs instead. It returned three rows, **all `unresolved`**, none
auto-resolved and none dropped. Set-wide equality check for the gap-closure
plan set: 3 probe-surfaced edge items == 0 authored into `must_haves.truths` +
3 surfaced as flagged assumptions (2 here, 1 in each of 08.5-04 and 08.5-05,
the TEST-08 row being the same item recorded against both plans that touch its
record).

| Requirement | Category | Probe | Why it stays unresolved |
|---|---|---|---|
| RULES-01 | concurrency | "If interrupted or run in parallel, what is guaranteed?" | **Assumption, not a verified property.** Rules fold once at manage time (D-8.5-03) and the fold is driven from the single-threaded X event loop, so no concurrent fold is expected. Nothing in this gap-closure set exercises an interrupted or parallel fold, and no test asserts the property. Recorded so a reader does not mistake the absence of a failure for a guarantee. |
| XDIS-05 | unclassified | "unclassified -- review manually" | **Assumption, not a verified property.** The probe could not classify the requirement. Manual reading: what is unresolved is whether a status measured on one server version, on one host, on one day generalises. The shipped notes' closer at L361-367 already states that it does not, and this plan preserves that paragraph byte-for-byte rather than resolving the question. |
| TEST-08 | unclassified | "unclassified -- review manually" | **Assumption, not a verified property.** Recorded against plans 08.5-04 and 08.5-05, which touch the TEST-08 record. See those plans. |

No prohibition candidate was dropped by canon-referral without a breadcrumb: the
generic half of the credential-handling prohibition (secret scanning of committed
artifacts) is canon security and is owned by the existing pre-commit screening
grep; the project-specific half -- this host's `~/.xsession-errors`, which the
operator has decided not to rotate -- is kept as a prohibition above because it
names a specific live file rather than a general practice.

## Assumption-delta decision

The deterministic scan fired on this phase with one `pluralization` signal, on
the term `fallback` in success criterion 1: "`_NET_WM_NAME` preferred, `WM_NAME`
as fallback".

- **Noun that is now primary:** the window's *advertised title*, not either
  specific X property.
- **Decision:** `no-change`. The promotion already happened in plan 08.5-01 and
  is verified. `Client::getWindowTitle()` (`src/Client.cpp:916-921`) is the
  single reader; `m_name` holds the promoted general representation; the two
  properties are details of one code path rather than two parallel primaries.
  Both directions are pinned by tests (#127 for the EWMH direction, #204 for the
  fallback direction). Nothing in this gap-closure set touches that code.
- **Rationale:** this is the shape the checkpoint recommends -- promote the
  general representation, demote the specific one to a variant. It is already in
  place, so the correct action is to record it, not to redo it.

Advisory and non-blocking. No task is added.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Committed evidence -> shipped document | A measurement in a transcript is restated as prose a user relies on; the restatement is unchecked unless a guard checks it |
| Requirements ledger -> shipped document | Two documents state the same requirement's status independently and can drift apart, which is this gap |
| Validation host -> published repository | Transcript excerpts quoted into the notes can carry window titles, host names and user names |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8.5-DOC-01 | Tampering | `docs/RELEASE-NOTES.md` remote-target section | **high** | mitigate | Every status cell traceable to a named transcript path; seven negative greps on the retracted claims, each confirmed to print a matching line against the file as it stands today; positive greps on the corrected row tokens, each confirmed to print `0` today. The gate can fail in both directions. |
| T-8.5-DOC-02 | Repudiation | Release notes versus `.planning/REQUIREMENTS.md:56` | **high** | mitigate | Task 2 replaces the paragraph that withheld the requirement, and its acceptance criteria bind the shipped count to the ledger's. A user and an auditor read the same status. |
| T-8.5-DOC-03 | Tampering | `docs/RELEASE-NOTES.md` rules section | medium | mitigate | Task 4's `comm -3` both-directions audit against `src/Config.cpp` runs after all editing, so a documented key that does not exist cannot survive the edit. |
| T-8-XSESSION | Information Disclosure | `~/.xsession-errors` on the validation host | **high** | mitigate | Never read, copied, quoted or committed. The operator has decided against rotation, so the handling rule is the only remaining control. Carried as a prohibition. |
| T-8-REMOTE | Information Disclosure | Transcript excerpts quoted into the shipped notes | medium | mitigate | Only extension names, extension counts, server version strings, package versions and geometry offsets are quoted. No window tree, no title list, no host or user name. |
</threat_model>

<verification>
- The seven-literal negative sweep over `docs/RELEASE-NOTES.md` exits 0 and prints nothing.
- `grep -c '^| \*\*TightVNC\*\* | \*\*Validated' docs/RELEASE-NOTES.md` prints `1`.
- `grep -c '^| \*\*X2Go\*\* | \*\*Partially validated' docs/RELEASE-NOTES.md` prints `1`.
- `grep -cF 'All four' docs/RELEASE-NOTES.md` prints at least `1`.
- `comm -3 <(grep -oE 'rule-[a-z-]+' src/Config.cpp | sort -u) <(grep -oE 'rule-[a-z-]+' docs/RELEASE-NOTES.md | sort -u)` prints nothing.
- Every extension verdict in the two rewritten cells matches the corresponding `capabilities.txt` extension matrix.
- No committed change quotes a window title, host name or user name.
</verification>

<success_criteria>
Success criterion 6 is closed: both Phase 8 deviations reach the shipped
document in the form the signoff record now carries them -- TightVNC retired on
evidence with its wrong rationale gone, X2Go restated and narrowed to the NX
compression proxy -- and no statement in `docs/RELEASE-NOTES.md` is contradicted
by a file this phase committed.
</success_criteria>

<output>
Create `.planning/phases/08.5-v1.0-closeout/08.5-03-SUMMARY.md` when done.
Record in it: the before and after readings of the Task 1 guard, the `comm -3`
audit output verbatim, and any place where the checklist's deviation text and
the shipped notes were deliberately worded differently.
</output>
