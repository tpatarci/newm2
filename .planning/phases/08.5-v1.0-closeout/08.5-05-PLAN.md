---
phase: 08.5-v1.0-closeout
plan: 5
type: execute
wave: 3
depends_on: ["08.5-04"]
files_modified:
  - COMPILED_CODE_BEHAVIOR_CHECKLIST.md
  - .planning/phases/08.5-v1.0-closeout/evidence/README.md
  - .planning/phases/08.5-v1.0-closeout/evidence/INTERACTION-CHECKLIST.md
autonomous: true
gap_closure: true
requirements: [TEST-08]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Every gate row in COMPILED_CODE_BEHAVIOR_CHECKLIST.md cites a log in this phase's bundle, by a path that names the phase it belongs to."
    - "The test-surface row states the figures produced by the row's own three commands run at HEAD, and the per-file table matches the tree file for file."
    - "Every User Interaction Checklist row that remains open says why, citing the numbered row of INTERACTION-CHECKLIST.md that records the operator's decision."
    - "The Release Evidence row for manual interaction checklist results is ticked, and no row the interaction table records as deferred or n/a is ticked anywhere in the checklist."
    - "The checklist's ticked and open totals are recomputed from the file rather than adjusted, and the recomputed figures are stated."
    - ".planning/phases/08.5-v1.0-closeout/evidence/README.md exists, opens with the credential-handling warning, and carries a section naming what the bundle does not establish."
  artifacts:
    - COMPILED_CODE_BEHAVIOR_CHECKLIST.md
    - .planning/phases/08.5-v1.0-closeout/evidence/README.md
  key_links:
    - ".planning/phases/08.5-v1.0-closeout/evidence/gates/* -> the Build And Test Gates rows and the Release Evidence rows in COMPILED_CODE_BEHAVIOR_CHECKLIST.md"
    - "grep -c '^TEST_CASE' tests/*.cpp and ctest -N -> the test-surface row's stated figures and per-file table"
    - ".planning/phases/08.5-v1.0-closeout/evidence/INTERACTION-CHECKLIST.md rows 1-31 -> the User Interaction Checklist rows and the Release Evidence interaction row"
    - "evidence/README.md -> the index a reader reaches before opening any transcript in the bundle"
  prohibitions:
    - "Do not tick a checklist row whose interaction-table counterpart is DEFERRED, n/a, or PASS (verdict) where the row demands a walked result. Eleven of the table's thirty-one rows are deferred by explicit operator decision and every one of them stays open with its reason inline. Ticking them would make the release record say the opposite of what the operator said."
    - "Do not transcribe a test count from 08.5-VERIFICATION.md, from a summary, or from this plan. Run the row's own three commands and state what they print. The row carries a bolded instruction against transcription because the figure it carried rotted once already."
    - "Do not repoint a row at this phase's bundle when the evidence it cites genuinely belongs to Phase 8. Qualify it with the Phase 8 directory instead. A row that points at the wrong bundle is the same defect as a row that points at no bundle."
    - "Do not read, copy, quote or commit ~/.xsession-errors or any part of it. The evidence index written in this plan warns about that file; the warning must not be illustrated with its contents."
    - "Do not describe the bundle as establishing anything the per-target SCOPE.md files say it does not. The index is the document most likely to be skimmed instead of the transcripts, so it inherits their limits rather than smoothing them."
---

<objective>
Repoint the release signoff record at the evidence this phase produced, and
write the index that makes the bundle readable.

Purpose: this is the record half of failed success criterion 7. Plan 08.5-04
captures the gates; without this plan they sit in a directory nothing cites,
while every gate row in `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` still certifies
Phase 8's snapshot -- taken before five source files changed. The row that says
in bold that its figures must be recomputed rather than transcribed was
transcribed, and is stale by eighteen test cases. And the Release Evidence row
that gates the manual interaction record still asserts that a per-item table
does not exist, in a phase that created, filled and committed one.

Output: a checklist whose every gate row cites this phase's bundle at this
phase's commit, a test-surface row recomputed by its own commands, User
Interaction Checklist rows that reflect the operator's actual per-row decisions,
and an `evidence/README.md` following the Phase 8 layout.

Sequencing: depends on 08.5-04 because a row cannot cite a log that does not
exist, and the test-surface recompute needs the configured `build/debug` tree the
gate run produces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.5-v1.0-closeout/evidence/INTERACTION-CHECKLIST.md
</context>

<!-- planner-discipline-allow: evidence/gates/ -->

<tasks>

<task type="auto">
  <name>Task 1: Repoint every gate row at this phase's bundle</name>
  <files>COMPILED_CODE_BEHAVIOR_CHECKLIST.md</files>
  <read_first>
    - `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:130-241` -- the whole "Build And Test Gates" section. Ten rows cite gate logs by an unqualified relative path and four of them state a pass figure taken from the Phase 8 snapshot. Read every row before editing any of them; the section also carries the test-surface row, which belongs to Task 2 and must be left alone here.
    - `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:74-95` -- the Environment Preflight rows, two of which cite the preflight versions log.
    - `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:552-560` and `:589-596` -- the Robustness and Remote Desktop rows that cite the ASan log and the preflight log.
    - `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:632-661` -- "Release Evidence Required". Eight rows, seven of which cite gate artifacts. The eighth, the manual interaction row, belongs to Task 3.
    - `.planning/phases/08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt` -- written by plan 08.5-04. The commit, the branch, the per-gate exit codes and the per-tree test totals every rewritten row quotes come from here and from the logs it names.
    - `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:596-609` -- the X2Go remote-desktop row, for the phase-qualified path convention this file already uses three times.
  </read_first>
  <precondition>`.planning/phases/08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt` exists and is non-empty, and the twelve-file bundle inventory from plan 08.5-04 Task 4 passes. Rewriting rows against a bundle that is not yet complete produces citations to files that do not exist.</precondition>
  <reversibility rating="reversible">Prose rows in a tracked markdown file, restorable by `git revert`. The underlying gate artifacts are unaffected.</reversibility>
  <action>
Every gate row in this file currently points at a log by an unqualified relative path, and the same relative path exists in two phases' bundles. That ambiguity is how a signoff document came to certify a tree that no longer exists.

**Qualify every gate-log path with the phase directory it lives in.** The file already uses that convention three times for the remote-desktop transcripts, so follow its own precedent: `08.5-v1.0-closeout/evidence/gates/build-all-debug.log` and its siblings for the rows this phase re-ran, and `08-xrandr-vnc-compatibility-focus-rules/evidence/...` for rows whose evidence genuinely belongs to Phase 8 and was not re-produced -- the screenshots row is the clear case, since no screenshot was taken in this phase. A row that points at the wrong bundle is exactly as wrong as one that points at no bundle, so decide each row on what it actually cites rather than repointing the section wholesale.

**Replace every stated pass figure with the figure this phase's log reports.** The four rows carrying a test-count claim, and the two carrying it a second time in the Robustness and Release Evidence sections, all quote the Phase 8 snapshot's number. Take the replacement from the corresponding `08.5-v1.0-closeout/evidence/gates/build-all-<tree>.log`, not from `PROVENANCE.txt`'s summary and not from any figure in this plan -- the log is the primary record and the summary is a copy of it.

**Replace every reference to the Phase 8 snapshot commit** in a gate row with the commit `PROVENANCE.txt` records. The one occurrence that survives legitimately is inside the test-surface row, and Task 2 owns that.

**The runtime smoke row** in Release Evidence cites the nested-server transcript. It now has four per-target directories plus this phase's `08.5-v1.0-closeout/evidence/gates/runtime-smoke/`, captured at the gate commit rather than inherited. Say which is which.

**Leave the row bodies' reasoning intact** where it is still true. This is a repointing pass, not a rewrite: the ASan row's explanation of why the sanitizer report files are accounting rather than findings, the static-analysis row's note that the cppcheck baseline is keyed on `(id, fileName)` because cppcheck 2.7 never computes the hash element -- both are still correct and both are worth more than the figures around them. If this phase's ASan run produced no report files at all, say so plainly and drop the sentence that counts six of them, rather than leaving a count that no longer has files behind it.
  </action>
  <verify>
    <automated>! grep -nE '`evidence/gates/' COMPILED_CODE_BEHAVIOR_CHECKLIST.md</automated>
    <fails_when>non-zero exit, and the command prints one or more numbered lines -- each is a gate-log citation that does not name the phase whose bundle it means. Against the file as it stands today this prints sixteen lines and exits 1.</fails_when>
    <automated>! grep -v '^#' COMPILED_CODE_BEHAVIOR_CHECKLIST.md | grep -nF -e '295/295'</automated>
    <fails_when>non-zero exit, and the pipeline prints one or more numbered lines -- a Phase 8 pass figure survives in a row this phase re-ran. Against the file as it stands today this prints five lines and exits 1.</fails_when>
    <automated>grep -c '08.5-v1.0-closeout/evidence/gates/' COMPILED_CODE_BEHAVIOR_CHECKLIST.md</automated>
    <fails_when>prints `0` -- no row cites this phase's bundle at all, so the repointing did not happen. Prints `0` against the file as it stands today.</fails_when>
    <automated>for f in $(grep -oE '08\.5-v1\.0-closeout/evidence/gates/[A-Za-z0-9._/-]+' COMPILED_CODE_BEHAVIOR_CHECKLIST.md | sort -u); do test -e ".planning/phases/$f" || echo "DANGLING: $f"; done</automated>
    <fails_when>prints any `DANGLING:` line -- a row cites a file that is not in the bundle, or cites it with brace expansion so the extracted path is a truncated prefix rather than a file a reader can open.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE '`evidence/gates/' COMPILED_CODE_BEHAVIOR_CHECKLIST.md` prints `0` -- every gate-log citation names its phase (prints `16` before this task)
    - `grep -v '^#' COMPILED_CODE_BEHAVIOR_CHECKLIST.md | grep -cF '295/295'` prints `0` (prints `5` before this task)
    - `grep -c '08.5-v1.0-closeout/evidence/gates/' COMPILED_CODE_BEHAVIOR_CHECKLIST.md` prints at least `9` -- one per gate artifact this phase produced (prints `0` before this task)
    - The dangling-path check prints nothing, and no citation uses brace expansion: every cited log is named as an individual file
    - Every replacement pass figure is quoted from `08.5-v1.0-closeout/evidence/gates/build-all-<tree>.log`, and the summary states which log each came from
    - The screenshots row is qualified with the Phase 8 directory, not repointed at this phase's bundle -- no screenshot was taken in this phase
    - If the ASan run produced no sanitizer report files, no row asserts a count of them
    - The ASan row's accounting-versus-findings explanation and the static-analysis row's baseline-keying note both survive the edit
  </acceptance_criteria>
  <done>Every gate row in the signoff document points at a file in this phase's bundle, at this phase's commit, and a reader can open each cited path.</done>
</task>

<task type="auto">
  <name>Task 2: Recompute the test-surface row and its per-file table</name>
  <files>COMPILED_CODE_BEHAVIOR_CHECKLIST.md</files>
  <read_first>
    - `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:157-202` -- the whole test-surface row: the three stated figures, the bolded instruction, the three commands the row supplies for refreshing itself, the per-file table, the note about which files moved and why, and the closing paragraph explaining that a drifted count is a prompt rather than automatically a defect. Read all of it. The row is unusually long because it is the row that rotted, and the explanation is the part worth keeping.
    - `tests/` directory listing -- the row states a file count alongside the case count, and files have been added since it was written.
    - `.planning/phases/08.5-v1.0-closeout/evidence/gates/build-all-debug.log` -- the ctest summary from this phase's gate run, which must agree with the `ctest -N` figure this task computes.
  </read_first>
  <precondition>`build/debug` exists and is configured with tests enabled -- plan 08.5-04 Task 1's debug gate leaves it that way. `ctest --test-dir build/debug -N` is one of the row's own three refresh commands and cannot run without it.</precondition>
  <action>
Run the row's own three commands and write down what they print. Do not take a figure from the verification report, from a summary, or from this plan: the row carries a bolded instruction against transcription because the figure it once held was taken two phases before it was last read, omitted three whole test files and understated a fourth.

The three commands the row supplies are the source-case total, the per-file breakdown, and the number ctest actually registered -- which is the number that gates a release, since a case that silently stopped being registered is invisible to the source count.

Then rewrite the row:

- the source-case total and the number of files it spans, both computed;
- the registered-test total from ctest, and the arithmetic linking the two: the difference is the three fixture tests plus the one deliberately hidden case;
- the commit the recompute was done at, taken from `PROVENANCE.txt`;
- the per-file table, rebuilt from the per-file breakdown rather than patched. Three rows are stale as of HEAD and patching a table row by row is how it drifted in the first place;
- the "moved since" note, rewritten to name the files whose counts changed in this phase and the plan that changed them, replacing the Phase 8 note about the two files that moved then.

**Keep the hidden case's explanation.** One case is not registered on purpose -- the `[.][wm_resource_calibration]` case in `tests/test_wm_resource.cpp`, hidden so that the judged resource budget cannot have been produced by a run that was itself being judged. That sentence explains an arithmetic discrepancy a future reader would otherwise treat as a defect, and it is the most valuable line in the row.

**Keep the bolded instruction and the three commands.** They are what makes the row self-refreshing, and this task is the second time they have paid for themselves.
  </action>
  <verify>
    <automated>grep -cF "$(grep -c '^TEST_CASE' tests/*.cpp | awk -F: '{s+=$2} END {print s}') Catch2 test cases" COMPILED_CODE_BEHAVIOR_CHECKLIST.md</automated>
    <fails_when>prints `0` -- the stated source-case total is not the total the row's own first command produces at HEAD. This is a self-computing check: it recomputes the truth and looks for it in the file, so it cannot be satisfied by a stale number and does not need this plan to name the right one. Prints `0` against the file as it stands today.</fails_when>
    <automated>grep -cF "Total Tests: $(ctest --test-dir build/debug -N | tail -1 | grep -oE '[0-9]+$')" COMPILED_CODE_BEHAVIOR_CHECKLIST.md</automated>
    <fails_when>prints `0` -- the stated registered-test total is not what ctest reports at HEAD, or `build/debug` is not configured so the inner command produced nothing to compare against. Prints `0` against the file as it stands today.</fails_when>
    <automated>grep -c '^TEST_CASE' tests/*.cpp | sed 's|^tests/||' | while IFS=: read -r f n; do grep -q "\`$f\` | $n " COMPILED_CODE_BEHAVIOR_CHECKLIST.md || echo "STALE: $f=$n"; done</automated>
    <fails_when>prints any `STALE:` line -- the per-file table disagrees with the tree for that file. Against the file as it stands today this prints three lines, one per file this phase's test work touched.</fails_when>
    <automated>! grep -v '^#' COMPILED_CODE_BEHAVIOR_CHECKLIST.md | grep -nF -e '293 Catch2' -e '292 are registered' -e 'Total Tests: 295'</automated>
    <fails_when>non-zero exit, and the pipeline prints one or more numbered lines -- a stale figure survives in the row that carries a bolded instruction against transcribing figures. Against the file as it stands today this prints three lines and exits 1.</fails_when>
  </verify>
  <acceptance_criteria>
    - The self-computing source-case check prints `1`
    - The self-computing registered-test check prints at least `1`
    - The per-file staleness loop prints nothing (prints three `STALE:` lines before this task)
    - The three stale figures are gone (`293 Catch2`, `292 are registered`, `Total Tests: 295` each print `0`)
    - The row states the file count alongside the case count, and the file count matches `ls tests/*.cpp | wc -l`
    - The row states the commit the recompute was done at, and it matches the commit in `08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt`
    - The arithmetic linking the source total to the registered total is stated and correct
    - The bolded recompute instruction, the three refresh commands, the hidden-case explanation and the closing "a drifted count is a prompt" paragraph all survive
    - The "moved since" note names the files whose counts changed in this phase and the plan that changed them
  </acceptance_criteria>
  <done>The row that rotted once is accurate at HEAD, and every figure in it was produced by running the row's own commands in this session.</done>
</task>

<task type="auto">
  <name>Task 3: Update the interaction rows from the filled table, and recount</name>
  <files>COMPILED_CODE_BEHAVIOR_CHECKLIST.md, .planning/phases/08.5-v1.0-closeout/evidence/INTERACTION-CHECKLIST.md</files>
  <read_first>
    - `.planning/phases/08.5-v1.0-closeout/evidence/INTERACTION-CHECKLIST.md` -- all of it. The three-value Result vocabulary and why the values are kept apart, all 31 numbered rows with their notes, the three findings from the sitting, and the coverage summary. This table is the input to every edit in this task; the checklist rows are its output.
    - `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:409-486` -- the twelve User Interaction Checklist rows, five ticked and seven open. Read each open row's current reason: several attribute the gap to the *form* of the Phase 8 second manual pass, which is no longer the reason.
    - `COMPILED_CODE_BEHAVIOR_CHECKLIST.md:651-658` -- the Release Evidence row for manual interaction results. It is the release gate for exactly the artifact this phase created, and it currently asserts that artifact does not exist.
  </read_first>
  <precondition>`.planning/phases/08.5-v1.0-closeout/evidence/INTERACTION-CHECKLIST.md` carries a tester name and a date at the top. The Release Evidence row is ticked on the presence of those two things; without them the row stays open regardless of how many table rows are filled.</precondition>
  <action>
Map the table onto the checklist row by row. This is a mapping exercise with a specific and slightly uncomfortable result, so the result is stated here rather than left to be discovered: **none of the seven open User Interaction Checklist rows flips to ticked.** Each one corresponds to table rows the operator explicitly declined, and the eleven declined rows are the honest gap in this release. What changes is not the box but the reason inside it.

**Rewrite each open row's reason** to cite the numbered table row that now records its disposition, the operator's decision, the tester and the date -- replacing the current reasoning, which in several rows blames the shape of a Phase 8 manual pass. That explanation was true when it was written and is not true now: a per-item record exists, it covers these rows, and what it records is a decision rather than an absence. The distinction matters because the two point at different follow-ups. An unrecorded row needs a record; a declined row needs the v1.1 gesture-coverage work.

The mapping, so the executor is not re-deriving it and cannot silently drop a row:

| Checklist row | Table rows | Disposition |
|---|---|---|
| Hidden-client entries restore through the menu row | 4 | declined; no automated coverage either |
| Menu exit item at the lower-right edge | 5 | declined; no automated coverage either |
| Right-click circulation across five client permutations | 6, 7, 8, 9, 10 | declined; zero-client case automated, several-client case exercised in the Phase 8 passes |
| Dragging a tab moves a window and sends a correct synthetic ConfigureNotify | 12, 13 | the move is walked and passes; the synthetic event is still unasserted, so the row stays open on that half alone |
| Middle-click tab toggles maximize | 19 | declined at the gesture; the state is automated |
| Right-button circular gesture toggles fullscreen | 20, 21 | declined; the function has no coverage of any kind |
| Pointer grabs released after every cancel path | 22 | declined; no coverage as a property |

**The five already-ticked rows** get a citation to the table rows that confirm them, and no other change. Two of them close defects the operator themselves filed in Phase 8 -- the drag clamp and the widened close-button target -- and were confirmed fixed by the person who filed them, which is worth a sentence.

**Tick the Release Evidence row for manual interaction results.** The table exists, has a tester name and a date, and covers every row. Replace the row's body -- which currently asserts the artifact's absence -- with the citation: tester, date, target and commit from the table's header, the four-value split, and the total. Then say what it does not close: eleven rows declined, nine of them gestures, which is the v1.1 backlog line this table is the evidence for.

**Recount, do not adjust.** Phase 8's own checklist walk got its arithmetic wrong twice and the verifier caught it both times. Count the boxes with a command and state the recomputed totals. Exactly one box in the whole file flips in this task.

**One correction inside the table itself.** Its preamble states a number of declined rows that disagrees with both the table body and its own coverage summary. Recount from the Notes column how many declined rows have no automated coverage of any kind, list those row numbers explicitly so the figure is checkable, and state that number in the preamble. Do not simply copy the coverage summary's total across -- the two sentences are counting different things, which is how they came to disagree.
  </action>
  <verify>
    <automated>grep -c '^- \[ \]' COMPILED_CODE_BEHAVIOR_CHECKLIST.md</automated>
    <fails_when>prints anything other than `13`. Prints `14` before this task; a `12` or lower means a row was ticked that the interaction table records as declined, which is the failure this task is most at risk of.</fails_when>
    <automated>awk '/^## User Interaction Checklist/,/^## ICCCM/' COMPILED_CODE_BEHAVIOR_CHECKLIST.md | grep -c '^- \[ \]'</automated>
    <fails_when>prints anything other than `7`. The eleven declined table rows map onto exactly these seven checklist rows and none of them was exercised, so any other number means a box moved that the operator's own decisions do not support.</fails_when>
    <automated>awk '/^## Release Evidence Required/,/^## Accepted Deviations/' COMPILED_CODE_BEHAVIOR_CHECKLIST.md | grep -c '^- \[ \]'</automated>
    <fails_when>prints anything other than `0`. Prints `1` before this task -- the manual interaction row, the last open row in the section.</fails_when>
    <automated>awk '/^## User Interaction Checklist/,/^## ICCCM/' COMPILED_CODE_BEHAVIOR_CHECKLIST.md | grep -c 'INTERACTION-CHECKLIST.md'</automated>
    <fails_when>prints fewer than `7` -- at least one open row still explains itself without citing the table row that now records its disposition. Prints `0` against the file as it stands today.</fails_when>
    <automated>! grep -v '^#' COMPILED_CODE_BEHAVIOR_CHECKLIST.md | grep -nF -e 'What does NOT exist is a' -e 'general verdict rather than a per-item result'</automated>
    <fails_when>non-zero exit, and the pipeline prints one or more numbered lines -- a row still asserts the interaction table does not exist, or still attributes an open row to the form of a Phase 8 manual pass. Against the file as it stands today this prints two lines and exits 1.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[ \]' COMPILED_CODE_BEHAVIOR_CHECKLIST.md` prints `13` (prints `14` before this task) -- exactly one box flips
    - The User Interaction Checklist section still has exactly `7` open rows, and each cites `INTERACTION-CHECKLIST.md` with the numbered row that records its disposition
    - The Release Evidence section has `0` open rows, and its manual interaction row names the tester, the date, the target, the commit, the four-value split and the total from the table's header and summary
    - The five already-ticked User Interaction rows each cite the table rows confirming them, and none of their boxes changed
    - The recomputed ticked and open totals are stated in the summary, computed by command rather than adjusted from the previous figures
    - In `INTERACTION-CHECKLIST.md`, the preamble's count of declined rows without automated coverage agrees with the row numbers listed beside it, and those row numbers agree with the Notes column of the table body
    - `.planning/phases/08.5-v1.0-closeout/evidence/INTERACTION-CHECKLIST.md` is otherwise unchanged: no Result value is altered, no row is added or removed, and the total stays `31`
  </acceptance_criteria>
  <done>The signoff record says what the operator actually decided, row by row, and the release gate for the interaction table is closed on the table's existence rather than left open against it.</done>
</task>

<task type="auto">
  <name>Task 4: Write the evidence index for this phase's bundle</name>
  <files>.planning/phases/08.5-v1.0-closeout/evidence/README.md</files>
  <read_first>
    - `.planning/phases/08-xrandr-vnc-compatibility-focus-rules/evidence/README.md` -- the whole file. The layout to mirror: the snapshot caveat, the correction to that caveat about per-target transcripts carrying their own commits, the credential warning under its own "read this before adding anything" heading, T-8-REMOTE, the per-section tables, the caveat that would otherwise be misread, and the closing "what this bundle does not establish".
    - `.planning/phases/08.5-v1.0-closeout/evidence/tightvnc/SCOPE.md` and `.planning/phases/08.5-v1.0-closeout/evidence/x2go-nxagent/SCOPE.md` -- the per-target limits. The index inherits them rather than summarising them away.
    - `.planning/phases/08.5-v1.0-closeout/evidence/STRESS-RESULTS.md` -- part of this bundle and currently indexed nowhere.
    - `.planning/phases/08.5-v1.0-closeout/evidence/gates/PROVENANCE.txt` -- the commit, the exit codes and the totals the index's gates table quotes.
    - `.planning/phases/08.5-v1.0-closeout/08.5-CONTEXT.md` security section -- the `~/.xsession-errors` situation and the operator's recorded decision not to rotate, which changes the wording of the warning relative to Phase 8's.
  </read_first>
  <precondition>Every artifact the index describes is already on disk -- the gates bundle from plan 08.5-04, both target transcripts and their `SCOPE.md` files, `INTERACTION-CHECKLIST.md` and `STRESS-RESULTS.md`. An index written first would describe intentions.</precondition>
  <action>
Write `.planning/phases/08.5-v1.0-closeout/evidence/README.md` following the Phase 8 index's structure, because a reader who has seen one bundle should be able to navigate the other without re-learning it.

**Lead with the credential warning**, under its own heading, before the layout and before anything else. It goes first for the same reason it goes first in the Phase 8 index: that file is the first thing anyone reaches for when an X session misbehaves, and this bundle exists because two more remote-desktop sessions were run. One thing changed since the Phase 8 wording and must be stated: the operator has decided the four keys will not be rotated. That is their call, it is recorded rather than re-argued, and it makes the handling rule the only remaining control -- so the warning is more binding here, not less. State the rule -- the file must never be read, copied, quoted or committed -- and state that the transcripts in this bundle were screened before commit, with the screen that was run.

**Then T-8-REMOTE**, in the Phase 8 index's terms: `xwininfo -root -tree` records every window title, this bundle's captures were taken on displays started for their runs, and anything added later must be read before it is committed.

**Then the layout**, as tables:

- `gates/` -- one row per file, what it is, and which blocker or Release Evidence row it satisfies. State the results at this snapshot, quoted from `PROVENANCE.txt`: the per-tree totals and every gate's exit code. If the ASan run produced no sanitizer report files, say so and say why that is a result rather than a missing artifact.
- the per-target transcripts -- `tightvnc/` and `x2go-nxagent/`, each with a one-line status and a pointer to its `SCOPE.md`. TightVNC is the degraded-server result and the reason this bundle is worth reading; x2go-nxagent is a floor and not a tick.
- `INTERACTION-CHECKLIST.md` -- the per-item table, its tester, date and target, and the four-value split.
- `STRESS-RESULTS.md` -- the churn measurement, and what it answers that the enforced resource budget does not.

**Reproduce Phase 8's correction to its own framing, for this bundle.** The `gates/` material is all from one commit; the two per-target transcripts are not. Each stamps its own commit in its provenance header -- `tightvnc/` and `x2go-nxagent/` were captured earlier in the phase, before the release-notes correction landed. Say so, and name each one's commit. And record the one thing a reader cannot otherwise recover: both transcripts stamp their tree state as dirty, as all four Phase 8 transcripts do, so the exact source state behind a transcript cannot be reconstructed from its commit hash alone. That is the established convention here rather than a regression in this phase, and it is worth one sentence so nobody has to work it out twice.

**Close with "what this bundle does not establish".** Name, at minimum: X2Go over a real connection with the NX compression proxy in the path, which is what D-8-X2GO is now restated around; anything judged by eye on TightVNC, since no viewer was attached and no screenshot taken; the eleven declined interaction rows and the fact that nine of them are gestures with no automated coverage either; behaviour over measured latency, which no capture in this bundle measures; and that a gate bundle is a snapshot which nothing re-validates when a later commit lands.

Do not claim more than the `SCOPE.md` files claim. This index is the document most likely to be skimmed instead of the transcripts, which is exactly why it inherits their limits.
  </action>
  <verify>
    <automated>test -s .planning/phases/08.5-v1.0-closeout/evidence/README.md</automated>
    <fails_when>non-zero exit -- the index is missing or zero-length. This is the state before this task.</fails_when>
    <automated>test "$(grep -n 'xsession-errors' .planning/phases/08.5-v1.0-closeout/evidence/README.md | head -1 | cut -d: -f1)" -le 30</automated>
    <fails_when>non-zero exit -- the credential warning is absent from the index entirely (the inner command prints nothing and the comparison errors), or it is buried past line 30 instead of leading the file. It leads because that file is the first one anyone opens when an X session misbehaves.</fails_when>
    <automated>grep -cF -e '9869aea' -e 'f5bbea7' .planning/phases/08.5-v1.0-closeout/evidence/README.md</automated>
    <fails_when>prints fewer than `2` -- the index does not name the commit each per-target transcript was captured at, so a reader cannot tell that the transcripts and the gates belong to different commits. Both hashes come from the transcripts' own provenance headers.</fails_when>
    <automated>for d in gates tightvnc x2go-nxagent INTERACTION-CHECKLIST.md STRESS-RESULTS.md; do grep -qF "$d" .planning/phases/08.5-v1.0-closeout/evidence/README.md || echo "UNINDEXED: $d"; done</automated>
    <fails_when>prints any `UNINDEXED:` line -- an artifact in the bundle is not mentioned in the index that exists to make the bundle navigable.</fails_when>
    <automated>grep -c 'does not establish' .planning/phases/08.5-v1.0-closeout/evidence/README.md</automated>
    <fails_when>prints `0` -- the index has no section stating the bundle's limits, which is the section that stops a skimmer from over-reading three green directories.</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/08.5-v1.0-closeout/evidence/README.md` exists and is non-empty
    - The `~/.xsession-errors` warning appears within the first 30 lines, states the operator's recorded decision against rotation, states that this makes the handling rule more binding rather than less, and names the screen that was run over the transcripts
    - T-8-REMOTE is stated with the rule that anything added later is read before it is committed
    - The `gates/` table has one row per file in the bundle, each naming the blocker or Release Evidence row it satisfies, and every path in it resolves -- no dangling entry
    - The per-tree totals and per-gate exit codes are quoted from `PROVENANCE.txt`, and the ASan sanitizer-report situation is stated either way
    - Both per-target transcripts are indexed with their own capture commits (`9869aea`, `f5bbea7`) and with a pointer to their `SCOPE.md`
    - The dirty tree-state convention is stated once, with the note that it is the established convention across all six transcripts rather than a regression in this phase
    - `INTERACTION-CHECKLIST.md` and `STRESS-RESULTS.md` are both indexed
    - A "what this bundle does not establish" section exists and names at minimum: X2Go over a real connection with the NX proxy, anything judged by eye on TightVNC, the eleven declined interaction rows, behaviour over measured latency, and the snapshot's lack of self-revalidation
    - No claim in the index exceeds what the two `SCOPE.md` files state
  </acceptance_criteria>
  <done>The bundle has an index that a reader reaches before a transcript, carrying the credential warning first and the bundle's limits last.</done>
</task>

</tasks>

## Artifacts this phase produces

Produced by this plan (08.5-05):

| Artifact | State after this plan |
|---|---|
| `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` -- Build And Test Gates rows (L130-241) | Every gate-log citation phase-qualified at this phase's bundle; every pass figure taken from this phase's logs; the Phase 8 snapshot commit gone from the gate rows |
| `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` -- test-surface row (L157-202) | Recomputed by the row's own three commands at HEAD; per-file table rebuilt; hidden-case explanation and recompute instruction preserved |
| `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` -- User Interaction Checklist rows (L409-486) | All twelve rows cite the numbered interaction-table rows; the seven open rows keep their boxes and gain the operator's recorded decision as their reason |
| `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` -- Release Evidence Required rows (L632-661) | Gate rows repointed; the manual interaction row ticked with tester, date, target, commit and the four-value split; section has zero open rows |
| `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` -- Environment Preflight, Robustness and Remote Desktop rows citing gate logs | Phase-qualified |
| `.planning/phases/08.5-v1.0-closeout/evidence/README.md` | Created -- credential warning first, T-8-REMOTE, layout tables for `gates/` and the per-target transcripts, the per-transcript commit correction, and a closing limits section |
| `.planning/phases/08.5-v1.0-closeout/evidence/INTERACTION-CHECKLIST.md` | One correction: the preamble's count of declined rows without automated coverage recomputed and its row numbers listed. No Result value, row or total altered |

Produced by the rest of this gap-closure set: `docs/RELEASE-NOTES.md`'s rewritten
remote-desktop section (08.5-03) and the twelve-file `evidence/gates/` bundle
(08.5-04). The full table is in `08.5-03-PLAN.md`.

## Flagged assumptions (spec-less edge probe)

No `08.5-SPEC.md` exists, so the deterministic edge probe ran over the phase
requirement IDs. All three rows returned `unresolved`; none was auto-resolved
with a backstop and none was dropped. The row bound to this plan:

| Requirement | Category | Probe | Why it stays unresolved |
|---|---|---|---|
| TEST-08 | unclassified | "unclassified -- review manually" | **Assumption, not a verified property.** The probe could not classify the requirement. Manual reading of what is unresolved here: nothing in this repository holds the checklist's figures to the tree. The self-computing acceptance criteria in Task 2 make the row's staleness *detectable* by a command a reader can run, but they run at plan-execution time only -- no test, hook or gate re-runs them on a later commit. So the row can rot again, and this plan narrows the window rather than closing it. Recorded so the next reader does not mistake a recomputed figure for a maintained one. |

Set-wide equality for the gap-closure plan set: 3 probe-surfaced edge items ==
0 authored into `must_haves.truths` + 3 surfaced as flagged assumptions
(RULES-01 and XDIS-05 in `08.5-03-PLAN.md`, TEST-08 in `08.5-04-PLAN.md` and
here, the same item recorded against both plans that touch its record). No
silent drops.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evidence bundle -> signoff record | The checklist asserts things about files it does not open; an unqualified path can mean two phases' bundles |
| Source tree -> stated test surface | A figure in a document has no mechanism holding it to the tree that produced it |
| Operator's recorded decisions -> release gate rows | A declined row and an unrecorded row look identical once collapsed into an unticked box |
| Evidence index -> a skimming reader | The index is read instead of the transcripts, so any over-claim in it propagates further than one in a transcript |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8.5-REC-01 | Tampering | Gate rows in `COMPILED_CODE_BEHAVIOR_CHECKLIST.md` | **high** | mitigate | Every citation phase-qualified, enforced by a negative grep on the unqualified form that prints sixteen lines today; every cited path resolved by a dangling-path check; every figure taken from this phase's log. |
| T-8.5-REC-02 | Repudiation | Ticking a row the operator declined | **high** | mitigate | Three counting gates fix the exact number of open boxes file-wide, in the User Interaction section, and in the Release Evidence section, so both over-ticking and under-ticking fail. Each open row must cite the table row recording the decision. |
| T-8.5-REC-03 | Tampering | The test-surface row's figures | **high** | mitigate | Self-computing criteria: the check recomputes the truth from the tree and greps for it, so it cannot be satisfied by a stale number and does not depend on this plan naming the right one. |
| T-8.5-REC-04 | Spoofing | `evidence/README.md` over-claiming on behalf of the transcripts | medium | mitigate | The index inherits the `SCOPE.md` limits rather than summarising them; a "what this bundle does not establish" section is required and gated. |
| T-8-XSESSION | Information Disclosure | `~/.xsession-errors` | **high** | mitigate | Never read, copied, quoted or committed. The index warns about the file within its first 30 lines, gated, and the warning is not illustrated with its contents. |
| T-8-REMOTE | Information Disclosure | Transcripts referenced from the index | medium | mitigate | The index restates the read-before-commit rule for anything added later; no transcript content is quoted into the index beyond extension names and counts. |
</threat_model>

<verification>
- `grep -cE '`evidence/gates/' COMPILED_CODE_BEHAVIOR_CHECKLIST.md` prints `0`.
- `grep -v '^#' COMPILED_CODE_BEHAVIOR_CHECKLIST.md | grep -cF '295/295'` prints `0`.
- Every `08.5-v1.0-closeout/evidence/gates/` path cited in the checklist resolves to a file on disk.
- The self-computing source-case and registered-test checks both find their recomputed figures in the file.
- The per-file staleness loop prints nothing.
- `grep -c '^- \[ \]' COMPILED_CODE_BEHAVIOR_CHECKLIST.md` prints `13`; the User Interaction section prints `7`; the Release Evidence section prints `0`.
- The User Interaction section cites `INTERACTION-CHECKLIST.md` at least seven times.
- `.planning/phases/08.5-v1.0-closeout/evidence/README.md` exists, carries the credential warning within its first 30 lines, names both transcript commits, indexes every bundle artifact, and has a limits section.
</verification>

<success_criteria>
The record half of success criterion 7 is closed: the release signoff document
certifies the tree that ships, by citations a reader can open, with figures
produced by the commands the document itself supplies. The last open Release
Evidence row is closed on the artifact this phase created, and the rows that
remain open say what the operator actually decided rather than describing an
absence that no longer exists.
</success_criteria>

<output>
Create `.planning/phases/08.5-v1.0-closeout/08.5-05-SUMMARY.md` when done.
Record in it: the three recomputed test-surface figures and the commands that
produced them; the recomputed ticked and open box totals; the one box that
flipped and why; any checklist row deliberately qualified at the Phase 8 bundle
rather than this one, with the reason; and the recount behind the correction to
`INTERACTION-CHECKLIST.md`'s preamble.
</output>

